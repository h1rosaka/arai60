# 問題タイトル
121. Best Time to Buy and Sell Stock
https://leetcode.com/problems/best-time-to-buy-and-sell-stock/description/


# STEP 1 自力で解く。
```py
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        bottom = math.inf
        max_profit = 0
        for price in prices:
            max_profit = max(price - bottom, max_profit)
            bottom = min(price, bottom)
        return max_profit
```

# STEP2 読みやすくする＆他の人の解法を見る

- https://github.com/hayashi-ay/leetcode/pull/52/files#diff-0474f0ee7711182f0e97bb4047531dc4c65356748eafab139512400ac88c5c0bR67
    - step1では、「今日売ったらどうか」を考えたが、「今日買ったらどうか」を配列を後ろから見ることで考える

- https://discord.com/channels/1084280443945353267/1196472827457589338/1196473519689703444
    - その日までの最小値が入った配列Aとpricesの各要素の差分を取り、「各日で取れる最大利益」のリストを作成、maxを取る
    - >scanl min でその日までの最安の値。 zipWith (-) で利益。max を取る


- https://github.com/goto-untrapped/Arai60/blob/1f7d312405a44b21068a54133b07728b38481c9d/121.%20Best%20Time%20to%20Buy%20and%20Sell%20Stock/BestTimeToBuyAndSellStockStep2.java#L54
    - 未来と過去が見えるようになったとして、「ある日i以前の最安値(BUY)」と「ある日i以降の最高値(SELL)」の配列をそれぞれ作成
    - 日毎に買える日の範囲と売れる日の範囲が異なるため、BUYとSELLをそれぞれ先頭から見て要素同士を引き算し、maxをとる


scanl min　→ zipWith　→ max の真似
```py
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        index_to_bottom_so_far = []
        bottom = inf
        for price in prices:
            bottom = min(bottom, price)
            index_to_bottom_so_far.append(bottom)
        index_to_max_profit = [price - bottom_so_far for price, bottom_so_far in zip(prices,index_to_bottom_so_far)]
        return max(index_to_max_profit)
```


# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        bottom = math.inf
        max_profit = 0
        for price in prices:
            max_profit = max(price - bottom, max_profit)
            bottom = min(price, bottom)
        return max_profit
```
計算量
- 時間：O(N)
- 空間:O(1)

# STEP4 レビューFB反映