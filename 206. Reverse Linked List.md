# 問題リンク
https://leetcode.com/problems/reverse-linked-list/


# STEP 1 自力で解く。

- 所要時間
    - 思い付かず、答えを見た。
- 計算量
    - 時間
        - O(N)
        - node_valsへの追加がO(N)、取り出す作業もO(N)なので。
    - 空間
        - O(N)
        - node_valsがO(N)。新しく作った連結リストもO(N)

```python
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node_vals = []
        node = head
        while node:
            node_vals.append(node.val)
            node = node.next

        dummy_head = ListNode()
        node = dummy_head
        for i in range(len(node_vals)):
            node.next = ListNode(node_vals.pop())
            node = node.next

        return dummy_head.next
```

# STEP2 読みやすくする＆他の人の解法を見る　

参考
https://github.com/hayashi-ay/leetcode/pull/13/files の4th




- stack
    - node_valではなくnode自体を保存して、古いつながりを切りつつ連結してる。
    - この場合の方が、step1の解放と違って連結リストは一つしかできないので、メモリの効率は少し良さそう。(O(N)には変わりなしだが。)
    - ただし連結を切ったり付けたりがstep1より複雑で、うっかりバグをつくりそうな気もするが、私が linked list に慣れてないだけなのだろうか


- recursive
    - 参考にしつつ少し変更して書いた
    - 時間: O(N)
        - O(1)のループが一回
    - 空間: O(N)
        - 再帰のスタックがN回分でO(N)

```py
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if head is None:
            return None
        def reverse(prev_node, node):
            if node.next is None:
                node.next = prev_node
                return node
            next_node = node.next
            node.next = prev_node
            return reverse(node, next_node)
        return reverse(None, head)

```
- iterative
    - 再帰とすごく似ている
    - 参考にしつつ微調整
    - 時間:O(N)
        - O(1)のループが一回
    - 空間: O(1)
        - 再帰のスタックがないので。
```py
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        prev_node = None
        while node:
            tmp = node.next
            node.next = prev_node
            prev_node = node
            node = tmp
        return prev_node
```


https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.x5w37bodndgj

>再帰というのは、リストの頭を担当する部下が、何かを下流にお願いして、仕事が返ってきて、自分も何かをすると、全体のリストが逆順になるというものです。
なるほど。自分が先頭に立ってたとして、最後に自分に何が返ってきて欲しいかと言われれば、確かに繋ぎ直した鎖の先頭と尻尾が欲しい。
上に書いたやり方は、自分じゃなくて、最後の人が成果物を返してたが、このやり方は自分で返す。

```py
# 自分の千切る→次の人以降の終わった結果をもらう→その結果に自分のをつける
def reverseListHelper(node):
    # そもそもノードが一つもない時
    if node is None:
        return None, None
    # 最後のノードの時、つまりはノードが一つしかないケースの時は頭も尻尾の自分
    if node.next is None:
        return node, node
    # 千切るまえに次のやつがどこか覚えておく(ちぎったらわからなくなるので)
    node_next = node.next
    # 自分の担当nodeをちぎる
    node.next = None
    # 次以降の番の人たちが全部終わったと仮定してその結果の鎖の頭と尻尾をもらう
    head, tail = reverseListHelper(node_next)
    # 返ってきた逆順の鎖に自分の分をつける
    tail.next = node
    # 前の人に結果を返すような形にしておかないといけないので、返すべきもの返す
    return head, node
new_head, _ = reverseListHelper(head)
return new_head


# 亜種。結果もらう時、実は尻尾はどこにあるか知ってるので教えてもらわなくても良い
def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
    def reverseListHelper(node):
        # そもそもノードが一つもない時
        if node is None:
            return None
        # 最後のノードの時、つまりはノードが一つしかないケースの時は頭も尻尾の自分
        if node.next is None:
            return node
        # 千切るまえに次のやつがどこか覚えておく(ちぎったらわからなくなるので)
        node_next = node.next
        # 自分の担当nodeをちぎる
        node.next = None
        # 次以降の番の人たちが全部終わったと仮定してその結果の鎖の頭をもらう
        new_head = reverseListHelper(node_next)
        # 返ってきた逆順の鎖に自分の分をつける
        tail = node_next
        tail.next = node
        # 前の人に結果を返すような形にしておかないといけないので、返すべきもの返す
        return new_head
    return reverseListHelper(head)

# 元々の再帰と同じ、一個ずつ繋ぎかえるが、社員への指示が違う
# 元々：全体のことは知らんでいいから、目の前のやつひっくり返せ
# これ: ひっくり返し終わったやつと、残りがあるから、一個追加でひっくり返せ
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        
        def reverseListHelper(reversed_head, rest_head):
            if rest_head is None:
                return reversed_head
            next_rest_head = rest_head.next
            next_reversed_head = rest_head
            next_reversed_head.next = reversed_head
            return reverseListHelper(next_reversed_head, next_rest_head)

        return reverseListHelper(None, head)


```
https://github.com/quinn-sasha/leetcode/pull/7#discussion_r1948355100
- stackにしまった直後に、しまったnodeのnode.nextをNoneにしておけば、スッキリするので後から繋ぎ直すときもシンプル


# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        reversed_ = None
        rest = head
        while rest:
            rest_next = rest.next
            rest.next = reversed_
            reversed_ = rest
            rest = rest_next
        return reversed_

```

5:11, 1:45, 1:15

# STEP4 レビューFB反映