# 問題タイトル
1011. Capacity To Ship Packages Within D Days
https://leetcode.com/problems/capacity-to-ship-packages-within-d-days/description/#:~:text=1011.%20Capacity%20To%20Ship%20Packages%20Within%20D%20Days



# STEP 1 自力で解く。
```py
class Solution:
    def shipWithinDays(self, weights: List[int], days: int) -> int:
        """
        探し物: days日で運搬可能な容量のうち、もっとも小さい(左)のもの
        L : ここと、ここより左にはない
        R : ここより右にはない
            L < ありうる範囲　<= R  つまり (L, R]
            FFFFFFFTTTTTTTT
        """
        def can_ship_within_the_period(capacity) -> bool:
            i = 0
            for _ in range(days):
                total_weight = 0
                while total_weight + weights[i] <= capacity:
                    total_weight = total_weight + weights[i]
                    i += 1
                    if i == len(weights): # 載せきった。
                        return True
            return False       
        left = 0
        right = sum(weights)
        while right - left > 1:
            mid = (left + right) // 2
            if can_ship_within_the_period(mid):
                right = mid
            else:
                left = mid
        return right
```      
- 25 分かかったが、一発Accepted。うれしい。
- 実行時間が402msで下から6%なので、もっと速いのがありそう
- 二分探索はO(logN)ですでに速いので、ループの中の処理が遅いのかな?
    - 今の計算量は、min(days, len(weights))に依存。O(N)またはO(M)といった形。
        - 荷物一つずつ見ていってなくなるか、あるいは残り日数がなくなって無理だとわかるまで作業
            - もっと早く打ちきれないか
                - 絶対不可能な場合
                    - capacity超えの荷物があって最初から無理だとわかっている場合
                - 絶対行ける場合
                    - (capacity超えの荷物がない状態で)日数が荷物の数より多い
            - 上記二つの条件を入れてみたが、たいして速くならなかった。
                - 元々O(N)で、入れてもO(N)なので、定数倍しか変わらない。が、O(N)より速くしてO(logN)にするには、二分探索とかをしないといけないはずだが、それはできなさそう。ということは定数倍だが、大きく影響のある定数倍がどこかにあるはず。他の人の解法を見てみる。
            


# STEP2 読みやすくする＆他の人の解法を見る
https://github.com/hayashi-ay/leetcode/pull/55/files#diff-4e146417f14c744a10f851601f26cd2cb17b420ff966720e568f6f5679aa475eR36-R58
```py
    def can_ship(max_capacity):
        day = 1
        capacity = 0
        for weight in weights:
            capacity += weight
            if capacity > max_capacity:
                day += 1
                capacity = weight
        return day <= days
```
- weightsを一回舐めるだけで済むようになっている。（一重ループ）
    - STEP1は実際1日ずつ積んでみて結果どうか確認、みたいなイメージだったが、元の問題的には「必要な大きさの船を知りたい」というプロジェクトの計画段階であることを考えれば、こちらの方が素直でシンプルだったか。
    - ループが一重で済んでいることで、無駄なオーバーヘッド(ジャンプ命令等)を発生させずに済んでいるから速いのかな?(分岐予測が当たりやすい、とかもあるかも)

- leftが0からではなく、max(weights)から始めている。これも少しだけ効きそう。


https://github.com/nanae772/leetcode-arai60/pull/43/files?short_path=7003a5b#diff-7003a5baa3f89ae7ceafbb688dab132a20dfe083744966edff31be3fc901637b
- ちゃんとコード書き始める前に所要時間の見積もりしていて偉いなぁ。。自分もstep2のコード書くところからやる。
- weightsが[]や[0],[-1]だったときの対応、daysが負の時の対処も。
- day を＋１するたびにdaysとの大小比較して、全部見る前にダメなことに気づく作戦の話
    - 自分の手を動かしてやるなら、すべての荷物やるのではなく、days覚えておいて比較しそうな気がする。

https://github.com/docto-rin/leetcode/pull/27/files#diff-7621ca097a66d8850331853241ed62b900277d528a4f2ff8a3a9c56e63ea56a7R129-R144
- dayがdaysになったらFalse返して終了



- 上記を踏まえてSTEP２を書く
- 実行時間
    - 二分探索で解くので、O(logN)。1回あたりのcan_shipの判定が、weightsを全部舐めるO(N)、全体でO(NlogN)
        - 条件は下記の通り
            - 1 <= days <= weights.length <= 5 * 10^4
            - 1 <= weights[i] <= 500
        - するとステップ数は、5 * 10^4 * (log(5) + 4log(10)) →  5 * 10^4 * (15くらい) = 75 * 10^4
        - pythonは大体10^6ステップ1秒でこなせるので、1秒もかからず、問題ない。

```py
class Solution:
    def shipWithinDays(self, weights: List[int], days: int) -> int:
        """
        探し物: days日で運搬可能な容量のうち、もっとも小さい(左)のもの
        L : ここと、ここより左にはない
        R : ここより右にはない
            L < ありうる範囲　<= R  つまり (L, R]
            FFFFFFFTTTTTTTT
        """
        def can_ship_within_the_period(capacity) -> bool:
            day_count = 1
            loaded_weight = 0
            for weight in weights:
                if loaded_weight + weight <= capacity:
                    loaded_weight += weight
                    continue
                loaded_weight = weight
                day_count += 1
            return day_count <= days
   
        left = max(weights) - 1
        right = sum(weights)
        while right - left > 1:
            mid = (left + right) // 2
            if can_ship_within_the_period(mid):
                right = mid
            else:
                left = mid
        return right

```
- 積荷シミュレーション作業の仮装日付として何日目か、をday_countと呼んで管理。１日の終わりに、日めくりカレンダー的にめくってから翌日へ引き継ぎ。





# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def shipWithinDays(self, weights: List[int], days: int) -> int:
        """
        days以内で運び切れるcapacityのうち、最も小さいものを知りたいFFFFFFTTTTT
        L: ここと、ここより左にはない
        R: ここより右にはない
            → ありうる範囲: L < area <= R
        """
        def can_ship(capacity: int) -> bool:
            day_count = 1
            loaded_weight = 0
            for weight in weights:
                if loaded_weight + weight <= capacity:
                    loaded_weight += weight
                    continue
                loaded_weight = weight
                day_count += 1
            return day_count <= days
        
        left = max(weights) - 1
        right = sum(weights)
        while left + 1 < right:
            mid = (left + right) // 2
            if can_ship(mid):
                right = mid
            else:
                left = mid
        return right
        
```
- 二分探索のwhile条件の書き方を、leftがrightより左に来るように書くようにした。
5:55, 4:50, 4:40

# STEP4 レビューFB反映