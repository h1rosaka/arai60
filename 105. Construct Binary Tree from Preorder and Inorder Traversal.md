# 問題タイトル
[105. Construct Binary Tree from Preorder and Inorder Traversal](https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/description/)



# STEP 1 自力で解く。
```py
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:
        if not preorder or not inorder:
            return None
        root_val = preorder[0]
        root = TreeNode(root_val)
        root_index = inorder.index(root_val)
        root.left = self.buildTree(preorder[1:1 + root_index], inorder[:root_index])
        root.right = self.buildTree(preorder[1 + root_index:], inorder[root_index + 1:])

        return root
```
- わからず、答えを見た。
    - 方針：preorderの先頭要素を一つ取り、preorder,inorderそれぞれに関して、左に使えるのはどれか、右に使えるのはどれかを絞って渡して、再帰。
    - 毎回、返すべきrootは、その時使えるpreorderの先頭で決まっている。(preorderの定義的にそう。)
    - preorder = [root, (左部分木), (右部分木)]
    - inorder = [(左部分木), root, (右部分木)]
    - preorderからrootを取る
    - inorderみて、どれが左でどれが右なのか判断。左に入るのはroot_index個の要素である。
    - 再びpreorder見ると、左に入るべき個数がわかってるので、どこまでを左に使うべきか簡単に分けられる


# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/43/files
    - > 細かい点となりますが、「変数 + 1」と「1 + 変数」は、「変数 + 1」 に統一したほうが読みやすいかなと思いました。
    - https://github.com/hayashi-ay/leetcode/pull/43/files#diff-e9b6f8cf3e6bf93b2177690b6fb60c6eaa153a33e3acaf7bad26c23ebd604dcaR83-R103
        - preorderのリストの先頭の数字と同じ数字を、inorderのリストからつまみ上げ、左にあるものは左部分木に。右にあるものは右部分木に。preorderのリストの次の数字に移って再起的に繰り返し。
            - こちらが、preorderを絞らなくて良いのは、結局左→右の再帰にして、毎回先頭から取ると、それって絞らないでずっと先頭から取ってるのと同じだから。
            - preorderの定義的から考えれば、当然。こっちの方がスマートに感じる。
        - preorder: preorderの順で木を作っていく時、このリストの順でさわれば、それが正しい順番
            →ただし、これだけだと、どこの配下につけるべきかの選択肢は複数ある
        - inorder: preorderの順で、inorderの要素を上につまみ上げると、作りたい木ができる。(inなので、rootがどれかわかると、その左右にくるべきものが何なのかわかる)
        - value_to_inorder_index を作ってる
            - https://github.com/olsen-blue/Arai60/pull/29#discussion_r1948256437
                - 実は辞書も内包表記で書ける

    - https://github.com/olsen-blue/Arai60/pull/29/files/70d435a3da11519919566bd6e99157bf6df552c7#r1948645912
        - 副作用は避けた方が無難とのこと。


    - 再帰の中でnonlocalな変数を扱いたくなった時
        - 1. nonlocalで扱う
        - 2. リスト(ミュータブル)の受け渡しで、どこからいつ触っても最新の(共通の)ものを触れるように
        - 3. 再帰関数の帰り値として返してあげて、常に最新のものを触れるように。
            - 再帰関数がreturnするとき、常にその時の最新の状態も一緒に返すことで、呼び出し元に情報連携

```py
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:
        
        value_to_inorder_index = {val: i for i, val in enumerate(inorder)}
        def build_tree(begin: int, end: int, preorder_index: int) -> tuple[Optional[TreeNode], int]:
            if begin >= end:
                return None, preorder_index  
                
            root_value = preorder[preorder_index]
            root = TreeNode(root_value)
            inorder_index = value_to_inorder_index[root_value]        
            preorder_index += 1

            root.left, preorder_index = build_tree(begin, inorder_index, preorder_index)
            root.right, preorder_index = build_tree(inorder_index + 1, end, preorder_index)
            return root, preorder_index

        tree, _ = build_tree(0, len(inorder), 0)
        return tree
```
- treeのleft/rightと重なるので、begin/endにしてみたが、left/rightの方がイメージがしやすかったかもしれない。
- 副作用を避けてみたが、少し複雑になった。

# STEP3　　3回連続でエラーなしで解けるまで解く

```py
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:
        # inorderから指定した数字をつまみ上げてノードにして、その左右に対して再帰的に
        # 数字の指定は、preorderを使って、外から管理
        val_to_inorder_index = {val: i for i, val in enumerate(inorder)}
        preorder_index = 0
        def build_tree(left, right):
            if left >= right:
                return None
            nonlocal preorder_index
            root_val = preorder[preorder_index]
            preorder_index += 1
            root = TreeNode(root_val)
            inorder_index = val_to_inorder_index[root_val]

            root.left = build_tree(left, inorder_index)
            root.right = build_tree(inorder_index + 1, right)
            return root

        return build_tree(0, len(inorder))

```
5:32, 5:09, 4:09
- nonlocalの方が直感的でシンプルに感じたのでこちらを採用した。

- 計算量:
    - 時間:O(N)
        - val_to_inorder_indexの作成：O(N)
        - build_tree: O(N) 各ノード一回ずつ処理。
    - 空間:O(N)
        - val_to_inorder_index：O(N)
        - build_tree: 再帰の深さのスタックメモリ：O(h)　hはlogN ~ N