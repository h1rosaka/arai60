# 問題タイトル
https://leetcode.com/problems/merge-two-binary-trees/

# STEP 1 自力で解く。
```py
class Solution:
    def mergeTrees(self, root1: Optional[TreeNode], root2: Optional[TreeNode]) -> Optional[TreeNode]:
        
        if root1 is None and root2 is None:
            return None
        if root1 is None:
            root1 = TreeNode(0)
        if root2 is None:
            root2 = TreeNode(0)

        node = TreeNode(root1.val + root2.val)
        node.left = self.mergeTrees(root1.left, root2.left)
        node.right = self.mergeTrees(root1.right, root2.right)

        return node

```
- わからず答えを見た
- 手順
    - 上から再帰的に両treeの合計で新たにnode作って繋げて
    - 両方nodeなかったらそこで終了。片方だけあった時は、ない方は"0"があったと仮定して進めて

BFSでも書いてみた
```py
class Solution:
    def mergeTrees(self, root1: TreeNode, root2: TreeNode) -> TreeNode:
        dummy_head = TreeNode(None)
        node_pair_parent_direction = deque([(root1, root2, dummy_head, "left")])
        while node_pair_parent_direction:
            node1, node2, parent, direction = node_pair_parent_direction.popleft()
            if node1 is None and node2 is None:
                continue
            if node1 is None:
                node1 = TreeNode(0)
            if node2 is None:
                node2 = TreeNode(0)
            new_node = TreeNode(node1.val + node2.val)
            if direction == "left":
                parent.left = new_node
            else:
                parent.right = new_node
            node_pair_parent_direction.append((node1.left, node2.left, new_node, "left"))
            node_pair_parent_direction.append((node1.right, node2.right, new_node, "right"))
        return dummy_head.left
```
- BFSだと、新しくノード作った時にその子供となるノードがまだqueueに居なくて指定できず、コネクションが貼れなくて困ったので、貼り方を自分→子供ではなく、親→自分としてみた。
- 親が誰だったのかと、自分が親の左の子だったのか右の子だったのかの情報を、自分が生まれる前に追加で引きついでおく必要がある。


# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/12/files
    - どちらか片方(や両方)がなかった時、rootそのものやそのdeepcopyを返すことで、そこから先の処理はしないで済んでいる(上のstep1は、葉ノードに到達するまで続く)
    - deepcopyの挙動
        - リストやクラスのインスタンスなど、他のオブジェクトを含むオブジェクトをcopyした時の挙動
            - shallow: 新しくオブジェクト作って、中身は元の方への参照を入れる
            - deep: 新しくオブジェクト作って、中身も再帰的にコピーする
        - https://docs.python.org/3/library/copy.html#copy.deepcopy
        - 今回の場合、あるnodeをdeepcopyすると、その先の子/孫...がみな再帰的にコピーされる


- https://github.com/tarinaihitori/leetcode/pull/23/files#diff-1ede2b2a752e6743ca4d35b115594d80caecd186a464943ab76618d8d1811252R43
    - BFSだが私のと少し違う
    - "親が誰か" "左右どちらの子か"の情報の連携の仕方が違い、下記二つで対応している
        - 直接操作したい対象ではなく、その親をqueueに入れる
        - 左右どちらの子のパターンも1回の操作手順の中で両方まとめて行う
    
- https://github.com/tarinaihitori/leetcode/pull/23/files#r1920919690
    - partial()
        - ある関数を受け取って、その引数の一部を固定した新たな関数を返す
        - https://docs.python.org/3/library/functools.html#functools.partial
        - 今回は、コネクションを貼る関数を生成して、操作するnodeたちと一緒にstackに入れておくことで、"誰が親だったのか"、"左右どちらの子か"の情報の引き渡しに使っている。
        - コネクションの貼り方は、親から自分へ。


deepcopyする案を採用
```py
class Solution:
    def mergeTrees(self, root1: TreeNode, root2: TreeNode) -> TreeNode:
        if root1 is None:
            return copy.deepcopy(root2)
        if root2 is None:
            return copy.deepcopy(root1)

        node = TreeNode(root1.val + root2.val)
        node.left = self.mergeTrees(root1.left, root2.left)
        node.right = self.mergeTrees(root1.right, root2.right)

        return node

```


# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def mergeTrees(self, root1: TreeNode, root2: TreeNode) -> TreeNode:
        if root1 is None and root2 is None:
            return
        if root1 is None:
            root1 = TreeNode(0)
        if root2 is None:
            root2 = TreeNode(0)

        node = TreeNode(root1.val + root2.val)
        node.left = self.mergeTrees(root1.left, root2.left)
        node.right = self.mergeTrees(root1.right, root2.right)

        return node
```
3:33, 3:13, 2:43
計算量
- 時間：O(n)
    - 全てのノードを確認する。一回あたりはO(1)
- 空間:O(n)
    - 新しくノード作って木を作成するのに必要
    
