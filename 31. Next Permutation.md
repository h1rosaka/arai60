# 問題タイトル
[31. Next Permutation](https://leetcode.com/problems/next-permutation/description/)



# STEP 1 自力で解く。
- わからなかったので答えを見た。


考え方
- 1 2 3 6 5 4 の次を考える
- 次に大きい数を作りたいわけなので、なるべく右の桁を変更して大きくしたい
    - →変更できる桁の内最も右の桁を探そう
- 後ろから見ていって、6 5 4 は、その部分内での変更だけをしても大きい数にできないので、NG
    - 4は4内での変更なんてそもそもない
    - 54はその中での変更でこれ以上大きくできない
    - 654はその中での変更でこれ以上大きくできない
- つまり、次の数へと大きくするにあたって、「変更できる桁の内最も右の桁」は、その一つ左の桁の数字(3)であり、それを"次の数字"へと交換すると良い
- "次の数字"はどれにすべきか
    - 6 5 4 のなかで、3より大きく、できるだけ小さい（最も右にある）もの → 4
- 一旦"次の数字(4)"を3と交換してあるべきところにハメる
    - 1 2 4 6 5 3
- 4よりあとの部分は、辞書順を考えるなら昇順にならぶべきなので、直す
    - 1 2 4 3 5 6

- 実際に1,2,3,4で上記の考え方でいけるか確認してみる↓
    1,2,3,4
    1,2,4,3
    1,3,2,4
    1,3,4,2
    1,4,2,3
    1,4,3,2
    2,1,3,4
    ...

- 自分で思いつくのは難しいな、、、

- step1
    ```py
    class Solution:
        def nextPermutation(self, nums: List[int]) -> None:
            if len(nums) <= 1:
                return
            
            for i in range(len(nums) - 1, -1, -1):
                if nums[i - 1] >= nums[i]:
                    continue
                target_i = i - 1
                break
            else:
                nums.reverse()
                return

            for i in range(len(nums) - 1, target_i, -1):
                # 後ろから見ていって、最初に見つかったtargetより大きいもの
                if nums[i] > nums[target_i]:
                    swap_i = i
                    break
            nums[target_i], nums[swap_i] = nums[swap_i], nums[target_i]
            nums[target_i + 1:] = sorted(nums[target_i + 1:])
    ```
    
    
    - 最後はsortedである必要はなく、reversedでよかったみたい。
        - なぜなら、swapした後も、引き続き後半部分は右肩下がりに並んでいるので、ひっくり返すだけで良い
        - sortedだとtim sortなので、時間O(NlogN), 空間O(N)
        - reverseなら、(先頭,末尾)から順にswapなので、時間O(N)、空間O(1)で済む。
    
    - Tim sortの復習
        - Insertion SortとMerge　Sortの合わせ技
            - マージソート：時間O(NlogN), 空間O(N)
            - 挿入ソート：時間O(N^2), 空間O(1)
                - ただしほぼ整列済みだとO(N)。さらに小さい配列の時速い。
                - 時間　＝　必要ステップ数　x ステップあたり必要クロック数
                    - 挿入ソートはステップあたりに必要クロック数が少ない(分岐や再帰のオーバーヘッドがなかったり、操作内容的にキャッシュにも乗りやすイノで)
                    - 要素数が小さい間は必要ステップ数(n^2)の影響が小さく、こちらの影響の方が大きい
                    - 例
                        ```
                        Insertion 1操作 = 3サイクル(数字は仮)
                        Heap 1操作      = 30サイクル(数字は仮)
                        Insertion: n²
                        Heap:      n log n
                        n=20 のとき：
                            Insertion: 20^2 × 3 = 1200
                            Heap:      20 log2 20 x 30 ≈ 86 × 30 = 2580
                                → ステップ数が20と小さいので、ステップあたりのクロック数(3,30)の方が影響大きく、逆転
                        ```
                - timsortはこれをうまく利用。先に小さなソート済み列をいくつも作って、後でマージ。
        


# STEP2 読みやすくする＆他の人の解法を見る

- https://github.com/hayashi-ay/leetcode/pull/67/changes
    - "first_decreasing_index_from_right()"のように関数に切り出している。

- https://discord.com/channels/1084280443945353267/1201211204547383386/1232011836543467660
    - c++での実装
        - 大体同じ。targetより大きいもの探すのに二分探索を中で使ってる。
            - しかし、結局そこだけO(logN)になっても、全体はO(N)のまま。逆順の二分探索なので、bisectもそのまま使えない。


- https://github.com/shining-ai/leetcode/pull/50#discussion_r1564830252
    - itertoolsのpermutations()の実装
    - <details><summary>
        - printデバッグ文を足したもの。`list(permutations2("ABCDEF", 3))`でprintが出せる
        - 固定済み | 今動かしてる桁 | 未使用、と言う感じ。
        ```py
            def permutations2(iterable, r=None):
        # iterableからr個選んで順列
        # permutations('ABC', 2) --> AB AC BA BC CA CB
        pool = tuple(iterable)  # 選択肢のpool: ('A', 'B', 'C')
        n = len(pool)  # 3
        r = n if r is None else r  # 2

        # n個しか要素がないのにr個選びたいと言われたら、何もせず抜ける
        if r > n:
            return
        
        # indices：poolの要素の並び順を管理する配列。
        indices = list(range(n))
        # cycles: この桁は、あと何回まわせるか
        cycles = list(range(n, n-r, -1)) 

        # 答えの一つとしてのpermutationを出す。今の並び順の先頭r個。
        yield tuple(pool[i] for i in indices[:r])  
        while n:
            # 一番下の桁から、全ての桁に関してループ回す(r回)
            for i in reversed(range(r)):
                # print("i=", i, "でループ始めます")
                fixed = [pool[x] for x in indices[:i]]
                current = [pool[indices[i]]] if i < len(indices) else []
                right = [pool[x] for x in indices[i+1:]]
                print("    →", fixed, "|", current, "|", right)


                cycles[i] -= 1 # この桁のまわせる回数の残数を-1　→ cycles: [3, 1]

                # その桁をもう回し切っていた場合、indicesとcyclesを元通りに直して、次の桁へ
                if cycles[i] == 0:

                    print("    → この桁は回し切ってました。左から",i+1,"個目以降をリセットします")


                    # リセット：左回転をもう1ステップ回す。"今動かしてる桁i"にいるものを、末尾へ
                    indices[i:] = indices[i+1:] + indices[i:i+1]
                    cycles[i] = n - i
                    fixed = [pool[x] for x in indices[:i]]
                    current = [pool[indices[i]]] if i < len(indices) else []
                    right = [pool[x] for x in indices[i+1:]]
                    print(f"   {fixed} | {current} | {right}   ← リセット後")
                    print("   リセットを終えたので次の桁へ行きます")
                    print("")


                else:
                    j = cycles[i]  # 1 この桁が後何回回せるか
                    # print("    ",i+ 1, "桁目を入れ替えます")
                    indices[i], indices[-j] = indices[-j], indices[i]  # [0,2,1]



                    yield tuple(pool[i] for i in indices[:r])
                    break
            else:
                return
        ```
        </summary>

        </details>
    - ![alt text](image.png)



- https://github.com/Ryotaro25/leetcode_first60/pull/63#discussion_r2011056670
    - 最初に変数用意して、そこから1ずつ減らして、終わったら準備完了、だと"初期化完了"だとわかりにくい
        - 見つけてから代入、の方がわかりやすい

- https://github.com/olsen-blue/Arai60/pull/59/changes#diff-ee8de8f68c4ae03917be0fa643c8defc8592e248688aed43937777d663dbfccdR200-R209
    - おーすごい。こうも書けるのか。やってることは実はほぼ一緒。
        - 123654の例で言うところの、3がpivot,4がswap
            - pivotは一番後ろの一つ手前から見ていくけども、pivotより後ろが全てpivot以下の間(下り坂の範囲の間)は、continueするだけ。
    - 計算量に関するコメント
        - https://github.com/olsen-blue/Arai60/pull/59/changes/BASE..cdb784fbed52aeeb8bd3a65750bb0908591b93b2#r2030531369
        - 654321と並んでいると、pivotがn回、swapがn回で、O(N^2)
        - しかし、もっと早く見つかることもあるので、長さkのところで見つかったとすると、その時のコストはO(k^2)
        - 続いてその確率を考える
            - 知りたいのは、末尾の数字k個が、降順になっている確率
            - k個の数字の並び方はk!通り。その中で全て降順になっているのは1つだけなので1/k!
            - ただしこの状況Aは、末尾k個より前がどうなっているかは考えていない
                - A-1: 末尾k個より一つ前で、降順がSTOP
                - A-2: 末尾k個より一つ前も、降順が続く(その先は考えていない)
            - 今回知りたいのはA-1.
            - P(k) = P(≥k) - P(≥k+1)
            - = 1/k! − 1/(k+1)! = k/(k+1) * 1/k!
        - 確率とコストかけて期待値出して、k/k+1が常に1以下であることを使うと、抑えられる。




# STEP3　　3回連続でエラーなしで解けるまで解く

```py
"""
12"3"654
12"4"65"3"
124356

「後ろから見ていって段差が見つかったけど、その後swap対象がなかった」は発生しない
    →段差があったってことは段差の落ち先より大きいものが絶対右側にあるので。
"""
class Solution:
    def nextPermutation(self, nums: List[int]) -> None:
        for i in range(len(nums) - 2, -1, -1):
            if nums[i] >= nums[i+1]:
                continue 
            for swap_candidate_i in range(len(nums) - 1, -1, -1):
                if nums[i] >= nums[swap_candidate_i]:
                    continue
                nums[i], nums[swap_candidate_i] = nums[swap_candidate_i], nums[i]
                nums[i + 1:] = reversed(nums[i + 1:])
                return
        nums.reverse()

```

- reversed()
    - https://docs.python.org/ja/3/library/functions.html#reversed
    - >要素を逆順に取り出すイテレータ (reverse iterator) を返します
    - 配列渡したらリバースした配列渡してくれてるものかと勘違いしていた。
    - `nums[i+1:] = reversed(nums[i+1:])`は動く
        - スライス代入はイテラブルを受け付ける
        - > The assigned object should be iterable. 
            - https://docs.python.org/ja/3/reference/simple_stmts.html#assignment-statements:~:text=The%20assigned%20object%20should%20be%20iterable.

# STEP4 レビューFB反映