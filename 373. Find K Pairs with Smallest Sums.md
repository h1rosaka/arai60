# 問題
https://leetcode.com/problems/find-k-pairs-with-smallest-sums/description/


# STEP 1 自力で解く。

```py
"""
nums1からの要素とnums2からの要素の組み合わせを、2次元のgridで整理する
ある組み合わせをsmallest_pairsに入れたら、その右と下を確認したい
しかし、右への動きとして到達した新たなマスが、別のマスからの下への動きとして重複して到達することがある
→下への動きは、一番左の列でのみ実施する。
    →2行目の一番左のマスを候補入れておきながら、1行目のマスを一つずつ右へ見ていく
    →2行目の一番左のマスがpopされたら、3行目の一番左のマスを入れて、同じことをしていく

まず左上スミのマスをcandidatesに追加
pairsにk個入るまでループ
    candidatesから最小のものをpop
    その右のマスをcandidates追加
    (初回含め、一番左のマスがpopされた時だけ)その下のマスもcandidates追加

girdのイメージ:
　　　　　index2→
index1↓ 
"""
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        candidates = [(nums1[0] + nums2[0], 0, 0)]
        heapq.heapify(candidates)
        smallest_pairs = []
        while len(smallest_pairs) < k:
            _, index1, index2 = heapq.heappop(candidates)
            smallest_pairs.append([nums1[index1], nums2[index2]])
   
            if index2 + 1 < len(nums2):
                candidate = (nums1[index1] + nums2[index2 + 1],
                             index1,
                             index2 + 1)
                heapq.heappush(candidates, candidate)

            if index2 != 0:
                continue
    
            if index1 + 1 < len(nums1):
                candidate = (nums1[index1 + 1] + nums2[index2],
                             index1 + 1,
                             index2)
                heapq.heappush(candidates, candidate)

        return smallest_pairs 

```
コメント
- 全て要素を入れてからソートして、、とやろうとしたが、Memory limit Exceedで解けなかった。
- 上記の解法は下記の他の方のものを参考にして書いた。

https://github.com/hayashi-ay/leetcode/pull/66/files#diff-c26614d26c37c5ff5b5b65c6bd9c8794ce69288118e1e2c8a184fa9e335b28bfR75-R98

- 全部heapに入れてから作業するのではなく、追加しながら同時にsmallest_sum_pairsも作る。
- 二次元のマスの左上から塗りつぶしていけるように条件付け(自身の左と上が既に入っている時だけ入れる)して制御することで、まだすべてheapに入れてない状況でも、各時点で一番小さいものを確実に取れるようにしている

https://github.com/hayashi-ay/leetcode/pull/66/files#diff-c26614d26c37c5ff5b5b65c6bd9c8794ce69288118e1e2c8a184fa9e335b28bfR111-R122
- 下のマスを一つとっておいて、それ以外は右のマスをどんどん足していく。もし下のマスの方が小さくてheapからなくなったら、今度はそこを起点に下のマス一つとっておいて、右のマスを確認していく
- 左上の最初のマスがpairsに入った後、その次に候補になるのは、右のマスか下のマス。
- ただし、毎回右と下のマスを追加すると、重複する可能性があるので、下のマスを足すのは一番左の列でだけとする。
- こうすると、じわじわ左上から探索できる。(そして結局一つ上の解法の、"自身の左と上が既に入っている時だけ"と同じ形になる)


https://github.com/Yoshiki-Iwasa/Arai60/pull/9#discussion_r1647019606
- いろいろなやり方がある。


# STEP2 読みやすくする＆他の人の解法を見る

```py
"""
candidates
seen = set()
smallest_pairs

candidateからpopして、smallest_pairsに追加
popしたものの、右と下を追加。ただし、「本当に追加する必要あるかのチェック」をする

「本当に追加する必要あるかのチェック」
・1.girdの外側なら追加する必要なし
・2.自身の左と上が両方 seen に入っていないなら、(もっと良い候補がいるということなので、まだ今は)追加しない
・3.一番左の列や一番上の行は、2の条件に引っかかってしまうが、無視したい。これらのマスが追加候補になるときは、追加して良い時のみ

"""
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:

        candidates = [(nums1[0] + nums2[0], 0, 0)]
        seen = set([(0,0)])
        smallest_pairs = []

        def need_to_be_added(index1, index2):
            if len(nums1) <= index1 or len(nums2) <= index2:
                return False
            if index1 == 0 or index2 == 0:
                return True
            return (index1 - 1, index2) in seen and (index1, index2 - 1) in seen


        def add_if_necessary(index1, index2):
            if need_to_be_added(index1, index2):
                heapq.heappush(candidates, (nums1[index1] + nums2[index2], index1, index2))
               

        while len(smallest_pairs) < k:
            _, index1, index2 = heapq.heappop(candidates)
            smallest_pairs.append([nums1[index1], nums2[index2]])
            seen.add((index1, index2))
            add_if_necessary(index1 + 1, index2)
            add_if_necessary(index1, index2 + 1)
        return smallest_pairs

```




# STEP3　　3回連続でエラーなしで解けるまで解く
STEP 1とロジックは同じ。
index1, index2は何度も出てくる割に長くて視認性が悪く、何度もミスに繋がったのでi,jにしてみた。

```py
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        smallest_pairs = []
        candidates = [(nums1[0] + nums2[0], 0, 0)]

        while len(smallest_pairs) < k:
            _, i, j = heapq.heappop(candidates)
            smallest_pairs.append([nums1[i], nums2[j]])

            # 右方向
            if j + 1 < len(nums2):
                candidate = (nums1[i] + nums2[j + 1], i, j + 1)
                heapq.heappush(candidates, candidate)

            # 下方向を足すのは、一番左の列にいる時だけにして、重複を避ける
            if j != 0:
                continue

            # 下方向
            if i + 1 < len(nums1):
                candidate = (nums1[i+1] + nums2[j], i + 1, j)
                heapq.heappush(candidates, candidate)
            
        return smallest_pairs


```
7:04, 6:58, 8:00

    
# STEP4 レビューFB反映