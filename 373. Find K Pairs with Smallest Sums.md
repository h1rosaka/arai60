# 問題
https://leetcode.com/problems/find-k-pairs-with-smallest-sums/description/


# STEP 1 自力で解く。

```py
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        heap = []
        pairs = dict()
        for num1 in nums1:
            for num2 in nums2:
                sum_ = num1 + num2
                heappush(heap, sum_)
                pairs[sum_] = pairs.get(sum_, []) + [(num1, num2)] 

        smallest_sum_pairs = []
        while len(smallest_sum_pairs) < k:
            sum_ = heappop(heap)
            smallest_sum_pairs += pairs[sum_]
        
        return smallest_sum_pairs[:k]

```
コメント
- heapという名前だと何が入ってるのかわからない
- pairsより、sum_to_pairsの方が良い
- pairs.get(sum_, []) より　setdefault の方がわかりやすい
- なんとなく一番小さいものを取る必要があると思ったからheapにしたが、全部要素入れ終わるまで取り出さないのなら、普通の配列に足してから後でソートの方が良い。
    - 追加するとき O(nlogn) で変わらず
    - 取り出す時　O(klogn) →　O(k)


# STEP2 読みやすくする＆他の人の解法を見る

```py
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        sums = []
        sum_to_pairs = dict()
        for num1 in nums1:
            for num2 in nums2:
                sum_ = num1 + num2
                sums.append(sum_)
                sum_to_pairs.setdefault(sum_, [])
                sum_to_pairs[sum_] += [(num1, num2)] 
        sums.sort(reverse=True)

        smallest_sum_pairs = []
        while len(smallest_sum_pairs) < k:
            sum_ = sums.pop()
            smallest_sum_pairs += sum_to_pairs[sum_]
        
        return smallest_sum_pairs[:k]

```


https://github.com/hayashi-ay/leetcode/pull/66/files#diff-c26614d26c37c5ff5b5b65c6bd9c8794ce69288118e1e2c8a184fa9e335b28bfR75-R98

- 全部heapに入れてから作業するのではなく、追加しながら同時にsmallest_sum_pairsも作る。
- 二次元のマスの左上から塗りつぶしていけるように条件付け(自身の左と上が既に入っている時だけ入れる)して制御することで、まだすべてheapに入れてない状況でも、各時点で一番小さいものを確実に取れるようにしている

https://github.com/hayashi-ay/leetcode/pull/66/files#diff-c26614d26c37c5ff5b5b65c6bd9c8794ce69288118e1e2c8a184fa9e335b28bfR111-R122
- 下のマスを一つとっておいて、それ以外は右のマスをどんどん足していく。もし下のマスの方が小さくてheapからなくなったら、今度はそこを起点に下のマス一つとっておいて、右のマスを確認していく
- 左上の最初のマスがpairsに入った後、その次に候補になるのは、右のマスか下のマス。
- ただし、毎回右と下のマスを追加すると、重複する可能性があるので、下のマスを足すのは一番左の列でだけとする。
- こうすると、じわじわ左上から探索できる。(そして結局一つ上の解法の、"自身の左と上が既に入っている時だけ"と同じ形になる)


https://github.com/Yoshiki-Iwasa/Arai60/pull/9#discussion_r1647019606
- いろいろなやり方がある。



# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        candidates = [(nums1[0] + nums2[0], 0, 0)]
        heapq.heapify(candidates)
        pairs = []

        while len(pairs) < k:
            _, index1, index2 = heapq.heappop(candidates)
            pairs.append((nums1[index1], nums2[index2]))

            if index2 + 1 < len(nums2):
                candidate = (nums1[index1] + nums2[index2 + 1],
                             index1,
                             index2 + 1)
                heapq.heappush(candidates, candidate)

            if index1 != 0:
                continue
            
            if index1 + 1 < len(nums1):
                candidate = (nums1[index1 + 1] + nums2[index2],
                             index1 + 1,
                             index2)
                heapq.heappush(candidates, candidate)

        return pairs 

```
8:11, 7:43, 7:20
計算量
- 時間 O(klogk)
    - whileループがk回
        - 1回あたり logk
            - heappush分かかる
                - k回しかループ回らないので、要素追加する回数もk回であり、heapの要素数も最大k
            
- 空間 O(k)
    - candidates　O(k)
    
# STEP4 レビューFB反映