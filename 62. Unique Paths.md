# 問題タイトル
https://leetcode.com/problems/unique-paths/description/



# STEP 1 自力で解く。

```py
class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        def kaijyou(x):
            result = 1
            while x > 0:
                result *= x
                x -= 1 
            return result
        return kaijyou(m+n-2) // (kaijyou(n-1) * kaijyou(m-1))
```
- 下記の通り色々エラーを起こしながら、解けた
    - なぜか!で階上が計算できると思ったが、そんなことはなかった
    - /だと少数の割り算になってしまう
- conbinationを計算する関数があった気がするが忘れた。階乗もきっとある。
- nCmはmath.comb(n,m)
    - https://docs.python.org/ja/3/library/math.html#math.comb
- nPmは
    - nCmはmath.perm(n,m)
    - https://docs.python.org/ja/3/library/math.html#math.perm
- 階乗はmath.factorial(n)
    - https://docs.python.org/ja/3/library/math.html#math.factorial
    - 他にも最大公約数とか色々ある様子
- /
    - 除算 (/) は常に浮動小数点数を返します
        - https://docs.python.org/ja/3.13/tutorial/introduction.html?utm_source=chatgpt.com#:~:text=%E9%99%A4%E7%AE%97%20(/)%20%E3%81%AF%E5%B8%B8%E3%81%AB%E6%B5%AE%E5%8B%95%E5%B0%8F%E6%95%B0%E7%82%B9%E6%95%B0%E3%82%92%E8%BF%94%E3%81%97%E3%81%BE%E3%81%99
- //
    - > (-11) // 4 は -2.75 を 小さい方に 丸める (訳注: 負の無限大への丸めを行う) ので -3 になることに注意してください。
    - https://docs.python.org/ja/3.13/glossary.html#term-floor-division


```py
class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        num_ways = [[0] * n for _ in range(m)]
        num_ways[0] = [1] * n
        for i in range(m):
            num_ways[i][0] = 1
        for row in range(1, m):
            for col in range(1, n):
                num_ways[row][col] = num_ways[row - 1][col] +  num_ways[row][col - 1]
        return num_ways[m - 1][n - 1]
```
- 手作業でマスを埋めていくイメージ。小学生の時に良くやった。



# STEP2 読みやすくする＆他の人の解法を見る

- https://github.com/hayashi-ay/leetcode/pull/39/files#diff-3d04158956739fa9e3948a043cdcf2a3246498baedcc5098bf90ee92cad790d0R129-R130
    - マス埋める解放で、一番左の列と一番上の列を先に埋めるのではなく、ループの中で該当するマスだったら1と言うロジックで埋めている。スマート。
    - returnするものも、`num_ways[-1][-1]`と書いている。こっちの方が右下のはじっこ感が出て良いかも

- https://github.com/hayashi-ay/leetcode/pull/39/files#diff-3d04158956739fa9e3948a043cdcf2a3246498baedcc5098bf90ee92cad790d0R26-R36
    - 実は全部のマスの情報を覚えておく必要はなくて、一列分だけのメモリがあれば十分考えていける、という解法。
        - 最初に全部1で埋めて、一番左の列は更新せず、2列目から、「元の自身の値+左隣の値」に更新


- https://github.com/hayashi-ay/leetcode/pull/39/files#diff-3d04158956739fa9e3948a043cdcf2a3246498baedcc5098bf90ee92cad790d0R48
    - @chacheを活用している
        - 階乗は同じ計算を後でもすることになるので、メモしておくとお得


- 再帰と計算量
- https://github.com/olsen-blue/Arai60/pull/33/
```py
class Solution:
    @cache
    def uniquePaths(self, m: int, n: int) -> int:
        if m == 1 or n == 1:
            return 1
        return self.uniquePaths(m-1, n) + self.uniquePaths(m, n-1)
```
- 計算量を考える
- 最後のマスから、2分木を1のマスが出るまで作り続けることを考える(道順の逆向きに進むイメージ)
- ノードの数に比例して、計算量が決まるので、ノードの数(N)を求めたい。
- 葉ノードの数から間接的に求める
    - 内部ノードの数をI、葉ノードの数をL、全ノードの数をNとおくと、N = 2L - 1 になる。以下ロジック説明。   
        - num_edges = N - 1
            - 木では常に、これが成り立つ。(根以外のすべてのノードには必ず親が1つあり、edgeを親を指すポインタと考えるとわかりやすい)
        - num_edges = 2I 
            - 内部ノードは必ず(m-1,n) と (m,n-1) の2つを呼ぶため、必ず2つの子を持ち、同時に2つの辺を持つ
        - N = I + L 
        - 上記3式から、　N - 1 = 2I さらに、　N - 1 = 2(N - L) よって、　N = 2L - 1 となって「Nは葉の数の2倍-1」とわかる

    - L は m+n-2Cm-1
        - この木は、すべて末端(葉ノード)が1になっていて、葉ノードの数が、知りたかった行き方の数になる
        - 行き方の数はm+n-2Cm-1

    - よって、N =  2 * m+n-2Cm-1 - 1


# STEP3　　3回連続でエラーなしで解けるまで解く

```py
class Solution:
    def uniquePaths(self, m: int, n: int) -> int:
        grid = [[0] * n for _ in range(m)]
        for row in range(m):
            for col in range(n):
                if row == 0 or col == 0:
                    grid[row][col] = 1
                else:
                    grid[row][col] = grid[row - 1][col] + grid[row][col - 1]
        return grid[-1][-1]

```
1:59, 2:20, 1:32

計算量
- 時間:O(m*n)
    - 1マスずつ処理
- 空間:O(m*n)
    - gridで使用





# STEP4 レビューFB反映