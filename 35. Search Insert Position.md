# 問題タイトル
[35. Search Insert Position](https://leetcode.com/problems/search-insert-position/description/)

# STEP 1 自力で解く。
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        # 2分探索したい
        # ok_i: target未満　ng_i: target 以上
        # 最初にngになるindexが知りたい
        ok_i = -1
        ng_i = len(nums)
        while ng_i - ok_i > 1: # ng_iとok_iが隣同士になったらストップ
            mid_i = (ng_i + ok_i) // 2
            if nums[mid_i] < target:
                ok_i = mid_i
            else:
                ng_i = mid_i
        return ng_i  
```
- 昔どこかで見つけた、めぐる式二分探索、というもので書いてみた。
    - https://x.com/meguru_comp/status/697008509376835584
    - https://qiita.com/drken/items/97e37dd6143e33a64c8c
- イメージ
    - 知りたいこと:okとngの境界
    - 定規の左端と右端にポストイットを貼る(絶対OKな位置と絶対NGな位置)
    - 真ん中を計算し、真ん中がOKだったらokポストイットをそこへ、NGだったらNGポストイットをそこへ.
    - ポストイット同士が隣に並んだら、右のNGポストイットが初めてNGになる位置なので、答え。
- 最初_iをつけておらず、何がindexなのかわかりにくくなって、なんどかWAになりながら書いた。
    - mid < targetとかいてしまっていた(midはindexなので、targetと比較しても意味ない)
    - ng = len(nums) + 1　と書いた(絶対NGなものはlen(nums) + 1個目ではあるが、そのindexはlen(nums))
- 配列の長さ0の時困るな
    - 長さ0で、`mid_i=(-1 + 0)//2 = -1`となってnums[-1]を見ようとして要素がなくてIndexError
        - //は負の方向に丸めるfloorなので、-0.5は-1になる


# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/rossy0213/leetcode/pull/26#discussion_r1698088949
    - >コードのパターンで覚えて書くのではなく、探索範囲が閉区間・半開区間・開区間のどれなのかを意識し、範囲を狭めるときに、範囲の端をどこまで狭めればよいかを意識して書くとよい
    - なるほど。STEP1は区間というイメージはあまり持っていなかったが、「区間の中から、ターゲット以上の最初の要素のインデックスを探す」と言う方式でやってみる。


閉区間
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        """
        閉区間 [low, high] を使用して、ターゲット以上の最初の要素のインデックスを返す
        """
        low = 0
        high = len(nums) - 1
        # ターゲット以上のものが見つからない場合、挿入位置は len(nums) となるため、これを初期値とする
        ans = len(nums) 
        # ギリギリ low <= high つまりlow = high の時、確認する範囲内に要素がまだ一つある。それ以上lowが大きくなったら終了
        while low <= high:
            mid = (low + high) // 2
            if target <= nums[mid] :
                # nums[mid] はターゲット以上の要素。midと言う以前よりマシな解が見つかったので更新
                ans = mid
                # より小さいインデックスにターゲット以上の要素がないか、左側 [low, mid - 1] を次回探索
                high = mid - 1
            else:
                # nums[mid] はターゲットより小さいので、答えは右側にある。
                # 右側 [mid + 1, high] を次回探索
                low = mid + 1
        # 最終的に ans にはターゲット以上の最初の要素のインデックス（または len(nums)）が残る
        return ans

```
- 「答えをこの中から探していきます。暫定解(ターゲット以上の要素)が見つかったら、それで更新します」という感じ。


半開区間
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        """
        半開区間 [low, high) を使用して、ターゲット以上の最初の要素のインデックスを返す
        """
        low = 0
        high = len(nums) # 配列のサイズ（含まない右端）
        # ターゲット以上のものが見つからない場合、挿入位置は len(nums) となるため、これを初期値とする
        ans = len(nums)
        # ギリギリ1つ差で low < high　の時、確認する範囲内に要素がまだ一つある。それ以上lowが大きくなったら終了
        while low < high:
            mid = (low + high) // 2
            if target <= nums[mid]:
                # nums[mid] はターゲット以上の要素。midと言う以前よりマシな解が見つかったので更新
                ans = mid
                # より小さいインデックスにターゲット以上の要素がないか、左側 [low, mid) を次回探索。
                high = mid
            else:
                # nums[mid] はターゲットより小さいので、答えは mid の右側にある。
                # 右側 [mid + 1, high) を次回探索
                low = mid + 1
        return ans 
```

開区間
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        """
        開区間 (low, high) を使用して、ターゲット以上の最初の要素のインデックスを返す
        """
        low = -1                 # 範囲外のインデックス（含まない左端）
        high = len(nums)          # 範囲外のインデックス（含まない右端）
        # ターゲット以上のものが見つからない場合、挿入位置は len(nums) となるため、これを初期値とする
        ans = len(nums) 
        while low < high - 1:    # low と high がギリギリ2個差なら、まだ確認すべき要素が一つある
            mid = (low + high) // 2
            if target <= nums[mid]:
                # nums[mid] はターゲット以上の要素。midと言う以前よりマシな解が見つかったので更新
                ans = mid
                # より小さいインデックスにターゲット以上の要素がないか、左側 (low, mid) を次回探索。
                high = mid
            else:
                # nums[mid] はターゲットより小さいので、答えは mid の右側にある。
                # 右側 (mid, high) を次回探索
                low = mid
        return ans
```

- https://github.com/hayashi-ay/leetcode/pull/40/files#diff-912c29ce0f491ca88b1474ced97db89f2d33a36a34cd0f516f5b523e400af176R6
    - >pythonではオーバーフローを考慮しなくて良いが、考慮する必要がある言語だと mid = left + (right - left) // 2のようにする。
    - left + rightは大きすぎて32ビット整数の最大値を超えてしまう、といったときでも、知りたいのは真ん中だから、真ん中までの距離出して足し算すればいける、と言う話。

- https://github.com/hayashi-ay/leetcode/pull/40/files#diff-912c29ce0f491ca88b1474ced97db89f2d33a36a34cd0f516f5b523e400af176R8
    - 半開区間。ansは使っていない。
    - なぜans使ってなくて、このロジックで答えになるのか
        - `return left`しているが、この時点で`left == right `なので、実質`return right`
        - 上記の私の半開区間のコードよくみると、ansを更新したときというのはmidの左側を見ようということでrightもansと同じ値にして`[left, ans)`をみるようにしている。つまり実質一緒。
        - ansが更新される時ってこういう時だよね！とわかっていれば書けるのかもしれないが、ちょっと難しい気もする。もしかしたら全然違うことを考えて書いているのかもしれない。どんなふうに考えているのだろう。


- https://docs.python.org/ja/3/library/bisect.html#bisect.bisect_left
    - bisect.bisect_left(a, x, lo=0, hi=len(a), *, key=None)
        - ソートずみのリストaにxを挿入したとして、挿入後のリストの中でのxのindexを返す。すでにxと同じものがが入ってたらその左の位置に挿入する。
        - Cpythonの実装
            - https://github.com/python/cpython/blob/cfbdce72083fca791947cbb18114115c90738d99/Lib/bisect.py#L74
                - 半開区間でやっている。上のhayashiさんと同じ。どんなふうに考えてるんだろう。

- https://github.com/Fuminiton/LeetCode/pull/41
    - `if nums[mid] == target: return mid`としている。こんなやり方もあるのか。
    - もしかしたら他にももっと色々あるのかもしれない。二分探索は書き方の自由度が高いな、、

# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        if not nums:
            return 0
        # ok: target未満, ng: target以上
        ok_i = -1
        ng_i = len(nums)
        while ng_i - ok_i > 1:
            mid_i = (ok_i + ng_i) // 2
            if nums[mid_i] < target:
                ok_i = mid_i
            else:
                ng_i = mid_i
        return ng_i
```

# STEP4 レビューFB反映

コメントいただいた後の追加学習メモ

- https://github.com/KentaroJay/Leetcode/pull/18#discussion_r2345947467
    ```
    1.入力はこういう制約のものである
    2.そこに、left, right という変数を置く、これはこういう不変条件を満たす変数である
    3.ループを回すときに、middle という位置について計算をする、それは left, right との大小関係等こういう性質を持っている
    4.middle での値によって、left, right をこのように更新すると、不変条件を満たしたままにできる
    5.更新をしていくと while をこういう条件で抜ける、この際にある式で表される値は left, right の不変条件から求めるものになっている
    6.これが必ず停止することは、ある値が狭義単調減少であることから言える
    ```


- https://discord.com/channels/1084280443945353267/1196498607977799853/1269532028819476562
    > 二分探索を、 [false, false, false, ..., false, true, true, ture, ..., true] と並んだ配列があったとき、 false と true の境界の位置を求める問題、または一番左の true の位置を求める問題と捉えているか？
    位置を求めるにあたり、答えが含まれる範囲を狭めていく問題と捉えているか？
    範囲を考えるにあたり、閉区間・開区間・半開区間の違いを理解できているか？
    用いた区間の種類に対し、適切な初期値を、理由を理解したうえで、設定できるか？
    用いた区間の種類に対し、適切なループ不変条件を、理由を理解したうえで、設定できるか？
    用いた区間の種類に対し、範囲を狭めるためのロジックを、理由を理解したうえで、適切に記述できるか？


- https://discord.com/channels/1084280443945353267/1416440171682988133/1423118134830694400
    - >ただ、その約束している内容としては可能性の話をするよりは具体的に否定している内容を書いたほうがいいでしょう。「0 から left - 1 (両端を含む)範囲すべてを開いたとして、それはすべて target 以下である。target と同じ値かもしれないが、target よりも大きい数字は存在しない。」ということですね。

- https://discord.com/channels/1084280443945353267/1227464441235509308/1229628764443643935
    >条件を満たすものを1つ探している場合、
        条件を満たすもののうち左端を探している場合、
        条件を満たすところと満たさないところの切れ目を探している場合(上とほぼ同じ)、
        あと、閉区間か、開閉区間か、などがあって

- https://github.com/TORUS0818/leetcode/pull/44#discussion_r1997506571
    - https://topcoder-g-hatena-ne-jp.jag-icpc.org/cafelier/20120703/1341324546.html
        - とてもわかりやすい。
        - >不変条件: 「常に答えは半開区間 (L, R] の範囲にある」
        - >以下、意地でもこの条件を崩さないことだけを念頭においてコーディング。
        - 「答えがどこにあるはずなのか」を決めて、それを狭めていって、範囲が残り1個になるまでやる。
        ```C#
        private int X = 1 以上 9_9999_9999 以下の秘密の整数;
        bool query(int x) { return X <= x; }
        int solve(){
            int L = 0;            // 条件を満たさないとわかっている数を適当に
            int R = 10_0000_0000; // 条件を満たすとわかっている数を適当に
            // 不変条件: 「常に答えは半開区間 (L, R] の範囲にある」
            while( R-L > 1 ) {// 答えの範囲が整数一つになるまで繰り返し
                int C = (L+R)/2; // 二分探索なので真ん中をとる
                (query(C) ? R : L) = C;
                // (query(C) ? L : R) = C; ではない。(L,R] が答えの範囲なので R が条件満たす方、L が満たさない方
            }
          return R; // 答えは (L,R] にあるのだから L じゃなくて R が答え
        }
        ```

- https://github.com/TORUS0818/leetcode/pull/44#discussion_r1997503302
    - 要素を探しているものの他に、境界を探しているパターンがある


- https://github.com/rm3as/code_interview/pull/9/files#r1565909542
    - >_i は index を表す接尾辞でしょうか。あまり見かけず、違和感があります。 target_index とフルスペルで書いたほうが、読み手に分かりやすいと思います。
        - なるほど。


- https://github.com/nittoco/leetcode/pull/28#discussion_r1680204548
    - >smaller < answer <= larger_or_equal を満たすというのが不変条件ですね。
        - なるほど。cafelierさんの「常に答えは半開区間 (L, R] の範囲にある」は言い換えると、L < answer <= Rをずっと満たしているということ。


- https://github.com/olsen-blue/Arai60/pull/13#discussion_r1912332672
    - >あとは、middle の制約。left - 1 の情報と right + 1 の情報は分かっているのだから、left <= middle <= right となるようなものを調べれば、少なくとも区間が1つは短くなりますね。(効率的かはともかく。)つまり、意味がある仕事をループのたびにしていて、仕事が進んでいます。
    - あまり考えていなかったが、確かにmiddleは仕事が進むようなものを選ばないといけない。どういう条件を満たすようにmiddleを選べば、少なくとも仕事が一つは進むのかの意識。

- https://github.com/olsen-blue/Arai60/pull/13#discussion_r1912342033
    >Python の bisect.bisect_left bisect_right は、境界を探すので上の方法とは違う意味付けで作られています。
        https://github.com/python/cpython/blob/main/Lib/bisect.py
        余裕があれば読んでみてください。
        「前任者までが何を保証していて、自分は何を調べて、その結果、自分は何を保証しているのか。終了条件、middle の制約、初期値。」「それをどう変数で表現するか。」が読み取れれば再現できるでしょう。
    - https://github.com/olsen-blue/Arai60/blob/41171886bb6299943cdcdf8e92e08bdc2833580f/349.%20Intersection%20of%20Two%20Arrays.md#step5%E5%89%8D%E6%97%A5%E3%81%BE%E3%81%A7%E3%81%AE%E4%BF%9D%E8%A8%BC%E5%86%85%E5%AE%B9%E6%9C%AC%E6%97%A5%E3%81%AE%E8%AA%BF%E6%9F%BB%E5%86%85%E5%AE%B9%E7%B5%82%E4%BA%86%E6%9D%A1%E4%BB%B6middle%E3%81%AE%E5%88%B6%E7%B4%84%E5%88%9D%E6%9C%9F%E5%80%A4%E3%81%AB%E3%81%A4%E3%81%84%E3%81%A6%E6%96%87%E5%AD%97%E3%81%AB%E8%B5%B7%E3%81%93%E3%81%99

- https://github.com/seal-azarashi/leetcode/pull/38#discussion_r1845409634
    - >あとは、停止性の議論がありますね。
        閉区間の場合、left <= middle <= right ですね。
        left = middle + 1;またはright = middle - 1;が起き、right - left は必ず1以上減っていくので、0未満になりますね。
        半開区間の場合は、left <= middle < right ですね。
        left = middle + 1;またはright = middle;が起きると、やはり right - left は必ず1以上減っていき、0以下になりますね。
    - 本当にループ終わるんだっけ、も考える。

- https://github.com/potatokun1999/leetcode/pull/1#discussion_r1957326621
    >まず、「何を探そうとしているのか。」をはっきりさせます。
    はっきりするというのは、他人でも分かる状態にすることです。たとえば、[1] * 10 で target=1 で 5 が返ってきたときにそれは正解なのか正解でないのか。
    次に、「ループが回っている中間状態で」変数が何を意味しているのかです。(上で説明していますね。)
    その後、「なぜループが無限ループに至らないのか」を説明します。
    そして、関数を抜けたときに何が起きて、もともとの探そうとしているものが出てくることを説明できます。

- https://github.com/TORUS0818/leetcode/pull/43#discussion_r1970989476
    - 不変条件を意識して書く
    - 不変条件を意識せずとも型を固定すれば書けなくはないが、いろんな書き方があるので、他の人が書いたものを読めるようになるには、何が不変条件なのか意識しないと読めない。 

- https://github.com/TORUS0818/leetcode/pull/43#discussion_r1980589196
    - >まずは「何を探しているのか」「ループごとに今どこまで分かっているのか」「それをどう変数に表現しているのか」というのが意味の部分の話で、あとはループの中で「終了条件」「更新」「必ず終了すること」という形式操作の話くらいです。

![alt text](image-1.png)


