# 問題タイトル
[35. Search Insert Position](https://leetcode.com/problems/search-insert-position/description/)

# STEP 1 自力で解く。
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        # 2分探索したい
        # ok_i: target未満　ng_i: target 以上
        # 最初にngになるindexが知りたい
        ok_i = -1
        ng_i = len(nums)
        while ng_i - ok_i > 1: # ng_iとok_iが隣同士になったらストップ
            mid_i = (ng_i + ok_i) // 2
            if nums[mid_i] < target:
                ok_i = mid_i
            else:
                ng_i = mid_i
        return ng_i  
```
- 昔どこかで見つけた、めぐる式二分探索、というもので書いてみた。
    - https://x.com/meguru_comp/status/697008509376835584
    - https://qiita.com/drken/items/97e37dd6143e33a64c8c
- イメージ
    - 知りたいこと:okとngの境界
    - 定規の左端と右端にポストイットを貼る(絶対OKな位置と絶対NGな位置)
    - 真ん中を計算し、真ん中がOKだったらokポストイットをそこへ、NGだったらNGポストイットをそこへ.
    - ポストイット同士が隣に並んだら、右のNGポストイットが初めてNGになる位置なので、答え。
- 最初_iをつけておらず、何がindexなのかわかりにくくなって、なんどかWAになりながら書いた。
    - mid < targetとかいてしまっていた(midはindexなので、targetと比較しても意味ない)
    - ng = len(nums) + 1　と書いた(絶対NGなものはlen(nums) + 1個目ではあるが、そのindexはlen(nums))
- 配列の長さ0の時困るな
    - 長さ0で、`mid_i=(-1 + 0)//2 = -1`となってnums[-1]を見ようとして要素がなくてIndexError
        - //は負の方向に丸めるfloorなので、-0.5は-1になる


# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/rossy0213/leetcode/pull/26#discussion_r1698088949
    - >コードのパターンで覚えて書くのではなく、探索範囲が閉区間・半開区間・開区間のどれなのかを意識し、範囲を狭めるときに、範囲の端をどこまで狭めればよいかを意識して書くとよい
    - なるほど。STEP1は区間というイメージはあまり持っていなかったが、「区間の中から、ターゲット以上の最初の要素のインデックスを探す」と言う方式でやってみる。


閉区間
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        """
        閉区間 [low, high] を使用して、ターゲット以上の最初の要素のインデックスを返す
        """
        low = 0
        high = len(nums) - 1
        # ターゲット以上のものが見つからない場合、挿入位置は len(nums) となるため、これを初期値とする
        ans = len(nums) 
        # ギリギリ low <= high つまりlow = high の時、確認する範囲内に要素がまだ一つある。それ以上lowが大きくなったら終了
        while low <= high:
            mid = (low + high) // 2
            if target <= nums[mid] :
                # nums[mid] はターゲット以上の要素。midと言う以前よりマシな解が見つかったので更新
                ans = mid
                # より小さいインデックスにターゲット以上の要素がないか、左側 [low, mid - 1] を次回探索
                high = mid - 1
            else:
                # nums[mid] はターゲットより小さいので、答えは右側にある。
                # 右側 [mid + 1, high] を次回探索
                low = mid + 1
        # 最終的に ans にはターゲット以上の最初の要素のインデックス（または len(nums)）が残る
        return ans

```
- 「答えをこの中から探していきます。暫定解(ターゲット以上の要素)が見つかったら、それで更新します」という感じ。


半開区間
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        """
        半開区間 [low, high) を使用して、ターゲット以上の最初の要素のインデックスを返す
        """
        low = 0
        high = len(nums) # 配列のサイズ（含まない右端）
        # ターゲット以上のものが見つからない場合、挿入位置は len(nums) となるため、これを初期値とする
        ans = len(nums)
        # ギリギリ1つ差で low < high　の時、確認する範囲内に要素がまだ一つある。それ以上lowが大きくなったら終了
        while low < high:
            mid = (low + high) // 2
            if target <= nums[mid]:
                # nums[mid] はターゲット以上の要素。midと言う以前よりマシな解が見つかったので更新
                ans = mid
                # より小さいインデックスにターゲット以上の要素がないか、左側 [low, mid) を次回探索。
                high = mid
            else:
                # nums[mid] はターゲットより小さいので、答えは mid の右側にある。
                # 右側 [mid + 1, high) を次回探索
                low = mid + 1
        return ans 
```

開区間
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        """
        開区間 (low, high) を使用して、ターゲット以上の最初の要素のインデックスを返す
        """
        low = -1                 # 範囲外のインデックス（含まない左端）
        high = len(nums)          # 範囲外のインデックス（含まない右端）
        # ターゲット以上のものが見つからない場合、挿入位置は len(nums) となるため、これを初期値とする
        ans = len(nums) 
        while low < high - 1:    # low と high がギリギリ2個差なら、まだ確認すべき要素が一つある
            mid = (low + high) // 2
            if target <= nums[mid]:
                # nums[mid] はターゲット以上の要素。midと言う以前よりマシな解が見つかったので更新
                ans = mid
                # より小さいインデックスにターゲット以上の要素がないか、左側 (low, mid) を次回探索。
                high = mid
            else:
                # nums[mid] はターゲットより小さいので、答えは mid の右側にある。
                # 右側 (mid, high) を次回探索
                low = mid
        return ans
```

- https://github.com/hayashi-ay/leetcode/pull/40/files#diff-912c29ce0f491ca88b1474ced97db89f2d33a36a34cd0f516f5b523e400af176R6
    - >pythonではオーバーフローを考慮しなくて良いが、考慮する必要がある言語だと mid = left + (right - left) // 2のようにする。
    - left + rightは大きすぎて32ビット整数の最大値を超えてしまう、といったときでも、知りたいのは真ん中だから、真ん中までの距離出して足し算すればいける、と言う話。

- https://github.com/hayashi-ay/leetcode/pull/40/files#diff-912c29ce0f491ca88b1474ced97db89f2d33a36a34cd0f516f5b523e400af176R8
    - 半開区間。ansは使っていない。
    - なぜans使ってなくて、このロジックで答えになるのか
        - `return left`しているが、この時点で`left == right `なので、実質`return right`
        - 上記の私の半開区間のコードよくみると、ansを更新したときというのはmidの左側を見ようということでrightもansと同じ値にして`[left, ans)`をみるようにしている。つまり実質一緒。
        - ansが更新される時ってこういう時だよね！とわかっていれば書けるのかもしれないが、ちょっと難しい気もする。もしかしたら全然違うことを考えて書いているのかもしれない。どんなふうに考えているのだろう。


- https://docs.python.org/ja/3/library/bisect.html#bisect.bisect_left
    - bisect.bisect_left(a, x, lo=0, hi=len(a), *, key=None)
        - ソートずみのリストaにxを挿入したとして、挿入後のリストの中でのxのindexを返す。すでにxと同じものがが入ってたらその左の位置に挿入する。
        - Cpythonの実装
            - https://github.com/python/cpython/blob/cfbdce72083fca791947cbb18114115c90738d99/Lib/bisect.py#L74
                - 半開区間でやっている。上のhayashiさんと同じ。どんなふうに考えてるんだろう。

- https://github.com/Fuminiton/LeetCode/pull/41
    - `if nums[mid] == target: return mid`としている。こんなやり方もあるのか。
    - もしかしたら他にももっと色々あるのかもしれない。二分探索は書き方の自由度が高いな、、

# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        if not nums:
            return 0
        # ok: target未満, ng: target以上
        ok_i = -1
        ng_i = len(nums)
        while ng_i - ok_i > 1:
            mid_i = (ok_i + ng_i) // 2
            if nums[mid_i] < target:
                ok_i = mid_i
            else:
                ng_i = mid_i
        return ng_i
```


# STEP4 レビューFB反映