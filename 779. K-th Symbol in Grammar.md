# 問題タイトル
[779. K-th Symbol in Grammar](https://leetcode.com/problems/k-th-symbol-in-grammar/description/)



# STEP 1 自力で解く。
再帰的に段を上がって確認する。
全要素数に対して計算量はlogオーダー。全体の個数は2kなので、2^n。O(log2^n)= O(n)

```py
class Solution:
    def kthGrammar(self, n: int, k: int) -> int:

        def return_answer(n, k):
            if n == 1:
                return 0

            parent = return_answer(n-1, (k+1)//2)
            if parent == 0:
                if k % 2 == 1:
                    return 0
                else:
                    return 1
            if parent == 1:
                if k % 2 == 1:
                    return 1
                else:
                    return 0
        return return_answer(n, k)


```
- 「なぜかもう欲しい答えが出る関数があったと仮定して、それを使って部下に何を返してもらえれば、自分の答えが出せるか」と考える
- ループへの書き直しをしたい。
- return_answerは名前なんとかしたい、そもそも別途関数定義せずともkthGrammarを使い回せたのでは。
- parentを元に0や1を返している処理は、もっとスマートにまとめる方法がありそう。


# STEP2 読みやすくする＆他の人の解法を見る
スマートに書き直したバージョン
```py
class Solution:
    def kthGrammar(self, n: int, k: int) -> int:
        if n == 1:
            return 0
        parent = self.kthGrammar(n-1, (k+1)//2)
        return parent if k % 2 == 1 else 1 - parent
```
- 再帰の深さはnで、問題の制約から30以下なので、recursion limit(=1000)には引っかからない

ループに書き直したバージョン1(明示的にスタックを使う)
- 戻りがけで、それぞれの階数でのkを使って処理を行っているので、その情報をスタックに保存しておいて、戻りがけに使う。
```py
class Solution:
    def kthGrammar(self, n: int, k: int) -> int:
        stack = []
        while n > 1:
            stack.append(k)
            k = (k + 1) // 2
            n -= 1
        
        val = 0
        while stack:
            prev_k = stack.pop()
            if prev_k % 2 == 0:
                val = 1 - val

        return val
```

ループに書き直したバージョン2
- 実は、0/1が何回flipするか（正確にはその結果最終的に表か裏か）だけわかれば良いので、kを覚えておいて戻りがけにやらずとも、行きがけに回数を数えておけばOK
```py
class Solution:
    def kthGrammar(self, n: int, k: int) -> int:
        flip = 0
        while n > 1:
            if k % 2 == 0:
                flip = 1 - flip
            n -= 1
            k = (k + 1) // 2
            
        return flip
```

- https://discord.com/channels/1084280443945353267/1200089668901937312/1216054396161622078
- https://github.com/olsen-blue/Arai60/pull/47/files#r2003238004
- ```py
    class Solution:
        def kthGrammar(self, n: int, k: int) -> int:
            return bin(k - 1).count("1") % 2
    ```
- >0 -> 01, 1 -> 10 を二分木として見て観察すると、 左側に行く場合は親と同じで右側に行く場合は反転する
    0 -> 01 -> 0110 を二分木として見て、 0-indexの2番の葉に辿り着くまでに根から順に右、左と移動したので ビットの反転は1度だけ起きる。 根が0でビットの反転が1度(奇数回)起きると最終的なビットは1
    - https://github.com/hroc135/leetcode/pull/44/files#diff-51246bfe90de9312497a14a935a13980cce9e8a58b9614c7d17f48d8ac78129a

- https://github.com/hroc135/leetcode/pull/44#discussion_r2005165087
    - SWARアルゴリズム。1になってるビットの数を効率よく数える。
    - 左から全部見ていくのではなく、二桁ずつ、四桁ずつ、8桁ずつ、、、とまとめていきながら桁数を減らしていく
    - bit演算だけで(ループや条件判定なしで)うまいことまとめながら数えていけるので、ループのオーバーヘッド(カウンタ更新、比較、ジャンプ)と条件判定の分岐予測の失敗による無駄がない。

# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def kthGrammar(self, n: int, k: int) -> int:
        stack = []
        while n > 1:
            stack.append(k)
            n -= 1
            k = (k + 1) // 2
        val = 0
        while stack:
            prev_k = stack.pop()
            if prev_k % 2 == 0:
                val = 1 - val
        return val
```



# STEP4 レビューFB反映