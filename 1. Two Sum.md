# 問題タイトル
問題リンク
https://leetcode.com/problems/two-sum/description/

# STEP 1 自力で解く。
```py
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]: 
        num_to_index = dict() 
        # 一つずつ辞書に追加する
        for i, num in enumerate(nums): 
            # 追加するときに、パートナーがすでに辞書に入ってないか確認し、入ってたら、return
            partner = target - num
            if partner in num_to_index:
                return [num_to_index[partner], i]

            num_to_index[num] = i
```
- 所要時間　7:22
- つまづいたところ/コメント
    - 最初はseenのようなsetに入れようかと思ったが、そうするとindexがすぐには取れないので辞書に入れることにした。
    - enumerateをスペルミスした。
    - 自然言語に近い形でコード書けるようになるために、先に日本語でコメント書いてから、コードを書いてみた。
    

# STEP2 読みやすくする＆他の人の解法を見る

- https://github.com/hayashi-ay/leetcode/pull/14/files
    - 解き方は同じだが、見つからなかったとき、Exceptionを raiseしたり、[-1, -1]を返している
    - 組み込み例外の一覧
        - https://docs.python.org/ja/3/library/exceptions.html
    - exception Exception
        - システム終了以外の全ての組み込み例外はこのクラスから派生しています。全てのユーザ定義例外もこのクラスから派生させるべきです。
    - exception ValueError
        - 演算子や関数が、正しい型だが適切でない値を持つ引数を受け取ったときや、 IndexError のようなより詳細な例外では記述できない状況で送出されます。
    - 引数args
        - 例外コンストラクタに与えられた引数のタプルです。組み込み例外は普通、エラーメッセージを与える一つの文字列だけを引数として呼ばれますが、中には (OSError など) いくつかの引数を必要とし、このタプルの要素に特別な意味を込めるものもあります。


https://discord.com/channels/1084280443945353267/1183683738635346001/1187326805015810089
>はじめに、一番初めと一番最後に着目します。
    着目しているものを足します。
    目標よりも小さかったら、前の方の着目しているのを一つ後ろにずらします。
    目標よりも大きかったら、後ろの方の着目しているのを一つ前にずらします。
    繰り返したら見つかるはずです。

最初minとmaxを使う
    大きすぎた場合　→　maxを使ってtargetを作るのは不可能なので、除外して、次に大きいmax2を使ってみる
    小さすぎた場合　→ minを使ってtargetを作るのは不可能なので、除外して、次に大きいmin2を使ってみる

```py
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]: 
        num_and_index = []
        for i, num in enumerate(nums):
            num_and_index.append((num, i))
        num_and_index.sort()

        left = 0
        right = len(num_and_index) - 1
        while True:
            if left == right:
                raise Exception("No answer found")
            total = num_and_index[left][0] + num_and_index[right][0]
            if total == target:
                return [num_and_index[left][1], num_and_index[right][1]]
            if total > target:
                right -= 1
            else:
                left += 1
```

# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        num_to_index = dict() 
        # 辞書に一つずつ格納
        for i, num in enumerate(nums):
            # 格納するときに、partnerがすでに入っていたら答えをreturn
            partner = target - num
            if partner in num_to_index:
                return [num_to_index[partner], i]
            num_to_index[num] = i
        # 答えがなかったらException
        raise Exception("No answer found")

```
- 所要時間　
5:50, 2:52, 2:37

- 計算量
    - 時間: O(n)
        - for ループがO(n)回
            - 1回あたりはO(1)
    - 空間: O(n)
        - num_to_indexがO(n)


# STEP4 レビューFB反映