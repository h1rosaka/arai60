# 問題タイトル
問題リンク
https://leetcode.com/problems/maximum-depth-of-binary-tree/description/


# STEP 1 自力で解く。
```py
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        node_and_depth_que = deque([(root, 1)])
        while node_and_depth_que:
            node, depth = node_and_depth_que.popleft()
            if node.left:
                node_and_depth_que.append((node.left, depth + 1))
            if node.right:
                node_and_depth_que.append((node.right, depth + 1))
        return depth
```
- 所要時間　8分
- コメント
    - 紙でやるならどうやるかを先に考えられたのは良かった
    - rootがNoneだった場合のケアを忘れていて一度エラーになった。
    - Noneのチェックは`is　None`でも良かったか。

# STEP2 読みやすくする＆他の人の解法を見る

DFS
```py
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))
```
- 自分が最後の担当者だったとして、部下たちに最終的に渡して欲しいものは「左に進んだ時の最大長」と「右に進んだ時の最大長」で、そこに1足せば良い


https://github.com/hayashi-ay/leetcode/pull/22/files
- "Iterative"と呼んでいる解法で、BFSをしている
    - 1つのqueueを使うのではなく、depthごとに新しいリストを作って、そこに要素入れていく形で処理している。

- "IterativeのDFS"と呼んでいる解法
    - 右、左の順でstackに入れてpopするので、常に左がある限り深く進み続ける
        - popするたびに最大深さを更新して、stackが空になったら返す
    - 再帰じゃないDFSの書き方をちゃんと認識できていなかったので見られて良かった
    - 根　→ 左　→ 右　の順番でチェックすることをpre-orderと呼ぶとのこと

 
- https://discord.com/channels/1084280443945353267/1227073733844406343/1236235351140339742
- 整理すると、DFSは下の表の4パターンがある

|             | 下から数えていく                    |  上から数えていく      | 
| ----------- | ----------------------------     | ---------------------| 
| 再帰関数を使う | 1(上記step2)                     | 2 (上記discordリンク)  | 
| スタックを使う | 3 (同discord内いくつか下のコメント) | 4 (hayashi-ayさんのiterativeのDFS) | 

3.
https://discord.com/channels/1084280443945353267/1227073733844406343/1236695050902048899
```py
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        res_depth_ref = [None]
        # (node, ret_depth_ref, left_depth_ref, right_depth_ref)
        stack = [(root, res_depth_ref, [None], [None])] 
        while stack:
            node, ret_depth_ref, left_depth_ref, right_depth_ref = stack[-1]
            if not node:
                ret_depth_ref[0] = 0
                stack.pop()
                continue
            if left_depth_ref[0] is None:
                assert(right_depth_ref[0] is None)
                stack.append((node.left, left_depth_ref, [None], [None]))
                stack.append((node.right, right_depth_ref, [None], [None]))
                continue
            assert(left_depth_ref[0] is not None and right_depth_ref[0] is not None)
            ret_depth_ref[0] = max(left_depth_ref[0], right_depth_ref[0]) + 1
            stack.pop()
        return res_depth_ref[0]

```
- ret_depth_ref
    - このノードの深さ(を親ノードに渡すための参照)
    - 再帰で書いた時の返り値みたいなもの(returnのret)
    - 更新されるのは下記2つの場合
        - 1. 一番下のnodeまで行ったあと、さらにもう一段下に行くと `if not node:`に該当するので0が入る
        - 2. left_depth_ref / right_depth_ref が両方計算されてNoneじゃなくなった時、大きい方に+1した値で更新
    - 更新されると、そのノードはstackからpopされる。
    - 子要素の ret_depth_ref は、親の left_depth_ref / right_depth_ref と同じものを指してる
    - つまり、子要素側の処理で ret_depth_ref が計算されると、親の left_depth_ref / right_depth_ref も更新される(これで情報を引き継ぐ)

- 流れ
    - 一番下のnode(A)のその一つ先(None)に行くまで、stackに積み続ける
    - `if not node:`に該当するので、ret_depth_ref[0] = 0
        - つまり、Aのleft_depth_ref / right_depth_refも0として更新される
        - 同時に、末端の２つのNoneはpopされてstackから消える
    - stackの末端がAになり、それに注目する
    - left_depth_ref, right_depth_refが計算済みでNoneではないので、Aのret_depth_refが、1として更新される
    - 以下繰り返し

- 自分で書くと大変だなと思った。ありがとう再帰関数。




# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if root is None:
            return 0

        node_and_depth = deque([(root, 1)])
        while node_and_depth:
            node, depth = node_and_depth.popleft()
            if node.left is not None:
                node_and_depth.append((node.left, depth + 1))
            if node.right is not None:
                node_and_depth.append((node.right, depth + 1))

        return depth

```
6:09, 3:15, 1:55

計算量
- 時間: O(N)
- 空間: O(N)
    - 綺麗に2分木だと、最後の段にN/2個のnode


/Users/sakamotohiroaki/src/arai60/
