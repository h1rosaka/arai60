# 問題タイトル
139. Word Break
https://leetcode.com/problems/word-break/description/


# STEP 1 自力で解く。
下記を書いてTLEになった。
```py
# 注意　@cacheがないので、TLEになります。
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        character_to_word_list = defaultdict(list)
        for word in wordDict:
            character_to_word_list[word[0]].append(word)
    
        can_break = False
        def word_break_checker(string_):
            nonlocal can_break
            if len(string_) == 0:
                can_break = True
                return
            for word in character_to_word_list[string_[0]]:
                if len(word) <= len(string_):
                    if string_[:len(word)] == word:
                        word_break_checker(string_[len(word):])

        word_break_checker(s)
        
        return can_break
```

- こういう少し複雑なものは自分がどれくらい大変な計算をさせているのかパッとイメージがつかない、、
    - ざっくり考える
        - word_break_checkerを呼び出せる単語の数が平均してm個あるとする。
        - それらの単語の長さを平均lとして、len(stinrg_)/l = nとすると
        - m回の分岐がn回発生するので、m^n　くらい呼び出しがある。
        - 1回あたりリストのコピーでO(l)計算量がかかるので、全体ではO(l*m^n)

- (STEP2で他の人の回答みて知ったが、)@cacheをつければ問題なく通った。
    - word_break_checker()の引数には「string_のX文字目以降」という形でしか入らないので、引数の種類はN=len(string_)しかなく、 最大でもN回しか呼ばれないようなもの。
    - 1回あたりの時間計算量は、O(ml)なので、O(Nml)

- `if len(string_) == 0:` はnot string_でよかったな
- 「複数のwordで確かめるのを進めて、どれか一つだけでもOKだったらOK」という状況をどう管理しようか悩んだ
    - 「どれか一つでもTrueならTrue」の管理はanyを使うとよさそう
        ```py
        def word_break_checker(string_) -> bool:
            if not string_:
                return True
            return any(
                len(word) <= len(string_) and word_break_checker(string_[len(word):])
                for word in character_to_word_list[string_[0]]
            )
        ```

# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/61
    - TOP DOWN DP
    ```py
    class Solution:
        def wordBreak(self, s: str, wordDict: List[str]) -> bool:
            @cache
            def can_split(index):
                if index == len(s):
                    return True
                for word in wordDict:
                    if s[index:index + len(word)] != word:
                        continue
                    if can_split(index + len(word)):
                        return True
                return False

            return can_split(0)
    ```
    - マスにメモしていきながら解いていくもののことをDPというのかと勘違いしていたが、こういうのもDPというとのこと。
        - 「大きな問題を、重複する小さな部分問題に分割し、その部分問題の結果を保存（メモ）しながら解く手法」がDP。
        - "TOP DOWN"なのは、一番大きい問題(can_apllit(0))をはじめに問いかけて、順々に小さい問題の落としていくから。
    - 「X文字目以降を分解できますか」という問題を、ちょっとずつ小さくしながら解いている
    - `if can_split(index + len(word)):`とすることで、どれか一つでもOKだったらOKを実現している


- https://github.com/hayashi-ay/leetcode/pull/61#discussion_r1537544999
    - Bottom UP DP
        - 「X文字目までは分解できますか(分解の仕方は問いません)」を1マスずつ確認、なるべくTrueになるように全単語パターンチェックして、ダメだったらFalseのまま次のマスへ。
        - こちらの方が計算量少なくて済みそうなのがイメージしやすい。
        ```py
        class Solution:
            def wordBreak(self, s: str, wordDict: List[str]) -> bool:
                # segmented_dp[i]は、s[0:i]つまりi文字目までが分割可能か
                segmented_dp = [False] * (len(s) + 1)
                segmented_dp[0] = True
                for end_i in range(1, len(s) + 1):
                    for word in wordDict:
                        if end_i < len(word):
                            continue
                        begin_i = end_i - len(word)
                        if s.startswith(word, begin_i, end_i) and segmented_dp[begin_i]:
                            segmented_dp[end_i] = True
                            break
                return segmented_dp[-1]
        ```
        - 変数名など色々工夫してみたものの、startswithでみてる範囲の時はend_iを含まないのに、segmented_dp[end_i]は、end_iを含んでまさにそこで分割して終われるかを考えていて、混乱する


- https://github.com/hayashi-ay/leetcode/pull/61/files#diff-25f1226927fe56c505f4cbf2124534215d66f3c2ca0decf160a04dc095c93e83R170
    - 到達可能な位置をstateに入れていき、そこから移動可能な箇所を全てまた確認してstatesに入れていく。
    - なるべくゴールに近いところを探せるように、heap使ってindexが大きいのから探索している。wrap_heappushによって大きい順を実現

- https://github.com/SuperHotDogCat/coding-interview/pull/23#discussion_r1619222086
    - startswith
        - https://docs.python.org/3/library/stdtypes.html#str.startswith
        - s.startswith(prefix)で、sがprefixから始まるか確認。これ使えばスライスで別途コピーして作らなくて良いので、毎回その単語の長さ分の計算時間をついやさずに、途中の文字で異常に気づいたらそこでやめられる。
        ```py
        s = "hello"
        "hello".startswith("he")  # True
        "hello".startswith("lo")  # False
        s.startswith("l", 2)  # True s[2:]に対してチェック
        s.startswith("l", 2, 4)  # True　s[2:4]つまり"ll"に対して"l"から始まるかチェック
        ```

- https://discord.com/channels/1084280443945353267/1201211204547383386/1235165575592939521
    - ローリングハッシュ
        - 部分文字列のハッシュを「累積和」みたいに O(1) で求める手法
            - "abc"のハッシュがわかってる状態で、"abcd"のハッシュが欲しいとき、普通にやると
                - 1.(N文字の)文字列abcdをスライスで作るO(N)
                - 2.1を使ってhash計算 O(1)
            - ローリングハッシュならこの1をスキップして、"abc"のハッシュと新たに足す文字"d"から"abcd"のハッシュを計算できる
            - hash("abcd") = hash("abc") * B + val('d')   (mod M)
            - イメージ
            ```py
            hash("abc") = 1*B^2 + 2*B^1 + 3*B^0
            hash("abcd") = hash("abc")*B + 4
                         = (1*B^2 + 2*B + 3)*B + 4
                         = 1*B^3 + 2*B^2 + 3*B + 4
            ```
            - Mで割るのは、割らないと大きい数になってしまって扱いづらいから。
            - Mには大きい素数を使う
                - なぜ大きいもの？
                    - ハッシュ値は「0 〜 M-1」の範囲になるので、たくさん種類あった方が嬉しい
                - なぜ素数?
                    - M=12（素数ではない）で割った余りを考える。
                        - 2の倍数（2, 4, 6, ...）は、剰余演算後も必ず2の倍数
                            - 12についたら、その先また12+2,12+4, ....が繰り返し。次は24+2, ...
                        - 3の倍数（3, 6, 9, ...）は、剰余演算後も必ず3の倍数
                    - 12で割ると、「12の約数」の倍数のところで被りが出て厄介
                        - → なるべく約数がないやつにしよう→素数
 
    - ローリングハッシュによる効率化
        - 事前に全ての単語に関して、途中まで/完全版のハッシュを全て計算、prefix_hashesという辞書に保存
        - sを調べるとき、s[i:j] のハッシュを伸ばしながら計算(a→aa→aap→aapl→aaple)
        - 毎回prefix_hashes にあるか確認、なかったら「もう絶対どの単語にもならない」と分かる
            - startswithだと、単語ごとにループで「この単語ありますか」みたいな感じだが、prefix_hashesの方はsを伸ばしながら、この文字列(のハッシュ)はどの単語かのprefixに入ってますか、というイメージ。






# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        @cache
        def can_split_after(index):
            if index == len(s):
                return True
            for word in wordDict:
                if not s.startswith(word, index):
                    continue
                if can_split_after(index + len(word)):
                    return True
            return False
        return can_split_after(0)

```
3:20, 2:02, 2:39
計算量
- 時間: O(Nml)
    - キャッシュなしでの呼び出しが最大N回
    - 1回の呼び出しあたり、wordの数m　* wordの長さl だけかかる
- 空間: O(N)
    - キャッシュが最大O(N)



# STEP4 レビューFB反映


