# 問題タイトル
問題リンク
122. Best Time to Buy and Sell Stock II
https://leetcode.com/problems/best-time-to-buy-and-sell-stock-ii/description/


# STEP 1 自力で解く。
```py
class Solution:
    def maxProfit(self, prices: List[int]) -> int:  
    # 買いからしか入らないトレーダーが最高益を出す方法は？→全ての単調増加上げ相場に始点で参加し、終点で売り抜ける
        profit = 0
        has_stock = False
        # 最終日は、持ってるなら絶対売ってほしいので、最終日翌日は株価を0にしておく
        prices.append(0)
        for i in range(len(prices) - 1):
            if has_stock:
                # 翌日下がることを予知したら(上げ相場の終わりだと理解して)今日中に売る
                if prices[i] > prices[i+1]:
                    profit += prices[i] - bottom
                    has_stock = False
                # 下がらないのであれば、そのままホールド
            else:
                # 翌日上がることを予知したら(今日から上げ相場の始まりだと理解して)買う
                if prices[i] < prices[i+1]:
                    bottom = prices[i]
                    has_stock = True
        return profit
```


# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/56
    - 1st
        - max_profit_with_stockとmax_profit_without_stockをdpの表として二つ管理　なるほど。

    - 2nd
        - >前日より上がっていれば売れば良い。 たとえば、[1, 3, 7]とあったときに「1で買って7で売る」のと「1で買って3で売って、3で買って7で売る」のはおなじになる。
            - 言われれば確かにそう。
        - 「1日前に戻れるタイムマシン」があって、毎日どうするか決めるイメージ。今日が利益取れる日なら行動。そうじゃないなら何もしない。

```py
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        profit = 0
        for i in range(1, len(prices)):
            if prices[i] > prices[i - 1]:
                profit += prices[i] - prices[i - 1]
        return profit
```

# STEP3　　3回連続でエラーなしで解けるまで解く
- step2と同じ
```py
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        profit = 0
        for i in range(1, len(prices)):
            if prices[i] > prices[i - 1]:
                profit += prices[i] - prices[i - 1]
        return profit
```
計算量
- 時間：O(N)
- 空間：O(1)
# STEP4 レビューFB反映