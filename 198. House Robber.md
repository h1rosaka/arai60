# 問題タイトル
https://leetcode.com/problems/house-robber/description/


# STEP 1 自力で解く。
```py
class Solution:
    def rob(self, nums: List[int]) -> int:
        if len(nums) == 1:
            return nums[0]
        index_to_max_money = [0] * len(nums)
        index_to_max_money[0] = nums[0]
        index_to_max_money[1] = max(nums[0], nums[1])
        for i in range(2, len(nums)):
            index_to_max_money[i] = max(index_to_max_money[i - 1],
                                        index_to_max_money[i - 2] + nums[i])
        return index_to_max_money[-1]

```
- numsのが長さが1だった場合の考慮が漏れており、一度WAになった。


# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/48
    - 大体同じ。amountの方がmoenyより良いかも。
- https://github.com/Mike0121/LeetCode/pull/47/files#r1799964450
    - メモ化再帰
    - find_max_value(i)は、i番目以降の家で盗める最大金額
    - >inner function は定義するたびにオブジェクトとして作り直されている
    - >このコードはスレッドセーフティーという意味でどうなっているでしょうか。
        - マルチスレッドよくわかっていなかったので調べた↓
        - マルチスレッド
            - モチベーション: I/O待ちを効率化できる
                - webページダウンロード → スクレイピング　という処理だと、ダウンロード待ちが長い
                - 1スレッドで直列に複数ページやると、長時間化
                - 複数スレッドでやれば、並行で実施できる。
        - スレッドセーフ
            - 複数のスレッドが同じコードやデータに同時アクセスしても、データ破壊や予期しない動作が起こらないこと
        
        - GIL(Global Interpreter Lock)
            - https://docs.python.org/ja/3.10/glossary.html#term-global-interpreter-lock
            - > CPython インタプリタが利用している、一度に Python の バイトコード を実行するスレッドは一つだけであることを保証する仕組み
            - >これにより (dict などの重要な組み込み型を含む) オブジェクトモデルが同時アクセスに対して暗黙的に安全になるので、 CPython の実装がシンプルになります。
                - 同時に同じ辞書にアクセスしても、Cで書かれたpython自体のプログラムが壊れることはないですよ、という意味らしい
                - pythonユーザー目線で、適当にプログラム書いても競合しませんよ、という意味ではないらしい
            - オプションで解除も可能


        - GILがあっても困り事が発生するケースの例
            - 下記のコメントアウトをつけた時と外した時で結果が違う。
                - sleepしないと200。sleepすると100ちょい。
            ```py
                import threading
                import time

                counter = 0
                def inc():
                    global counter
                    for _ in range(100):
                        old = counter
                        # time.sleep(0.000001)  # わざと文脈切り替えを誘発
                        counter = old + 1

                threads = [threading.Thread(target=inc) for _ in range(2)]
                [t.start() for t in threads]
                [t.join() for t in threads]
                print(counter)
            ```
            - 仕組み
                - プリエンプションが起きるまでのタイムスライスが 10 ms GIL保持期間が5msだとする
                - sleepしない場合
                    - `old = counter`と`counter = old + 1`は100回やっても10msに届かず、タイムスライス内で終わり、終わってから次のスレッドで後から100回やるので、200になる
                - sleepする場合
                    - サマリ：Bがoldを確認→Aが+1してcounterに書き込む→Bが更新後のcounterを考慮せずAと同じold+1をcounterに書き込んでしまう。
                    - スレッドAで`old = counter`でold(例:0)の読み込み
                    - `sleep`でスレッドがブロッキング状態になり、GILが開放されつつCPUも別のスレッドへ。
                    - スレッドBで、`old = counter`でold(例:0)の読み込み
                    - `sleep`でスレッドがブロッキング状態になり、GILが開放されつつCPUも別のスレッドへ。
                    - スレッドAで`counter = old + 1`して、1に。再び`old = counter`でold(例:1)の読み込み.
                    - `sleep`でスレッドがブロッキング状態になり、GILが開放されつつCPUも別のスレッドへ。
                    - スレッドBで`counter = old + 1`して、1に。再び`old = counter`でold(例:1)の読み込み.
                        - →　1週終わったが、1にしかなっていない。

            - (前提)CPythonがGILを開放して他スレッドに譲るのは下記2パターン
                - バイトコードを一定数実行したあと(デフォルトでは5ms経ったら。長さ変更可能)
                    - ちなみにOSが別スレッドに行ってしまうまでの時間は「数ミリ秒から 100 ミリ秒程度」
                        - 工学基礎シリーズ オペレーティングシステム (p.60). Kindle 版. 
                    - バイトコードの1命令は50ns ~ 200ns/命令とのこと(chatGPT曰く)
                - I/O操作やsleepなどのブロッキング呼び出しで待つとき
           

        -  >再帰計算で複数の変数から一つのグローバル変数memoにアクセスを行っているので、スレッドセーフではない認識です。
            - これはおそらく間違いで、memoはrob()が呼ばれるたびに作られるので1つではないはず。
                - memoはnums1つに対して1つあるべきなので、おそらくマルチスレッドするなら異なるnumsに対して実行するだろうから、今の実装のままで良さそう。
                - 逆にSolutionのメンバ変数としてmemoを作って、solutionを1インスタンスしか作らずにマルチスレッドしようとすると、numsAとnumsBで共有のmemoを使ってしまってバグるはず
                - 同様に@cahce使った場合も、スレッドセーフに記録できるが、別々のnumsAとnumsB使ってマルチスレッドに記録したものを使っても意図通りのことはできないはず


- https://github.com/hroc135/leetcode/pull/33/files#r1899009212
    - メモなし再帰の計算量
        - 上で議論していた再帰は、「i番目以降の家で盗める最大金額」だが、今回は、「i番目"まで"の家で盗める最大金額」
            - 個人的にはこちらの方が自然な気がする  
        - 計算量
            - 2^nで抑えられる
                - 1回の呼び出しが2つ新たに呼び出すので、二分木っぽくなる。
                - 一番後ろのマスから、手前に戻るイメージ
                - もし一つ後ろのマスを2回読んで、二分木作るなら、木の深さはnになって2^nだが、実際には1マス飛ばしのルートがあるため、もっと少ない
            - フィボナッチになる
                - max_upto_index(n)にかかる時間をT(n)とおくと、T(n) = T(n-2) + T(n-1) + O(1)
                - この式はフィボナッチと同じ　F(n) = F(n-2) + F(n-1)
                - フォボナッチはnが大きい時、φ^n /√5 になる。φは1.6くらい
            - φ^n /√5 の仕組み
                - F(n) = F(n-1) + F(n-2)をもしr^nとして表せたとすると、
                - r^n = r^n−1 + r^n−2 となり、両辺をr^n−2で割ると
                - r^2 = r + 1 となり、これの解をα,βとする。
                - 以降、[ここの説明](https://manabitimes.jp/math/643#:~:text=%E8%A7%A3%E3%81%8F%E3%81%A0%E3%81%91%E3%81%A7%E3%81%99%E3%80%82-,%E8%A8%BC%E6%98%8E,%E3%81%8C%E5%BE%97%E3%82%89%E3%82%8C%E3%82%8B%E3%80%82,-%E8%A3%9C%E8%B6%B3%EF%BC%9A%E9%AB%98%E6%A0%A1%E6%95%B0%E5%AD%A6)により、n→♾️でφ^n /√5になる



再帰
```py
class Solution:
    def rob(self, nums: List[int]) -> int:
        @cache
        def max_upto_index(i:int) -> int:
            if i == 0:
                return nums[0]
            if i == 1:
                return max(nums[0], nums[1])
            return max(nums[i] + max_upto_index(i - 2), max_upto_index(i - 1))

        return max_upto_index(len(nums) - 1)
```


# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def rob(self, nums: List[int]) -> int:
        if len(nums) == 1:
            return nums[0]
        index_to_max_amount = [0] * len(nums)
        index_to_max_amount[0] = nums[0]
        index_to_max_amount[1] = max(nums[0], nums[1])
        for i in range(2, len(nums)):
            index_to_max_amount[i] = max(index_to_max_amount[i - 1], index_to_max_amount[i - 2] + nums[i])
        return index_to_max_amount[-1]
```
計算量：    
- 時間: O(N)
- 空間: O(N)
    - 今更気づいたが、全部のマスを覚えておく必要はなく、直前の2マスがあれば良いので、O(1)にできたはず

# STEP4 レビューFB反映
