# 問題タイトル
[50. Pow(x, n)](https://leetcode.com/problems/powx-n/description/)


# STEP 1 自力で解く。
```
nが負の時は、逆数にする必要があるので注意
n回xを掛け算すると、O(N)
n = 2^31 = 2^10 * 2^10 * 2^10 * 2 
         ≒ 10^3 * 10^3  * 10^3 * 2
         = 10^9 * 2
1秒に10^6ステップ処理だとすると、2000秒以上かかる。30分強。
もっと小さなオーダーで処理する必要がある。
結果をどんどん2乗していけば、logのオーダーで解けそう。
logのオーダーなら、logn=31 <<10^6なので、十分短時間で終わる
```
```py
class Solution:
    def myPow(self, x: float, n: int) -> float:
        result = 1
        n_is_negative = False
        if n == 0:
            return result
        if n < 0:
            n = -n
            n_is_negative = True
        
        result *= x
        power_count = 1
        power_count_and_result = [(power_count, result)]

        while power_count < n:
            if power_count < n - power_count:
                power_count += power_count
                result *= result
                power_count_and_result.append((power_count, result))

            # 2乗するとnをオーバーする場合は、今まで計算したものの中からオーバーしない範囲で一番大きいものを掛ける  
            else:
                for i in range(len(power_count_and_result) - 1, -1, -1):
                    if power_count_and_result[i][0] > n - power_count:
                        continue
                    power_count += power_count_and_result[i][0]
                    result *= power_count_and_result[i][1]
                    power_count_and_result.append((power_count, result))

        if n_is_negative:
            return 1 / result
      
        return result

```



# STEP2 読みやすくする＆他の人の解法を見る

- https://github.com/hayashi-ay/leetcode/pull/41/files#r1515412661
    - 2^256 → 4^128 → 16^64 ... と、xを大きくしていく。スマート。
    - 確かに算数のテストで2^256を計算しろと出たら自分もこう考えたかも?
    - プログラミングの問題と考えず、まず自分が紙とペンでやるならどうするのかを考える。

- https://github.com/hayashi-ay/leetcode/pull/41/files#diff-fa34d808ee15ac1eccbae588a1275635b53a94c2569fec3c53074d78e307a4a4R76
    - 再帰で書いている。綺麗。


- https://github.com/hayashi-ay/leetcode/pull/41/files#r1514489206
    - 下記の通りで良いとのこと。スマート。
    ```py
    if n < 0:
    n = -n
    x = 1.0 / x

    ```

- https://github.com/hayashi-ay/leetcode/pull/41/files#r1515415125
    - 3^14 = 3^(8 + 4 + 2)     = 3^8 * 3^4 * 3^2といった形での分解
    - 3^15 = 3^(8 + 4 + 2 + 1) = 3^8 * 3^4 * 3^2 * 3^1
    ```py
    def myPow(self, x: float, n: int) -> float:
        if n == 0:
            return 1
        if n < 0:
            x = 1 / x
            n = -n
        result = 1
        power_base = x

        # 以下、n(exp_bit)が2進数(例:1110)で表されていると捉えて扱う
        exp_bit = n
        while exp_bit > 0:
            # 奇数、つまり今見てるexp_bitの一番下のビットが1なら、そこに対応するpower_baseは使うので、resultに掛ける。(ループごとにexp_bitは一番下の桁を取り除いて、毎度違うものになる)
            if exp_bit % 2:　
                result *= power_base
            # 次の桁のpoer_baseは、先ほど使っていたpower_baseの2乗
            power_base *= power_base
            exp_bit >>= 1 # ひとつ右にシフト(一番下の桁が取り除かれる)

        return result
    ```
- ">>"
    - https://docs.python.org/3/library/stdtypes.html#bitwise-operations-on-integer-types
    - xをnビット右にシフト
    - 負のシフトカウントは不正であり、例外がValueError発生します。
    - A right shift by n bits is equivalent to floor division by pow(2, n).
    - 例
        - 6(0110)を1ビット右にシフトすると、(0011)なので、3。これは2(=2^1)でfloor division(切り捨て)と同じ。
        - 15(1111)を2ビット右シフトすると、(0011)なので、3。これは4(=2^2)でfloor divisionと同じ。


- 逆方向に処理していく手法
    - 1000 → 0100 → 0010 → 0001 → 0000
    - 3^14 = 3^(8 + 4 + 2 + 0) = 3^8 * 3^4 * 3^2 * (3^1 * 0)

        ```
        最終ゴール: 3を14回かける
        方針:   下記4つに分解して、それぞれ必要であればかける(かけた回数の合計が14になるように採用する)
                3を8回かけたもの(採用/不採用)→1
                3を4回かけたもの(採用/不採用)→1
                3を2回かけたもの(採用/不採用)→1
                3を1回かけたもの(採用/不採用)→0
        14は、8 + 4 + 2 + 0なので、上の通り、1,1,1,0と採用すると14になる。
        それぞれの採用状況を元に全てかければ、欲しい結果が得られる(3^8 * 3^4 * 3^2 * (3^1 * 0))
        1110を左から読んでいく。
        ```
        ```
        3^14 = 3^(8 + 4 + 2 + 0) つまり、1110
        bit は最初、1000(一番左の桁を見ますよ、というイメージ。今見てる数は1)
        1ということは、3を1回かけたものを採用します。(他はしません)ということ。
        結果を保存してるresを2乗する。(1^2=1)(ここではあまり意味がない)
        resにbaseである3を掛ける(1*3=3)(3を1回かけたものを採用、を実現)
        ```
        ```
        bitをひとつ右にずらす→0100に。(左から二桁目に注目。今時点までで見てる数は11)
        結果を保存してるresを2乗する。(3^2=9)
            ひとつ前のステップでは、1110の一番左だけ見て、「3を1回かけたものを採用」ということを表す桁だと思っていたが、
            左から2桁目までみることにしたので、さっき見た一番左の1は実はもう一桁significantな、「3を2回かけたものを採用」ということを表す桁だったことが発覚。
            → 先ほどまでの結果を2乗することで帳尻をあわせる
        今見ている桁数のbitと一致する桁数がnでも1あり、この桁は採用ということなので、
        resにbaseである3を掛ける(1*3=3)(3を1回かけたものを採用、を実現)
        ```
        ```
        bitをひとつ右にずらす→0010に。(次の桁までを見る。今見てる数は111)
        結果を保存してるresを2乗する。
            先ほど見ていた11は、それぞれ「3を2回かけたものを採用」と「3を1回かけたものを採用」という意味だと解釈していたが、実際にはそれぞれもう一桁ずつsignificantな桁であったことが発覚
            → 先ほどまでの結果を2乗することで帳尻をあわせる
        今見ている桁数のbitと一致する桁数がnでも1あり、この桁は採用ということなので、
        resにbaseである3を掛ける(1*3=3)(3を1回かけたものを採用、を実現)
        ```
        ```
        bitをひとつ右にずらす→0001に。(次の桁までを見る。今見てる数は1110)
        結果を保存してるresを2乗する。
            先ほど見ていた111は、実際にはそれぞれもう一桁ずつsignificantな桁であったことが発覚
            → 先ほどまでの結果を2乗することで帳尻をあわせる
        今見ている桁数のbitと一致する桁数がnでは0であり、この桁は不採用ということなので、特に何もしない
        ```
        ```
        bitをひとつ右にずらす→0000に
        `while bit > 0:`から外れて、ループ終了
        ```



- >数が多いときはk-ary sliding windowでwindowサイズずつ計算していく。
    - https://github.com/hayashi-ay/leetcode/pull/41/files#diff-fa34d808ee15ac1eccbae588a1275635b53a94c2569fec3c53074d78e307a4a4R52
    - 速くするためのテクニック。
        - 二分累乗(上で書いた方法)
            - 指数 n を 2進数で見る。 
            - n = 11010101
            - 左から読んで
                - 毎回平方する
                - ビットが1の時「xを掛ける」
        - 巨大整数だと、掛け算のコストが高い→掛け算の回数を減らしたい
            - TODO: なぜ掛け算のコストだけ高いのか。2乗は高くないの？
        - 二分累乗での計算イメージ
            ```
            1 → x
            1 → x
            0 → 何もしない
            1 → x
            0 → 何もしない
            1 → x
            ...
            ```
        - sliding windowでの計算イメージ
            - あらかじめ、x¹, x³, x⁵, x⁷を作っておき、下記計算。
            ```
            11  → x³ を1回で掛ける
            0   → 平方だけ
            101 → x⁵ を1回で掛ける
            01  → x¹ を掛ける
            ```
            - 例えば、最初の2桁は、二分累乗だと、xかけて、それに2乗してxかけてx^2になって、その後次の桁のxを掛けるので、結局x^3になる。
            →だったら最初からx^3を掛けよう。
            - ウィンドウのルール
                - 左のビットから見ていく
                - 次のビットが 0 の間は window を作らない（平方だけ）
                - 1 を見つけたら window 開始
                - 最大 k ビットまで、かつ「最後が 1 になるように」window を伸ばす(上の例はk=3)
                - 作ったウィンドウを1回の掛け算で処理
            - 最後が1になるようにwindow作るので、あらかじめ用意する累乗結果は奇数だけでOK。
            - なぜslidingと呼ばれるのか
                - 窓の大きさが固定(fixed)ではなく1が出たら最大kビットまで読む

    - なぜ掛け算のコストは高く、２乗のコストは比較的安いのか
        - 掛け算をするときは筆算と同じなので、n桁同士で、O(n^2)
        - 2条の時は、同じ数字同士の掛け算が必ずもう一度発生する
        - 100マス計算みたいなイメージをすると、対角線を挟んで上と下は同じ計算をしているので、片方(半分)だけ計算すれば良い。

            



# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def myPow(self, x: float, n: int) -> float:
        if n == 0:
            return 1
        if n < 0:
            n = -n
            x = 1 / x

        exp = n
        power_base = x
        result = 1
        while exp > 0:
            if exp % 2:
                result *= power_base
                exp -= 1
            power_base **= 2
            exp //= 2

        return result

```






# STEP4 レビューFB反映