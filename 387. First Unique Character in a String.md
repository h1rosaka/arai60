# 問題タイトル
[387. First Unique Character in a String](https://leetcode.com/problems/first-unique-character-in-a-string/description/#:~:text=387.%20First%20Unique%20Character%20in%20a%20String)


# STEP 1 自力で解く。
```py
class Solution:
    def firstUniqChar(self, s: str) -> int:
        for i in range(len(s)):
            c = s[i]
            rest = s[:i] + s[i+1:]
            if c not in rest:
                return i
        return -1

```
- 所要時間　3:18
- コメント
    - あまり効率は良くないO(n^2)

# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/28/files
    - コード書く前にTLEにならないか検討している
        - 私のstep1でTLEにならなかったのは偶然？
        - 所要時間の検討
            - O(n^2)で、nは10^5だから、計算ステップ数は大体10^10
            - pythonは1秒間に百万ステップ(10^6)処理できるので、
            - 10^4秒 = 10,000秒　3時間？！
                - 上記解法でもacceptされてしまったので、leetcodeは必ずしも最大のn数でジャッジしているわけではないのかもしれない。
    - 2nd: 2週することで二重ループしないで解いている
        - O(n^2)の解法書いた後、効率上げるためにO(n)の解法思いつこうとして、1週でなんとかしようと考えて思いつかなかったが、確かに2週の解法を考えるのでも良かったのだという気づき。
    - 4th: 1週でやってる。char_to_first_appear_indexで、first_appear_indexかダミーとしてのinfを格納
        - 辞書のdel
            - >del で key: value のペアを削除することもできます
            - del tel['sape']
            - https://docs.python.org/ja/3/tutorial/datastructures.html#dictionaries
        - リストのdel
            - del a[0] で、リストaのindex=0の要素を削除(その他は前に詰める)
            - del a[2:4] とかの範囲指定も可
            - 存在しないindexを消そうとすると、IndexError になる
            - https://docs.python.org/ja/3/tutorial/datastructures.html#the-del-statement
        - return next(iter(char_to_first_appear_index.values()))
            - next(iterator)
                - >iterator の __next__() メソッドを呼び出すことにより、次の要素を取得します。イテレータが使い尽くされた場合、 default が与えられていればその値が返されます。そうでない場合は StopIteration が送出されます。
                - https://docs.python.org/ja/3.13/library/functions.html#next
            - iter() は「イテラブル（繰り返し可能なもの）」から「イテレータ（実際に1つずつ取り出せるもの）」を作る関数
            - ジェネレータは式だが、イテレータはクラスやオブジェクト
            - 普段イテレータを使わないので勉強になる

2週するバージョン
```py
class Solution:
    def firstUniqChar(self, s: str) -> int:
        char_to_count = defaultdict(int)
        for c in s:
            char_to_count[c] += 1
        for i, c in enumerate(s):
            if char_to_count[c] == 1:
                return i
        return -1

```


# STEP3　　3回連続でエラーなしで解けるまで解く

1週で済ませるバージョン
```py
class Solution:
    def firstUniqChar(self, s: str) -> int:
        char_to_first_index = dict()
        for i, c in enumerate(s):
            if c in char_to_first_index:
                char_to_first_index[c] = math.inf
                continue
            char_to_first_index[c] = i
        min_index = min(list(char_to_first_index.values()))
        if min_index == math.inf:
            return -1
        return min_index

```
3:29, 3:30, 2:30

# STEP4 レビューFB反映