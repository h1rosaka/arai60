# 問題タイトル
213. House Robber II
https://leetcode.com/problems/house-robber-ii/description/


# STEP 1 自力で解く。
```py
class Solution:
    def rob(self, nums: List[int]) -> int:
        if len(nums) == 1:
            return nums[0]

        # 1軒目を盗むパターン
        max_amount = [0] * len(nums)
        max_amount[0] = nums[0]
        max_amount[1] = nums[0]
        ## 最後の家は盗めないので、その手前まで
        for i in range(2, len(nums) - 1):
            max_amount[i] = max(max_amount[i - 2] + nums[i], max_amount[i - 1])    
        max_first_house_rob_pattern = max_amount[-2]

        # 1軒目を盗まないパターン
        max_amount = [0] * len(nums)
        max_amount[0] = 0
        max_amount[1] = nums[1]
        for i in range(2, len(nums)):
            max_amount[i] = max(max_amount[i - 2] + nums[i], max_amount[i - 1])    
        max_first_house_not_rob_pattern = max_amount[-1]

        return max(max_first_house_rob_pattern, max_first_house_not_rob_pattern)
```
- `if len(nums) == 1:`を忘れていて一度WAに。indexがiでアクセスするときは範囲外気をつけるようになったが、今回のように1でアクセスしてるときは気をつけるの難しい。コード書いたら出す前に変なパターンでも通るか考えないといけない。



# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/50/files#diff-ce0cd96bb02b9d0f849c47d3ecac921373d9a9834854c70c62942f51d909f929R35
    - >最初の家を除くlist、最後の家を除くlistについて考えれば直線に並んでいるものとして考えられる。
        - なるほど確かに
- https://github.com/hayashi-ay/leetcode/pull/50/files#diff-ce0cd96bb02b9d0f849c47d3ecac921373d9a9834854c70c62942f51d909f929R6
    - 2回for loop回すのではなく、2つ別のmax_amountを用意して、1回のループで回している

最初と最後を除く形の解法
```py
class Solution:
    def rob(self, nums: List[int]) -> int:
        if len(nums) == 1:
            return nums[0]
    
        def max_amount(nums: List[int]) -> int:
            if len(nums) == 1:
                return nums[0]
            index_to_max_amount = [0] * len(nums)
            index_to_max_amount[0] = nums[0]
            index_to_max_amount[1] = max(nums[0], nums[1])
            for i in range(2, len(nums)):
                index_to_max_amount[i] = max(index_to_max_amount[i - 2] + nums[i],
                                             index_to_max_amount[i - 1])
            return index_to_max_amount[-1]

        return max(max_amount(nums[1:]), max_amount(nums[:-1]))
```
- 1要素削って渡すので、numsの長さが1しかなかった場合を関数の外でケアする必要がある

# STEP3　　3回連続でエラーなしで解けるまで解く
STEP1を空間O(1)でやるパターン
- max_amountを使って更新していたときは、2個前のマスと1個前のマスにアクセスできないと更新ができなかったので、それらを別途特別に設定して2からループ始めていたが、今はそうではないので、別途用意せずとも共通のパターンで書ける
```py
class Solution:
    def rob(self, nums: List[int]) -> int:
        if len(nums) == 1:
            return nums[0]
        # 最初の家に入るパターン
        max_two_before = 0
        max_one_before = 0
        for i in range(len(nums) - 1):
            max_first_house_rob = max(max_two_before + nums[i], max_one_before)
            max_two_before, max_one_before = max_one_before, max_first_house_rob
        # 最初の家に入らないパターン
        max_two_before = 0
        max_one_before = 0
        for i in range(1, len(nums)):
            max_first_house_not_rob = max(max_two_before + nums[i], max_one_before)
            max_two_before, max_one_before = max_one_before, max_first_house_not_rob

        return max(max_first_house_rob, max_first_house_not_rob)

```
計算量
- 時間:O(N)
- 空間:O(1)
# STEP4 レビューFB反映