# 問題タイトル
問題リンク
https://leetcode.com/problems/string-to-integer-atoi/description/



# STEP 1 自力で解く。

```py
class Solution:
    def myAtoi(self, s: str) -> int: 
        index = 0
        nums = []
        # whitespace
        while index < len(s) and s[index] == " ": # 最初index < len(s)忘れて外に出てしまった
            index += 1
        # signedness
        if index < len(s) and (s[index] == "+" or s[index] == "-"):
            if s[index] == "+":
                sign = 1
            elif s[index] == "-":
                sign = -1
            index += 1
        else:
            sign = 1
        
        # conversion
        while index < len(s) and s[index] == "0":
            index += 1
        while index < len(s) and s[index] in "0123456789": # 0入れるの忘れた
            nums.append(s[index])
            index += 1
        if not nums:
            nums.append("0")
        # rounding
        print(nums)
        result = sign * int("".join(nums))
        if result < - 2**31:
            return - 2**31
        if result > 2**31 - 1:
            return 2**31 - 1
        return result
```

何度かミスした。
- index < len(s)忘れてout of range
- in "0123456789"に0入れ忘れて、後半に出てくる0をとり逃した
- signednessのロジック間違えて+-みたいな連続でくるものに対応できてなかった

改善できそうなこと
- whitespace
    - strip()みたいなのがあって使えるかも？(stripは前後から取ってしまうが)
    - https://docs.python.org/ja/3/library/stdtypes.html#str.strip
        - str.strip(chars=None, /)
        - charsに何も指定しないとwhitespaceが取れるが、何かしているすると、その文字が取れるとのこと。
        - >The chars argument is not a prefix or suffix; rather, all combinations of its values are stripped.
        - ```py
            ...
            while c in chars:
                continue
            ...
          ```
          - 特定の文字列を取り除くと言うより、上記のような感じ見たい。
        - 後ろからだけ取り除くrstrip()もあるとのこと。前からだけはないのか？
            - あった。lstrip()
                - https://docs.python.org/ja/3/library/stdtypes.html#str.lstrip
                

- `s[index] in "0123456789"`のところ
    - ascii_lowercase的なものが何かありそう
        - あった。string.digits
            - https://docs.python.org/3/library/string.html#string.digits
            - string.whitespaceなんてのもある。
                - https://docs.python.org/3/library/string.html#string.whitespace




# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/69/changes#diff-f2b395d63173ac2f2d3c547e8f9e8e07c9acfbeb6b5da935bb28d83d8bcc7a04R19
    - `INT_MAX = (1 << 31) - 1`みたいに一回変数に置いている
        - 説明的でわかりやすい。(`- 2**31`と　`2**31 - 1`の空目対策にも。)
        - `<<`は、2進数で書いたときの桁を左に進める、だから、1だったのが31桁目の1になって、結局2**31と同じになる。

    - `str.isdigit()`を使っている
        - https://docs.python.org/ja/3/library/stdtypes.html#str.isdigit
        - >文字列中の全ての文字が数字で、かつ 1 文字以上あるなら True を、そうでなければ False を返します。ここでの数字とは、十進数字に加えて、互換上付き数字のような特殊操作を必要とする数字を含みます。また 10 を基数とした表現ができないカローシュティー数字のような体系の文字も含みます。
        - ```py
            "²".isdigit() # True
            "①".isdigit()   # True
            "𐩂".isdigit() # True 古代インドで使われていたカローシュティー数字はdigit扱い
            "五".isdigit() # False 漢数字はダメ
            "Ⅳ".isdigit()      # False
            "Ⅳ".isnumeric()    # True（ローマ数字）
            ```
        - これ危ないので使わないほうがいい気がする、、
        - なんで使ってる人多そうな漢数字の"五"はダメで、古代インド文字なんて誰も使ってなさそうな方がちゃんと認識されるのかと思ってLLMに聞いてみたら、digit判定されるには桁の位置で表すシステムに則っているものだけTrue判定されるらしい。55は五十五になって途中に謎の"十"が入るからダメだ、とのこと。

    - `digit = ord(s[index]) - ord('0')`
        -　```py
            digit = ord(s[index]) - ord('0')
            number = number * 10 + sign * digit
        ``` 
        これで一桁ずつ数字を作り出している。int()使うのはこの問題的にはちょっとズルだったのかも？


- https://discord.com/channels/1084280443945353267/1201211204547383386/1232407641284935770
    - 「2147483648（正の数）を作る→それに -1 を掛けて -2147483648」 にする、とやると、2147483648の時点でオーバーフローする。
    - 

- https://github.com/shining-ai/leetcode/pull/59/changes#diff-c4e84ab78737111f2bbc13e26d30d141fee9ed4bba4448d84e242e20d5184da0R12
    - sign判定のロジックがシンプル。

- https://github.com/Yoshiki-Iwasa/Arai60/pull/64/changes/BASE..857ed449085ebae4c21cd475d45b541d6d43ccac#r1747497750
    - Q. なぜ、minの方がmaxより一つ多く数字取るのか
        - A. 「複数考えうる数字の割り当て方式の中で一番便利だったもの」が偶然「minの方がmaxより一つ多く数字取る形」になっていたから。

    - 概要
        - 3bitの例で考える
        - 3bitで普通表せる数字は0~7だが、3bitを使って負の数も表したいので、方法を考える。
        - 結局表せる数字は8種類なので、下記を考えることになる
            - 表現できる数字の範囲をどこからどこまでにするか
            - 8個あるbit列のうち、どれにどの数を割り当てるか
        - 一旦、割り当てを先に考える
            - 元の数とmod8で同じになる数を割り当てると、足し算が壊れないので都合良い（詳細後述）
                - ただし、足し算の結果が、表せる数字の範囲の中でないと、「mod8で見れば答えあってると言えるが、答えそのものではない」となる。
            - 円環状の双六のイメージ、あるいは時計のイメージ。
        - 次に、数字の範囲を考える
            - 上記のmod8方式の割り当てには複数のやり方が考えられる
            - その中の一つは、最上位bitが1かどうかで正負判定ができて便利なので、これを採用する。
        
        - 採用された方式はminの方がmaxより数字が一つ多くなっている。
            - この割り当て方は、実は、xの「2の補数」と呼ばれるものを-xとして扱っていることと一致する。

            - 2の補数
                - 前提: 補数(complement)
                    - 元の数に足すと桁上がりする数のうち、一番小さいもの

                - 2の補数
                    - 2進数における、元の数に足すと桁上がりする数のうち、一番小さいもの
                    - 0101(5)の2の補数は1011
                    - 2の補数の作り方: 0←→1 をひっくり返した後に1を足す
                        - 1011 を 0←→1 をひっくり返した後に1を足す
                        - 0←→1 をひっくり返しただけのもの(1010)を足すと、桁数変わらず全部1に(1111)
                        - そこにさらに1足すことで、1桁繰り上がって、一番左以外は0に。(10000)
                            ```
                                  0101   (5)
                                + 1011   (-5)
                                ------
                                1 0000
                            ```

                 - コンピューターは、負の整数を表現する方法として「2の補数」と呼ばれるものを採用している
                    - 1011は普通は11だが、これが2の補数として表現されているものだと捉えると、-5になる。
                    - https://www.rit.edu/academicsuccesscenter/sites/rit.edu.academicsuccesscenter/files/documents/math-handouts/DM3_TwosComplement_BP_9_22_14.pdf

    - 詳細
        - 「元の数とmod8で同じになる数を割り当てると、足し算が壊れないので都合良い（詳細後述）」について
            - 下記のように割り当てると、答えが-4~3(割り当てた数字の範囲)に収まる足し算なら、(負の数を導入したのに)今まで通りの普通の足し算で答えが求まる。
                ```
                000    0 → 0
                001    1 → 1
                010    2 → 2
                011    3 → 3
                100    4 → -4
                101    5 → -3
                110    6 → -2
                111    7 → -1
                ```
            - なぜ上手くいくか：
                - 円環上の双六のイメージ
                    - 8マスの円環双六上で、5マス進んだ時にいるマスは、3マス戻った時にいるマスと同じなので、こうなる。
                    - 5≡-3(mod8)、ということ。
                    - 時計でイメージしても良い。
                        - https://math.stackexchange.com/questions/1920772/why-twos-complement-works
            - ただし、答えが-4~3の範囲外だと、この限りではない    
                - -3 + (-4) → 001となり、下記割り当てを見ると1だが、答えは-7
                - しかし、1≡-7(mod8)なので、mod8の世界で見ると、あっているといえばあっている。

            - つまり、元の割り当ての数字とmod8で同じであれば、入れ替えが可能ということなので、下記のようなものも含め、他にも方法はいくつか考えられる
            - しかし、下記3つの要求を考えると、上で提示したものが一番便利で良いことがわかる。
                - 表せる範囲は連続した範囲が良い
                - そもそもやりたいことは正と負の数字(および0)を全て扱うこと(0を挟む範囲が良い)
                - 割り当てられた数字の符号の判定がもし簡単にできたら嬉しい(一番左のbitを見るだけ、など)

                ```
                000    0 → -80　→連続してない。
                001    1 → 1
                010    2 → 2
                011    3 → 3
                100    4 → 4
                101    5 → 5
                110    6 → 6
                111    7 → 7
                ```
                ```
                000    0 → 0
                001    1 → 1
                010    2 → 2
                011    3 → 3
                100    4 → 4
                101    5 → 5　→ 最初に提示した割り当てなら、MSB(一番左のbit)が1のものは全て符号が負であり簡単に判別できたが、この割り当てだとそうならない。
                110    6 → -2 
                111    7 → -1
                ```

    - tips: 2の補数は「一番左の桁だけマイナスの重み」と捉えて読むと、なんとそこに割り当てた数が算出できる
        - ![alt text](image.png)
        - 1011は、`-8 + 2 + 1`と読むと、-5が算出できる。
            - 1011の一番左はunsignedでは8
            - 2の補数では、そこを-8と読んでいるのと同じになる

        - Q.なぜ、このように算出できるのか
            - modの世界で数字を扱っているので、下記のような変換ができる↓
            - b1-b2-b3という3bitのbit列で表される数字をxとおく
            - x = b1 * 2^2 + b2 * 2 + b3
            - 今、3bitの世界なので、0~7の数字しか表せず、つまりはmod8。
            - mod8の世界だと、 x = x - 2^3 なので、これに先のxを代入する
            - x = b1 * 2^2 + b2 * 2 + b3 - 2^3
            -   = b1 * 2^2 + b2 * 2 + b3 - 2 * 2^2
            -   = (b1 - 2) * 2^2 + b2 * 2 + b3
            - ここで、今、最上位bitであるb1が1なら
            -   = 　-1 * 2^2 　 　+ b2 * 2 + b3
                - →最上位bitだけマイナスと読んだ時の式になる。     
            - 逆にb1が0なら。。?
                =   -2^3         + b2 * 2 + b3
                - ここで、mod8の世界だと、-2^3 = 0なので
                =     0          + b2 * 2 + b3
                - 最上位bitを0とみなし、残りはそのまま読んだ時の式になる



- JAVAやC++には、オーバーフローを検知する足し算や掛け算の関数がある
    - Pythonは自動でやってくれる(逆にいうと毎回この判定をしているので遅い)
    - https://discord.com/channels/1084280443945353267/1316770883729100810/1329745032202424330
    - https://discord.com/channels/1084280443945353267/1301587996298182696/1309473486879723542

- https://github.com/katsukii/leetcode/pull/9/changes#diff-4ac2258a9828437ca7cab8fbd9970eecd069418f46ad2e8464424478e58a4ae4R202-R205
    - オーバーフロー防止の事前チェック
        - 「次の桁の数字を末尾に付け足したときにMAX_INTを超えるなら」をMAX_INTより大きい数字が条件式の中に発生しないように表現している。
        - ギリギリMAX_INTならば、そのまま次のステップに行ってMAX_INTを作って返す。
        - MAX_INTより大きな数字になってしまうのならば、signに従ってMAX_INTかMIN_INTを返す。

- pythonはオーバーフローしないから、気をつけなくて良いのだが、あたかもオーバーフローするかのように気をつけて書いてみた
    ```py
    class Solution:
        def myAtoi(self, s: str) -> int: 
            INT_MAX = 2**30 - 1 + 2**30
            INT_MIN = -2**31
            index = 0
            # whitespace
            while index < len(s) and s[index] == " ":
                index += 1

            # signedness
            if index < len(s) and s[index] in "+-":
                if s[index] == "+":
                    sign = 1
                elif s[index] == "-":
                    sign = -1
                index += 1
            else:
                sign = 1
            
            # conversion
            number = 0
            while index < len(s) and s[index] == "0":
                index += 1
            while index < len(s) and s[index] in string.digits:
                digit = ord(s[index]) - ord("0")
                # overflow check, rounding
                if number > (INT_MAX - digit) / 10:
                    return INT_MAX if sign == 1 else INT_MIN

                number = number * 10 + digit
                index += 1
            
            return sign * number
    ```

# STEP3　　3回連続でエラーなしで解けるまで解く
- step2と同じ


# STEP4 レビューFB反映