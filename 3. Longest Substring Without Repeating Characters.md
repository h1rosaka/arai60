# 問題タイトル
[3. Longest Substring Without Repeating Characters](https://leetcode.com/problems/longest-substring-without-repeating-characters/description/)




# STEP 1 自力で解く。
```py

"""
▶︎やり方案1(不採用)
各マスに関して、
「このマスから重複が出るまで伸ばし続けると何マス取れるか」を確認
O(n^2) 25*10^8となってちょっと遅い。
実際は後半になるにつれ、そのループの中での最大長はどんどん小さくなるので、N^2よりは少ない計算量になるはずだが、正確なものはパッとわからない。

▶︎やり方案2
「このマスが終着点だった時に、最高の長さは」を各マスに記録していく
既出の文字が発生した場合、そこより前の文字列は使用不可として考える
"""
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        if len(s) == 0:
            return 0
        dp = [0] * len(s)
        dp[0] = 1
        char_to_last_index = {s[0]: 0}
        safe_begin_point = 0
        for i in range(1, len(s)):
            char = s[i]
            if char in char_to_last_index:
                safe_begin_point = max(
                    safe_begin_point,
                    char_to_last_index[char] + 1
                )
            dp[i] = i - safe_begin_point + 1
            char_to_last_index[char] = i
        return max(dp)
```
- 何度もエラーになりながら、これを書いたが、あまりスッキリしていないなと思う。
- `char in char_to_last_index`で、今までに1回でもあったか、を確認しているが、そうではなくて、今見ている文字のsetにあるかの確認で良いはず。

# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/47/files#diff-eaf04e4839867b1c256a01b37e3fd908cd889582123148e5910d72e4e4fcb421R87-R99
    - left, rightでsubstringを管理して、毎回maxと比較。なるほど。
    - step1では、dpのマスを全部埋めたけれども、最大が知りたいだけだから、余計な情報まで覚えておく必要はなかった。
    - 尺取り法、sliding window
    - 知りたいのは要素数の最大だけなので、leftやrightの位置情報もなくてもいける

```py
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        seen = set()
        index = 0
        max_len = 0
        for char in s:
            while char in seen:
                # ユニークな組み合わせになるまで、左から消していく(leftを動かすイメージ)
                seen.remove(s[index])
                index += 1
            seen.add(char)
            max_len = max(max_len, len(seen))
        return max_len
```


- https://github.com/hayashi-ay/leetcode/pull/47/files#diff-eaf04e4839867b1c256a01b37e3fd908cd889582123148e5910d72e4e4fcb421R121-R133
    - `if c in seen_index and seen_index[c] >= left:`は私のstep1と似ているが、こちらの方がわかりやすい。

- https://github.com/olsen-blue/Arai60/pull/49#discussion_r2005295464
    - dict.get()のなかった時にdefaultで-1を返させれば、maxのところで採用されないので、if条件をなくせる、というテクニック




# STEP3　　3回連続でエラーなしで解けるまで解く

```py
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        max_length = 0
        left = 0
        seen_index = {}
        for right, char in enumerate(s):
            if char in seen_index and left <= seen_index[char]:
                left = seen_index[char] + 1
            max_length = max(max_length, right - left + 1)
            seen_index[char] = right
        return max_length
```


# STEP4 レビューFB反映