# 問題タイトル
https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/description/


# STEP 1 自力で解く。

```py
"""
探してるもの: 左から見ていって、最初に右端以下となる要素。
常に答えが(L,R]の中にあるように狭めていく
満たしたい条件は「右端以下」
狭めきって要素が残り1つになった時、残り物は、条件を満たすものの中でindexが一番小さい。
"""
class Solution:
    def findMin(self, nums: List[int]) -> int:
        left = -1
        right = len(nums) - 1
        while right - left > 1:
            mid = (left + right) // 2
            if nums[mid] <= nums[-1]:
                right = mid
            else:
                left = mid
        return nums[right]
```
- 2分探索の問題だからFFFFFTTTTTかTTTTTFFFFFで、切れ目を探す、Tの条件は何かと考えたら、解法を思いつけた。
- 2分探索の問題って知らなかったら、思いつかなかったかもしれない。
- 書き方は前の問題で見たcafelierさん方式。
-  left = -1 は0の左のイメージだけど、pythonは-1が最後尾指しちゃうからちょっとやだな。何とかならんかな。
    - [L, R)で書けば、良いのかも？

# STEP2 読みやすくする＆他の人の解法を見る
[L, R)で書いてみたが、うまくいかなかった。
- なぜうまくいかないか
    - 探しているものが範囲にないパターンがあるから。
    - 探しているもの:「右端より大きいもの」の中で最も右にあるもの
    - returnしてるもの:探したものの、一つ右。
        - →「右端より大きいもの」が[0, len(nums))にないケースがある。(最初からソート済みの時)
        - →条件は、全要素の中に必ず一つは存在するようなもので設定しないといけない。
        - →左端以上、でやればうまくいくかも?
            - →これもダメ。結局最初から整列している時うまくいかない。探したものの一つ右を返しているから。
    - →最初から最終的に欲しいものを探すべきで、それが左側にないパターンがありうるなら[L, R)で探すのは筋が良くないかも.Rは含めるのが必須なのかな?Rも含めた[L, R]も試すか
```py
# 注意: WAになります。
class Solution:
    def findMin(self, nums: List[int]) -> int:
        left = 0 # [L, R)
        right = len(nums)
        while right - left > 1:
            mid = (left + right) // 2
            if nums[mid] > nums[-1]:
                left = mid
            else:
                right = mid
        return nums[right]
```

[L, R]閉区間にしてみた。
- 下記コードでTLEになったが、なぜか
    - (半開区間の時にはwhileを抜けていて発生しなかった)「leftとrightが隣同士」のパターンが新たに発生して、mid = (left + right) // 2　＝ leftになってしまい、既知のマスを見続ける
    - left = mid + 1 にする必要があった。
        - 後から考えれば当然で、ここはmidが条件を満たさなかったときなのだから、次の探索範囲はもう一つ右から始めないと、[L, R]に答えがあるという条件が崩れてしまう。
```py
# 注意: TLEになります。
"""
[L, R]の中から、「左から見ていって、最初に右端以下となる要素」を探したい
FFFFFTTTTTT
"""
class Solution:
    def findMin(self, nums: List[int]) -> int:
        left = 0 # [L, R]
        right = len(nums) - 1
        while left < right:
            mid = (left + right) // 2
            if nums[mid] <= nums[-1]:
                right = mid
            else:
                left = mid
        return nums[right]
```



- https://github.com/hayashi-ay/leetcode/pull/45/files#diff-856251eccb601f9962fc7fdd308675f5690975413b45fe6be0950672570bc6caR7
    - 境界線を探すパターンの[left, right] (or　leftが一つ手前の要素までOKを指してrightがNGな要素を指す[left, right))
        - 最終的に欲しいのは境界線の右側にある要素だから、rightの初期値はlen(nums)だと要素がなくなってしまうので、len(nums) - 1
        - 探しているもの: 自身の右側にある要素が、右端の要素より大きくない境界線(のうち最も左にあるもの)
    - あ、いや要素探してるのか？
        - hayashiさんは前の問題で境界線探している様子だったので先入観で上記の通り境界線を探しているのかと思ったが、上記の私の[L,R]のコードとほぼ同じなので、要素を探していたのかもしれない。

- https://github.com/hayashi-ay/leetcode/pull/45/files#diff-856251eccb601f9962fc7fdd308675f5690975413b45fe6be0950672570bc6caR27
    -  左端未満の要素を探している。左端未満の要素がないパターンは、` if len(nums) == 1 or nums[0] < nums[-1]:`でうまく回避している
        - 1回もローテーション起きてないって、`nums[0] < nums[-1]`と書けたのか。スマート。
            - ローテーションが起きていると、最後尾nums[-1]の次の数字がnums[0]になるので、これが成り立たない。



- https://discord.com/channels/1084280443945353267/1230079550923341835/1233971372946882600
>nums[0] <= nums[i] な領域と nums[0] > nums[i] な領域の境界を探せ
nums[-1] < nums[i]  な領域と nums[-1] >= nums[i] な領域の境界を探せ
```py
    def findMin(self, nums: List[int]) -> int:
        return nums[bisect_left(nums, True, key=lambda x: x <= nums[-1])]
        return nums[bisect_left(nums, True, key=partial(ge, nums[-1]))]
        return nums[bisect_right(nums, False, key=lambda x: x < nums[0]) - len(nums)]
```
- 何が起きているのか
    - `bisect.bisect_left(a, x, lo=0, hi=len(a), *, key=None)` のxにTrueやFalseを渡している。
    - keyは渡すと、下記のように使われる
        - https://github.com/python/cpython/blob/cfbdce72083fca791947cbb18114115c90738d99/Lib/bisect.py#L100-L106
- 1個目
    - xにTrueを渡す
    - numsは[4, 5, 6, 7, 0, 1, 2]
    - nums[mid]keyを噛ませられるので、nums[-1]との大小比較されて、結局nums'として[False, False, False, False, True, True, True]を使っているようなもの。
    - nums'[mid]で選ばれたものと、差し込みたいx(=True)の大小比較がされる
        - False < True
            - boolはintのサブクラスで、True == 1とFalse==0は両方Trueになる。
- 2個目
    - functools.partial(func, /, *args, **keywords)
        - https://docs.python.org/3/library/functools.html#functools.partial
        - 関数の引数の一部を固定した関数を返す
        - lambda x: ge(nums[-1], x)と同じ
            - geは  a greater or equal b
                -  https://docs.python.org/ja/3/library/operator.html#operator.ge
            - 結局　nums[-1]　>= xとなって、あとは1個目と同じ  
- 3個目
    - FFFFFFTTTTにFを入れる。条件は先頭未満。
        - bisect_right([False, False, False, False, 「True」, True, True], False)
            - nums[4]
    - 最後の - len(nums)
        - 全てがFalseだった時への対策
            - [False, False, False, False, False, False, False]だと、bisect_rightは7
            - しかし、この場合ローテーションしてないので、nums[7]ではなくnums[0]を返したい
            - -7すると、元が7の時だけ負の数にならないので、前から数える感じになって位置が変わる。魔法みたい。

- https://github.com/seal-azarashi/leetcode/pull/39#discussion_r1846547487
    - `if (nums[left] < nums[right]) `だったら、その区間整列済みなので左端を返す、をしている
        - こうなるのは、A.最初から整列済み　or B.leftが縮まって整列済みになった時。
            - right=middleでが更新される時は、mid以降に探しているものがない時(mid以降が昇順であり、その中に探しているものがない時)なので、区間が整列済みであることはない。
    - どの程度有効か
        - Bについて考える
            - leftが縮まって、ちょうど探しているもののところに来たときに、発生
            - 発生した時にどれくらい残っているかで、嬉しさが違う
            - ここまでわかったが、その先の議論は理解しきれなかったので、宿題。


- https://github.com/takuya576/leetcode/pull/2#discussion_r2049272458
    - 一個後ろまでOK, NGのポインタで管理
    - leftを返すけども%している。
        - これもさっきの7引く魔法みたいなもので、ソート済みだった時対策。(左端未満の要素が見つからない。)
        - left =len(nums)の時だけ5の時だけ%5すると0になって、それより手前の時はそのまま。こっちの方がわかりやすいな。

- https://github.com/takuya576/leetcode/pull/2#discussion_r2049274082
    - 一個後ろまでOK, NGのポインタで管理


# STEP3　　3回連続でエラーなしで解けるまで解く
```py
"""
FFFFFTTT
(L, R]
要素探し
"""
class Solution:
    def findMin(self, nums: List[int]) -> int:
        left = -1
        right = len(nums) - 1
        while right - left > 1:
            mid = (left + right) // 2
            if nums[mid] <= nums[-1]:
                right = mid
            else:
                left = mid
        return nums[right]
```



# STEP4 レビューFB反映

# いただいたコメント
>何を探しているかが先にあって、その後に範囲の取り方があるはずなので、なにかとても変なことを書いているように思います。
おそらく L、R が何を意味しているのかが混乱しています。
二分探索をシフトを組んでするとして、ループごとに日付けと人が変わって仕事の引き継ぎがなされるとしましょう。
今日、あなたは初出勤です。「左は20で、右は70」とだけ書かれたメモがおいてあったら困惑しますよね。左が20とは何を意味しているのだ、となります。
ついでに、あなたは、特別な任務を帯びています。この職場にはスパイが紛れ込んでいて、誤った引き継ぎをすることによって国家を転覆しようとしています。
要するに 20 に最小値があったら、自分より前にスパイがいたといえるのか、ということです。
これが左はどういう意味かということです。


# サマリ
    1. 2分探索の理解が浅く、変な思考方法で考えていた
	    素直な思考方法:  探したいものは何か　→　どういうふうに範囲管理して探すか(L/Rの定義、初期値)
	    今回していた(少し難しい&そして失敗した)思考方法: どういうふうに範囲管理して探したいか   → 何を(どんな条件を満たすもの)探していると捉えるか 

    2. 「I.どう考えるとわかりやすいのか」、「II.どう間違って考えていたのか」、「III.2分探索の難しかった所」をまとめた。


# I.どう考えるとわかりやすいのか
- 数字の書かれたカードが裏向きに並んでる(ソート済み)
- ある数字(=ある条件を満たす/満たさない中で最も左/右にあるもの)がどこにあるか探したいな
- TTTTTFFFF または FFFFFTTTT の 境界を探したい(True/ False)問題だと捉える
```
    A.TTTTFFFの時
        満たす中で、右
        満たさない中で左
    B. FFFTTTの時
        満たす中で、左
        満たさない中で、右
```

- 毎回1枚めくると、探しているものがそこより左にあるのか、あるいは右にあるのかがわかるな。(ソート済みなので)
    - (※厳密にはそこ含め右/そこ含め左、という情報が判明する可能性もある↓が、一旦ここでは話の大筋に影響しない)
    - →例:
        - 77778の並びから「7以下の数字の中で一番右」を探す。最初に左から2番目の7をめくったとき、わかるのは「そこより右」ではなく、「そこか、そこより右」
        - 5678の並びから「7以下の数字の中で一番右」を探してて、最初に右から1番目の8をめくったとき、わかるのは「そこより左」

- めくる度にどちら側にあるかの情報がわかるので、それらを整理/統合して「今までの情報からわかる、探しているものが存在しうる範囲(=Possible Area以降PA)」を管理/更新しながら探そう。
- 左端と右端を表すL, Rというポインターを使って、PAを管理しよう
    - 管理のための3ステップ(A,B,C)
    - A.L/Rの持つ意味の厳密な定義の決定
        - 点を使って範囲の端を管理するとき、白丸か黒丸か(塗りつぶしありかなしか)、つまりそこを含むかどうかでパターンがあるが、どちらにするか。
        - Lが指してるちょうどその場所に関して
            - 白丸:「そことそこより左にはないことがわかっている(=そことそこより左は条件を満たす/満たさない)」
            - 黒丸:「そこはギリギリ探しているものが存在し得えるが、そこより左にはないことがわかっている」
    
    - B.定義が決まったので、PAをLとRで表す。
            例) L<=PA<R,　L<PA<R など

    - C.L/Rの初期値の検討
        - 初期状態は、すべての範囲(すべてのカード)がPAである。
        - BでのPAの表現方法を元に、LとRにはどういう初期値を設定してあげると、この初期状態を表せるのか考える。
            - ※初期値にカードの存在しない範囲を指定する必要があるケースがあることに注意


- 範囲(≒L/R)の更新方法の検討
    - 1枚めくった時、どのようにL/Rを更新すると、PAの定義を崩さないか考える。
    - L...TTTFFF...R となっていて、今mid=Fがわかったのなら、F側であるRをmid周辺まで動かせるはず。


- 探索ループの終了条件の検討
    - 範囲を管理しているLとRの関係性がどうなったら探し終わったと言えるか
    - 探し終わった=PAに入る要素が1つだけになった(ので最終チェックして完了)
        - ただし、Bの初期値の設定で、カードが置かれていない箇所を設定している可能性があるので、先にカードがある箇所なのか確認が必要。

    

## 解く時のSTEPとしてまとめ直したもの。
- 1.探したいものの言語化(同時にTTTFFF/ FFFTTTどちらだと捉えているのかが決まる)
    - 例) XXXという条件を満たす(満たさない)ものの中で最も右(左)にあるもの.
        - →これでTTTFFFと捉えるのか、FFFTTTと捉えるのか決まる
- 2.探し物がありうる範囲(PA)の左端右端であるL/Rは白丸なのか黒丸なのか(そこに探し物が存在しうるか)の定義を決める。
- 3.2で決めた定義をもとに、L<=PA<Rなどの形でPAの範囲をL,Rで表す。
- 4.最初は全てのカードが裏返しでPAなので、その状態を表せるよう適切な初期値をLとRに入れる
- 5.LとRの間にTTTFFFが並んでいると考えて、1枚めくった時の更新方法を考える
    - 例) L..TTTFFF..Rとなっていて、今mid=Fがわかったのなら、F側であるRをmid周辺(midやmidの一つ右など。Rの定義次第。)まで動かして、PAの範囲を狭められる。
- 6.PAの中の要素が残り1個になるような終了条件を考える
- 7.最後に最終チェックをして、求められているものを返す。


## 補足
- 無限ループへの対処の理解の整理
    - midは (L + R)// 2 で計算するので、mid = Lとなってしまうことがある。(LとRが隣同士になると起きる)
    - midを計算した後、LもしくはRをmidを使って更新するが、ここでL=midと更新している場合、L=mid=Lとなって実質更新しておらず、無限ループになりうる。
    - 自分の書いた更新処理や、ループの終了条件が、無限ループになってしまうようなものになっていないか、念の為確認すると良い?




-----------------------------------------------------------------------------------------------------------------------------------



# II.どう間違って考えていたのか

### 前提:
    そもそも「この方法で書きたいな」が先行した少し変な思考回路になっていた。
    この思考回路でも、書きたい方法が決まった中で、しっかりと「どのような問題だと捉えるか」も検討すれば解けたが、そのようなことをする必要性に気づけていなかった。(後から整理したところ、この思考回路でも解けたが、少し複雑になるのでこの方法では考えない方が良さそう)


### 誤っていた理解その1:
        「[L, R)で管理すれば、L=0から始められてスッキリしそう」→間違い
		(補足)ここでの[L, R)で管理するの意味=探しているものが、[L, R)の範囲にあるように、狭めていく
		・L:探しているものはここより左には絶対ない(ここにはあるかも)
		・R:探しているものはここおよびここより右には絶対ない

	→これは間違い
	 "[L"を使ったら自動でL=0から書けると思い込んでいたが、そうではない。
     Lの定義を決めたら自動でLの初期値が決まるわけではない
     素直な解き方だと、「A:何を探していると捉えるか + B:L(とR)の定義」の二つを決めて、C:Lの初期値が決まる。
     今は逆にBとCからAを逆算しているので、Aはどんなものであれば良いのか注意深く検討が必要だったが、それに気づいておらず適当にAを決めたので失敗した



### 誤っていた理解その2
	[L,R)　or [L, R] で書きたいなら、TTTTTFFF
	(L, R]   or [L, R] で書きたいなら、FFFFFTTT 
		で捉えないといけない
			→つまり、Tがある方(右or左)は、そこを含むような管理方法(“[“または”]”)で管理しないといけない
        
    →これは間違い。
        　TやFは条件をひっくり返せば逆にできるので、あまり本質的な話ではない。
            例)下記二つは同じものを探している
               ・「左端の値以上のもの(T)で一番右」TTTFFF
            　 ・「左端未満という条件を満たさないもの(F)で一番右」FFFTTT
        　

### 実際に、「書きたい方法→何を探していると捉えるか」という逆の方法で解いてみた時のメモとコード
```py
"""
[L, R)かつLの初期値=0で書きたい→探しているものはどんな条件を満たすものだと捉えると良いか
    ※"満たさないもの"を探している可能性もあるが、条件を裏返せば良いだけなので、一旦満たすものを探していると考える

    補足:[L,R)
        ・L:探しているものはここより左には絶対ない(ここにはあるかも)　←→ここより左は条件を満たす
		・R:探しているものはここおよびここより右には絶対ない　←→こことここより右は条件を満たさない

[L,R)
    →Lが存在している場所には答えが存在しうる(条件を満たす)
    →TTTFFFという左がTの問題として捉える

Lの初期値は0
    →左端は必ず満たす条件のものを探している問題として捉える
    →左端のマスにTをおける、と言っている
            →左端は条件を 満たすことがわかっている必要があるということ。
                →A.左端と比較するような条件である必要がある
                    左端以上、とか


→以上から、「左端以上の中で一番右」を探したあと、その右隣を返すことにする
"""
class Solution:
    def findMin(self, nums: List[int]) -> int:
        # 探したいもの: 左端以上のものの中で一番右　(そして最後にそれの一つ右を返す)　TTTFFF
        # L: 探しているものはLより左にはない(Lにはあるかも)　
        # R: 探しているものはRとそこより右にはない
        # L <= possible area < R
        left_i = 0 # 初期値は0
        right_i = len(nums) # 初期値は一番右のマスの一つ外
        while right_i - left_i > 1: # 隣同士になったら残要素1こなので終了
            mid = (left_i + right_i) // 2
            if nums[mid] >= nums[0]:
                left_i = mid
            else:
                right_i = mid
        result_i = left_i + 1
        if result_i == len(nums):
            result_i = 0
        return nums[result_i]

```



# III.2分探索の難しかった所
### BFS/DFSみたいな感じで、閉区間/開区間という2通りの解き方があるのかと勘違いして理解しようとしてしまった。
    当初持っていた誤ったメンタルモデル
        2分探索は、「閉区間/開区間」2通りの解き方から選んで解くもの。
        1つの問題に対して解き方(捉え方)は2通りしかないので、どのどちらであるのか、を宣言すれば伝わるはず。

    実際は2分探索(真ん中ら辺を選んで範囲の左端/右端を絞って探していく)と一口に言っても色々ある
        左端/右端を含むか含まないか
        要素を探しているのか、境界を探しているのか
        未確認の範囲を管理しているのか、探しているものが存在しうる範囲を管理しているのか
        そもそも探しているものは何だと捉えているのか
        etc
        で色々なパターンがあり、さらに言うと、同じコードでも、別の解釈で読み解くことができる(詳細は下記)


### 同じコードに対して、それに対する解釈(メンタルモデル)を複数パターン用意することができてしまう。
    つまり、コードだけしかないとその人がどういう考えで書いたのか、特定が難しい。
    今までの問題ではそいうことがなかった?(or気づいていなかっただけ?)ので、混乱した。
    要素を探している、という捉え方のほかに、要素同士の境界線を探している、という捉え方もできる。
    境界を探していると捉えている例
        https://github.com/hayashi-ay/leetcode/pull/40/files#diff-912c29ce0f491ca88b1474ced97db89f2d33a36a34cd0f516f5b523e400af176R21
    要素を探していると捉えている例
        https://github.com/h1rosaka/arai60/pull/43#discussion_r2395031737



### 目的(何を探したいのか)から考えると良い、ということがわかっていなかった
    目的(何を探したいのか)から落としていけば、素直にhowが決まるが、
    自分の書きたい書き方のhow(leftの意味/初期値)から、何を探している問題だと捉えるかを逆に考えると、考慮漏れが発生しやすい?
    そもそも「’書き方’と”どんな問題だと捉えるか”が対応している」という理解が浅かったので、「自分がどんな問題だと捉えようとしているか」ということへの意識が薄かった


### 変な操作をしていることに気づきにくい
    今までの他のの問題では中身がわかるような変数名にこだわっていたのに、二分探索だけは雑にleftって書いてしまうので、
    自分がおかしな操作(代入/更新/それを使った計算)をしていることに気づくきっかけがない

### leftという変数にいろんな意味が込められているし、込められる意味のパターンにいくつか選択肢があるが、変数名からどの選択肢を取っているのかわからない
    leftという変数を見てわかりたい情報
        範囲を管理するための変数なのですよ(left/ right ,hgiht/lowで伝わる系のニュアンス)
        範囲の左端ですよ(left, loで伝わる系のニュアンス)
        左端そのものは含みますよ・含みませんよ
        範囲の意味はXXXですよ(探しているものが存在する範囲?未確認の範囲?)
        条件を満たすとわかったものの中で一番大きいindexです
            →これは管理の仕方によって違う意味になるバリエーションもある。その人がどう考えているのか推し量る必要がある
        条件はXXXです
    



-----------------------------------------------------------------------------------------------------------------------------------

# Appendix: 理解の途中に書いたメモも残しておく(未整理)

```py
"""
1.何を探したいのか整理する
	XXXという条件を{満たす|満たさない}ものの中で、一番{左|右}にあるもの												
2.探索範囲を管理するための変数LとRの意味を決める
    L(R)が指す {手前のマス|そのマス|(一個先のマス)}とそこより左(右)は条件を{満たす|満たさない}
3.LとRの意味をもとに、全体を探せるようなL,Rの初期値を決める
(4. 範囲が必ず減るように、midの更新方法とかを気をつける？？)


TODO: 一個先のマス、は本当にあまりやらないんだっけ？ 


1-1. TARGET以上という条件を満たすものの中で、一番左 
            FFFFTTT
1-2. TARGET未満という条件を満たさないものの中で、一番左
            TTTTFFF
1-3. TARGET以下という条件を満たすものの中で、一番右
            TTTTFFF
1-4. TARGETより大という条件を満たさないものの中で、一番右
            FFFFTTT
→{Tが左と捉える| Tが右と捉える} & targetを{T| F} とする

(以下、1-1だったと仮定して)
2-1
    L: Lが指す そのマスとそこより左は条件を満たさない
    R: Rがさす、そのマスとそこより右は条件を満たす(ただし、そこがマスの範囲外であれば、満たさない)
        →隣り合わせになる(探し物が存在しうるのは1マス)までやって、Rを返せばOK
2-2
    L: Lが指す 手前のマスとそこより左は条件を満たさない
    R: Rがさす、そのマスとそこより右は条件を満たす(ただし、そこがマスの範囲外であれば、満たさない)
        →重なるまでやって、Rを返せば良い
2-3
    L: Lが指す そのマスとそこより左は条件を満たさない
    R: Rがさす、手前のマスとそこより右は条件を満たす(ただし、そこがマスの範囲外であれば、満たさない)
        →重なるまでやって、Rの手前を返せば良い
2-4.
    L: Lが指す 手前のマスとそこより左は条件を満たさない
    R: Rがさす、手前のマスとそこより右は条件を満たす(ただし、そこがマスの範囲外であれば、満たさない)
        →すれ違うまでやって、Rの手前(つまりL)を返せば良い

開、閉は、未確認の区間のこと？
→あるいは探しているものが存在しうる区間、とも捉えられる
    2-1の場合、未確認の区間は、L<X<Rで、探しているものが存在しうる区間は、L<X<=R(ただし、Rがマスの範囲内の場合)
    2-2の場合、未確認の区間は、L<=X<Rで、探しているものが存在しうる区間は、L<=X<=R(ただし、Rがマスの範囲内の場合)
    2-3の場合、未確認の区間は、L<X<=Rで、探しているものが存在しうる区間は、L<X<=R + 1(ただし、Rがマスの範囲内の場合)
    2-4の場合、未確認の区間は、L<=X<=Rで、探しているものが存在しうる区間は、L<=X<=R + 1(ただし、Rがマスの範囲内の場合)

TODO: なぜL<=X<Rや、L<X<Rが出てこない？
        (一個先のマス)のパターンを列挙してないので、漏れてる。
        左側がFになる条件なので、Lは"満たさない"となるためL<

--------------------
何の範囲なのか、について言わないまま、境界の値を含むか含まないのかの話(閉/開く)をするから、混乱する?
XXXXをYYYYという方法で管理します。
YYYYだけ言及すれば、どんな捉え方

    →本当に大事なのは、LとRが何を指しているかであって、
        TODO: 考える。範囲とそのLRの定義は対応しているのか？
        正攻法
            1. L, Rの定義を決める
            2. そうすると{未確認の区間 | 探しているものが存在している区間}は、LとRを使ってXXX（←これが閉区間or半開区間etc の形に。）と表せます。
            3. なので、探索終了の条件はLとRを使ってこのように表せて、最後に確認して返すべきものはZZZになります。
        
        ここでの気づき
            - 同じL,Rの定義を使っていても、「未確認の区間」を管理すると捉えているか、「探しているものが存在しうる区間」を管理すると捉えているかで、開区間なのか
            半開区間なのか変わる。
                →が、「探しているものが存在しうる区間」を管理すると捉える方が自然か？
                (TODO: 難しさまとめに追記。なんの範囲を管理しているのか明示的でない。)



FFFFTTTTT
"""

# 2-1.
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        left = -1 # マスの外側　Lとそこより左は条件を満たさない
        right = len(nums) # マスの外側。Rとそこより右はtarget以上という条件を満たす。
        # FFFFTTT
        while right - left > 1:
            mid = (left + right) // 2
            if nums[mid] >= target:
                right = mid
            else:
                left = mid + 1
        # ここからさらに、rightがindex外になるパターンがあるので考慮が必要
        if nums[right] == target:
            return right
        return -1
```









