# 問題タイトル
https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/description/


# STEP 1 自力で解く。

```py
"""
探してるもの: 左から見ていって、最初に右端以下となる要素。
常に答えが(L,R]の中にあるように狭めていく
満たしたい条件は「右端以下」
狭めきって要素が残り1つになった時、残り物は、条件を満たすものの中でindexが一番小さい。
"""
class Solution:
    def findMin(self, nums: List[int]) -> int:
        left = -1
        right = len(nums) - 1
        while right - left > 1:
            mid = (left + right) // 2
            if nums[mid] <= nums[-1]:
                right = mid
            else:
                left = mid
        return nums[right]
```
- 2分探索の問題だからFFFFFTTTTTかTTTTTFFFFFで、切れ目を探す、Tの条件は何かと考えたら、解法を思いつけた。
- 2分探索の問題って知らなかったら、思いつかなかったかもしれない。
- 書き方は前の問題で見たcafelierさん方式。
-  left = -1 は0の左のイメージだけど、pythonは-1が最後尾指しちゃうからちょっとやだな。何とかならんかな。
    - [L, R)で書けば、良いのかも？

# STEP2 読みやすくする＆他の人の解法を見る
[L, R)で書いてみたが、うまくいかなかった。
- なぜうまくいかないか
    - 探しているものが範囲にないパターンがあるから。
    - 探しているもの:「右端より大きいもの」の中で最も右にあるもの
    - returnしてるもの:探したものの、一つ右。
        - →「右端より大きいもの」が[0, len(nums))にないケースがある。(最初からソート済みの時)
        - →条件は、全要素の中に必ず一つは存在するようなもので設定しないといけない。
        - →左端以上、でやればうまくいくかも?
            - →これもダメ。結局最初から整列している時うまくいかない。探したものの一つ右を返しているから。
    - →最初から最終的に欲しいものを探すべきで、それが左側にないパターンがありうるなら[L, R)で探すのは筋が良くないかも.Rは含めるのが必須なのかな?Rも含めた[L, R]も試すか
```py
# 注意: WAになります。
class Solution:
    def findMin(self, nums: List[int]) -> int:
        left = 0 # [L, R)
        right = len(nums)
        while right - left > 1:
            mid = (left + right) // 2
            if nums[mid] > nums[-1]:
                left = mid
            else:
                right = mid
        return nums[right]
```

[L, R]閉区間にしてみた。
- 下記コードでTLEになったが、なぜか
    - (半開区間の時にはwhileを抜けていて発生しなかった)「leftとrightが隣同士」のパターンが新たに発生して、mid = (left + right) // 2　＝ leftになってしまい、既知のマスを見続ける
    - left = mid + 1 にする必要があった。
        - 後から考えれば当然で、ここはmidが条件を満たさなかったときなのだから、次の探索範囲はもう一つ右から始めないと、[L, R]に答えがあるという条件が崩れてしまう。
```py
# 注意: TLEになります。
"""
[L, R]の中から、「左から見ていって、最初に右端以下となる要素」を探したい
FFFFFTTTTTT
"""
class Solution:
    def findMin(self, nums: List[int]) -> int:
        left = 0 # [L, R]
        right = len(nums) - 1
        while left < right:
            mid = (left + right) // 2
            if nums[mid] <= nums[-1]:
                right = mid
            else:
                left = mid
        return nums[right]
```



- https://github.com/hayashi-ay/leetcode/pull/45/files#diff-856251eccb601f9962fc7fdd308675f5690975413b45fe6be0950672570bc6caR7
    - 境界線を探すパターンの[left, right] (or　leftが一つ手前の要素までOKを指してrightがNGな要素を指す[left, right))
        - 最終的に欲しいのは境界線の右側にある要素だから、rightの初期値はlen(nums)だと要素がなくなってしまうので、len(nums) - 1
        - 探しているもの: 自身の右側にある要素が、右端の要素より大きくない境界線(のうち最も左にあるもの)
    - あ、いや要素探してるのか？
        - hayashiさんは前の問題で境界線探している様子だったので先入観で上記の通り境界線を探しているのかと思ったが、上記の私の[L,R]のコードとほぼ同じなので、要素を探していたのかもしれない。

- https://github.com/hayashi-ay/leetcode/pull/45/files#diff-856251eccb601f9962fc7fdd308675f5690975413b45fe6be0950672570bc6caR27
    -  左端未満の要素を探している。左端未満の要素がないパターンは、` if len(nums) == 1 or nums[0] < nums[-1]:`でうまく回避している
        - 1回もローテーション起きてないって、`nums[0] < nums[-1]`と書けたのか。スマート。
            - ローテーションが起きていると、最後尾nums[-1]の次の数字がnums[0]になるので、これが成り立たない。



- https://discord.com/channels/1084280443945353267/1230079550923341835/1233971372946882600
>nums[0] <= nums[i] な領域と nums[0] > nums[i] な領域の境界を探せ
nums[-1] < nums[i]  な領域と nums[-1] >= nums[i] な領域の境界を探せ
```py
    def findMin(self, nums: List[int]) -> int:
        return nums[bisect_left(nums, True, key=lambda x: x <= nums[-1])]
        return nums[bisect_left(nums, True, key=partial(ge, nums[-1]))]
        return nums[bisect_right(nums, False, key=lambda x: x < nums[0]) - len(nums)]
```
- 何が起きているのか
    - `bisect.bisect_left(a, x, lo=0, hi=len(a), *, key=None)` のxにTrueやFalseを渡している。
    - keyは渡すと、下記のように使われる
        - https://github.com/python/cpython/blob/cfbdce72083fca791947cbb18114115c90738d99/Lib/bisect.py#L100-L106
- 1個目
    - xにTrueを渡す
    - numsは[4, 5, 6, 7, 0, 1, 2]
    - nums[mid]keyを噛ませられるので、nums[-1]との大小比較されて、結局nums'として[False, False, False, False, True, True, True]を使っているようなもの。
    - nums'[mid]で選ばれたものと、差し込みたいx(=True)の大小比較がされる
        - False < True
            - boolはintのサブクラスで、True == 1とFalse==0は両方Trueになる。
- 2個目
    - functools.partial(func, /, *args, **keywords)
        - https://docs.python.org/3/library/functools.html#functools.partial
        - 関数の引数の一部を固定した関数を返す
        - lambda x: ge(nums[-1], x)と同じ
            - geは  a greater or equal b
                -  https://docs.python.org/ja/3/library/operator.html#operator.ge
            - 結局　nums[-1]　>= xとなって、あとは1個目と同じ  
- 3個目
    - FFFFFFTTTTにFを入れる。条件は先頭未満。
        - bisect_right([False, False, False, False, 「True」, True, True], False)
            - nums[4]
    - 最後の - len(nums)
        - 全てがFalseだった時への対策
            - [False, False, False, False, False, False, False]だと、bisect_rightは7
            - しかし、この場合ローテーションしてないので、nums[7]ではなくnums[0]を返したい
            - -7すると、元が7の時だけ負の数にならないので、前から数える感じになって位置が変わる。魔法みたい。

- https://github.com/seal-azarashi/leetcode/pull/39#discussion_r1846547487
    - `if (nums[left] < nums[right]) `だったら、その区間整列済みなので左端を返す、をしている
        - こうなるのは、A.最初から整列済み　or B.leftが縮まって整列済みになった時。
            - right=middleでが更新される時は、mid以降に探しているものがない時(mid以降が昇順であり、その中に探しているものがない時)なので、区間が整列済みであることはない。
    - どの程度有効か
        - Bについて考える
            - leftが縮まって、ちょうど探しているもののところに来たときに、発生
            - 発生した時にどれくらい残っているかで、嬉しさが違う
            - ここまでわかったが、その先の議論は理解しきれなかったので、宿題。


- https://github.com/takuya576/leetcode/pull/2#discussion_r2049272458
    - 一個後ろまでOK, NGのポインタで管理
    - leftを返すけども%している。
        - これもさっきの7引く魔法みたいなもので、ソート済みだった時対策。(左端未満の要素が見つからない。)
        - left =len(nums)の時だけ5の時だけ%5すると0になって、それより手前の時はそのまま。こっちの方がわかりやすいな。

- https://github.com/takuya576/leetcode/pull/2#discussion_r2049274082
    - 一個後ろまでOK, NGのポインタで管理


# STEP3　　3回連続でエラーなしで解けるまで解く
```py
"""
FFFFFTTT
(L, R]
要素探し
"""
class Solution:
    def findMin(self, nums: List[int]) -> int:
        left = -1
        right = len(nums) - 1
        while right - left > 1:
            mid = (left + right) // 2
            if nums[mid] <= nums[-1]:
                right = mid
            else:
                left = mid
        return nums[right]
```



# STEP4 レビューFB反映