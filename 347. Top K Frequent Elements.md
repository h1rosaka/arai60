# 問題タイトル
問題リンク
https://leetcode.com/problems/top-k-frequent-elements/description/


# STEP 1 自力で解く。
```py
class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        num_to_count = {}
        frequent_elements = []
        for num in nums:
            num_to_count[num] = num_to_count.get(num, 0) + 1
        sorted_values = sorted(num_to_count.values(), reverse=True)
        top_k_counts =  set(sorted_values[:k])
        for num, count in num_to_count.items():
            if count in top_k_counts:
                frequent_elements.append(num)
        return frequent_elements
```
- 所要時間 16min

- 計算量
    - 時間:O(n + mlogm)
        - for ループ：O(n)
            - 1回あたりは、O(1)
            - 見つかったユニークな要素数をmとする
        - sortするのにO(mlogm)
        - setの作成: O(k)
            - [:k]はコピー発生するのでk
            - リストの要素を一つずつsetにaddするのでk
        - for ループ　m回 O(m)
            - 1回あたりは、O(1)
        - 合計：
            - kとmはより大きいnに吸収されて、O(n + mlogm)
    - 空間:O(m)
        - num_to_countで O(m)
        - sortするのに　O(m)
        - top_k_countsはO(k)
        - frequent_elementsはO(k)


つまづいたところ/コメント
- num_to_count.values.sort()としていた　valuesはvalues()
    - pandasのSeriesのvaluesはプロパティだから()いらないが、[dictのvalues()](https://docs.python.org/ja/3/library/stdtypes.html#dict.values)はメソッドなので必要
- num_to_count.values().sort()としたら、valuesの返り値はリストではなかったので失敗
    - > dict.keys(), dict.values(), dict.items() によって返されるオブジェクトは、 ビューオブジェクト です。これらは、辞書の項目の動的なビューを提供し、辞書が変更された時、ビューはその変更を反映します。
    - https://docs.python.org/ja/3/library/stdtypes.html#dictionary-view-objects

- reverse=Trueの入れ忘れ

- 同率がいた時、全部OKにしてしまっている。
    - hayashi-ayさんのは(count, num)のタプルの配列を作ってセットのままバラさずにソートしてからk個に絞っているのでこういうことが起きない。
    - 追記:[実際の仕事をイメージすると、同率全部出ますは避けた方がよさそう](https://discord.com/channels/1084280443945353267/1367399154200088626/1371325723612151918)

- バラさずに辞書のままソートできないのだろうか
    - sortedとlambdaを使うと、[(key, value),..]のリストが得られる
    - https://qiita.com/yousuke_yamaguchi/items/23014a3c8d8beb8ba073
```py
# keyで並び替え
score_sorted = sorted(score.items(), key=lambda x:x[0])
print(score_sorted) # [('eigo', 60), ('kokugo', 33), ('sansuu', 85)]

#valueで
score_sorted = sorted(score.items(), key=lambda x:x[1])
print(score_sorted) # [('kokugo', 33), ('eigo', 60), ('sansuu', 85)]
```
[これ]↓(https://github.com/fuga-98/arai60/pull/10#discussion_r1967591652)だとvalueでソートしたkeyだけがもらえるとのこと
```py
sorted(num_count, key=num_count.get, reverse=True)
```
- 第一引数のnum_count
    - [sorted](https://docs.python.org/ja/3.13/library/functions.html#sorted)の第一引数はiterable
        - > sorted(iterable, /, *, key=None, reverse=False)
        - > iterable の要素を並べ替えた新たなリストを返します。
    - 辞書はイテラブルで、こちらの[for文の例](https://stackoverflow.com/questions/59270856/when-iterating-over-a-dictionary-in-python-why-do-you-get-the-key-and-not-the-k)を見ると、内部的にはiter()を呼んでいるらしい。
    - >iter(d) Return an iterator over the keys of the dictionary This is a shortcut for iter(d.keys()).
        - https://docs.python.org/3/library/stdtypes.html#mapping-types-dict
    - つまり辞書をイテラブルとしてイテレートすると、keyだけが返ってくるようになっている。

- 第二引数のkey=num_count.get
    - lambda渡す時と同じで、関数を渡している。
    - 関数の引数にはイテラブルの中の一つが渡される
    - num_count.get()と書いてしまうと関数が実行された結果が渡ってしまう。渡したいのは関数そのもの。

- 逆にkeyでソートしたvalueだけが欲しいなら？
```py
[num_count[key] for key in sorted(num_count)]
list(map(num_count.get, sorted(num_count)))
```

# STEP2 読みやすくする＆他の人の解法を見る
https://github.com/hayashi-ay/leetcode/pull/60/files?short_path=013dde8#diff-013dde8937de27960afd2d09c6325ebf53eb00c65d9d2772bc598e798bcdb877

- Counter
    - class collections.Counter([iterable-or-mapping])
    - > Counter は ハッシュ可能 なオブジェクトをカウントする dict のサブクラス
    - https://docs.python.org/ja/3.13/library/collections.html#collections.Counter

- 複数要素があるもののsort
    - (count, num)にすることでcountでソートされるようにしている。
        - ちなみにソートは安定しているので、同率カウントの場合は元の順序
    - itemgetter()なるものがある。
    ```py
        student_tuples = [
            ('john', 'A', 15),
            ('jane', 'B', 12),
            ('dave', 'B', 10),
        ]
        sorted(student_tuples, key=lambda student: student[2]) #sort by age
        sorted(student_tuples, key=itemgetter(2)) # sort by age
    ```

### ソートのSTEP2
```py
class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        num_to_count = {}
        for num in nums:
            num_to_count[num] = num_to_count.get(num, 0) + 1
        return sorted(num_to_count, key=num_to_count.get, reverse=True)[:k]
```
- 計算量
    - 時間:O(nlogn)
        - 辞書を埋めるのにn
        - sortにnlogn
        - スライスにk
    - 空間:O(n)
        - 辞書にn
        - ソートするのにもn


# クイックセレクトのStep2

### ロジックがわかりやすいが、インプレースではないのでメモリ効率が悪いバージョン。
- 自分より大きいもののリストhighと、自分と同じ値のもの何個かを返すことでk個返す作戦
```py
class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        freq_dict = Counter(nums)
        freq = [(count, num) for num, count in freq_dict.items()]

        def quickselect(arr, k):
            if not arr:
                return []

            pivot = random.choice(arr)
            lows = [x for x in arr if x[0] < pivot[0]]
            pivots = [x for x in arr if x[0] == pivot[0]]
            highs = [x for x in arr if pivot[0] < x[0]]
            
            
            if k <= len(highs):
                # highが大きすぎるので、そこから絞るケース
                return quickselect(highs, k)
            elif k <= len(highs) + len(pivots):
                # high全てと「自分と同じ値のもの」を何個か返すとちょうどOKなケース
                return highs + pivots[:k - len(highs)]
            else:
                # highと自分と同じ値のものでもまだ足りないので、足りない分だけlowから探すケース
                return highs + pivots + quickselect(lows, k - len(highs) - len(pivots))

        top_k = quickselect(freq, k)
        return [num for count, num in top_k]

```
- 時間:O(N)　最悪はO(N^2)
    - 1回あたり、low/pivot/highの作成にNこの要素を3回見てO(n)
    - 2回目以降は要素数が半分になっていくと考えると、合計で見る要素数はN + N/2 + N/4 + ... = 2N
    - (最悪ケースの時は、　N +（N-1）+ (N-2) + ...+ (N-N + 1) = (n^2 + n)/2) で、O(N^2)
    - https://stackoverflow.com/questions/56940793/quickselect-time-complexity-explained

- 空間: O(n) 最悪はO(N^2)
    - freq_dict　O(n)
    - freq O(n)
    - low/pivot/high 初回は合計で長さnのメモリ。その後は時間計算量と同様に平均ならO(N)、最悪ならO(N^2)
    - 再帰の回数分のスタック　logn
### inplaceバージョン
```py
"""
▶️quickselect()
ランダムにピボットをとる
partition()で、pivotより左側には、大きいものだけがくるようにする
pivotの場所がkth_largestのindex(=k-1)と同じなら、k-1番目までを返せばOK
k-1 　< pivotの場所　なら、
    biggerたちをもう少し整列する必要がある
    →pivotの場所の一つ手前までで、もう一度
pivotの場所 < k-1 なら
    足りないので、pivotより右側のsmallerのうち幾らかを追加で整列する必要がある 
    →pivotの場所より一つ右からで、もう一度

▶️partition()
引数：配列、left,rightの範囲、範囲内のランダムなpivot_index一つ
pivotのものを一番右に置いておく
leftから舐めていって、tailポインタ使いながらpivotより大きければswapで左詰めしていく
全要素見たら、pivotを正しい位置(tail)に差し込む
"""
class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        counter = Counter(nums)
        unique_nums = list(counter.keys())

        def partition(nums, left, right, pivot_index):
            pivot_count = counter[nums[pivot_index]]
            nums[right - 1], nums[pivot_index] = nums[pivot_index], nums[right - 1]
            sorted_tail = left
            for i in range(left, right - 1):
                count = counter[nums[i]]
                if pivot_count < count:
                    nums[i], nums[sorted_tail] =  nums[sorted_tail], nums[i]
                    sorted_tail += 1
            nums[right - 1], nums[sorted_tail] = nums[sorted_tail], nums[right - 1]
            return sorted_tail

        def quickselect(nums, left, right):       
            pivot_index = random.randint(left, right - 1)
            pivot_index = partition(nums, left, right, pivot_index)
            if pivot_index == k - 1:
                return
            if pivot_index < k - 1:
                quickselect(nums, pivot_index + 1, right)
            else:
                quickselect(nums, left, pivot_index)
        
        quickselect(unique_nums, 0, len(unique_nums))
        return unique_nums[:k]
```
- 時間:O(n)　最悪はO(m^2)
    - counter O(n)
    - unique_nums O(m)
    - 初回はN個、2回目以降は要素数が半分になっていくと考えると、合計で見る要素数はm + m/2 + m/4 + ... = 2m
    - (最悪ケースの時は、　m +（m-1）+ (m-2) + ...+ (m-m + 1) = (m^2 + m)/2) で、O(m^2)
  

- 空間: O(m) 最悪でもO(m)
    - counter　O(m) 
    - unique_nums O(m)
    - 再帰の回数分のスタック　logm ~ m



# ヒープのSTEP2
```py
class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        nums_to_count = {}
        for num in nums:
            nums_to_count[num] = nums_to_count.get(num, 0) + 1
        top_k_count = []
        for num, count in nums_to_count.items():
            heapq.heappush(top_k_count, (count, num))
            if len(top_k_count) > k:
                heapq.heappop(top_k_count)
        return [num for count, num in top_k_count]

```
- 時間:nlogk
    - nums_to_countつくる: n
    - n回ループ→nlogk
        - push/pop: logk
    - 最後の配列作成：k
- 空間:n
    - nums_to_countがn
    - heap: k
    - 最後の配列: k

- クイックソートについて知っていなくてはいけないこと
    - https://discord.com/channels/1084280443945353267/1183683738635346001/1185972070165782688
    - 最悪・平均計算量
        - 平均：O(nlogn)
            - 毎回真ん中でpivot選べたとすると、回ごとに半分になるので、回の数はlogn
            - 1回の中で、全ての要素がpivotと比較されるのでn
            - ランダムにピボットを選ぶと、平均的にはO(nlogn)で終わることが知られているとのこと
        - 最悪: O(n^2)
            - ピボットが毎回最小値だと、回の数がnになり、1回あたりは変わらないのでn^2
            - これは実質選択ソート(selectioin sort) (一番小さいの探して左に持っていく方法)
    - 末尾再帰最適化
        - 戻ってくる位置覚えておかなくて良くなって、スタックオーバーフローが発生しなくなる
    - ピボット選択
        - 上記の通りランダムに。
    - マージソートとのプロコン

マージソート
| 項目  | 内容         |
| ------ | ----------------------------------------- |
| **pros** | - 最悪計算量が O(n log n) で安定している |
|          | - 安定ソート(同じ値の要素の順序が変わらない)|
|          | - 並列処理に向いている（分割が独立している） |
| **cons** | - 空間計算量が O(n) と大きい（追加メモリが必要）|
|          | - 実装がやや複雑    |
|          | - 小規模データでは遅くなることもある (追加のメモリ割り当てや解放などのオーバーヘッドがある) |


クイックソート
| 項目         | 内容   |
| ---------- | ------------------------------- |
| **pros** 　| - 平均計算量が O(n log n) と高速（実行速度が速い    |
|           |- 空間計算量が O(log n) と小さい（再帰のスタックのみ）|
|           |- 実装が比較的簡単で高速なライブラリソートにも多く使われる |
| **cons** | - 最悪計算量は O(n²)（pivotの選び方が悪いと偏る）   |
|          |- 不安定ソート（同じ値の順序が変わる）|
|          | - 小さい配列でのパフォーマンスは最適ではない              |





- 平衡木
    - https://discord.com/channels/1084280443945353267/1227073733844406343/1231268645628416020
    - 平衡木に入れておけば順序がわかる(左→中央→真ん中の順で辿ると小さい順)
    - 一つずつ平衡木に入れて(logN)、topK個を取り出す(最大値まで辿り着くのにlogN, そこからk個辿る)
    - pythonの標準ライブラリにはないが、[sortedcontainers.SortedList](https://grantjenks.com/docs/sortedcontainers/introduction.html#sorted-list)というものがある様子



# STEP3　　3回連続でエラーなしで解けるまで解く
step2と同じ。
```py
class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        counter = Counter(nums)
        unique_nums = list(counter.keys())

        def partition(nums, left, right, pivot_index):
            pivot_count = counter[nums[pivot_index]]
            nums[right - 1], nums[pivot_index] = nums[pivot_index], nums[right - 1]
            sorted_tail = left
            for i in range(left, right - 1):
                count = counter[nums[i]]
                if pivot_count < count:
                    nums[i], nums[sorted_tail] =  nums[sorted_tail], nums[i]
                    sorted_tail += 1
            nums[right - 1], nums[sorted_tail] = nums[sorted_tail], nums[right - 1]
            return sorted_tail

        def quickselect(nums, left, right):       
            pivot_index = random.randint(left, right - 1)
            pivot_index = partition(nums, left, right, pivot_index)
            if pivot_index == k - 1:
                return
            if pivot_index < k - 1:
                quickselect(nums, pivot_index + 1, right)
            else:
                quickselect(nums, left, pivot_index)
        
        quickselect(unique_nums, 0, len(unique_nums))
        return unique_nums[:k]


```
8:50, 7:04, 10:24(翌日やったので少し減速)

# STEP4 レビューFB反映