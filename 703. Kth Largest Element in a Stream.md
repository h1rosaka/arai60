# 問題
https://leetcode.com/problems/kth-largest-element-in-a-stream/


# STEP 1 自力で解く。
```python
    class KthLargest:
        def __init__(self, k: int, nums: List[int]):
            self.until_k_th = sorted(nums, reverse = True)[:k]

        def add(self, val: int) -> int:
            self.until_k_th += [val] 
            self.until_k_th = sorted(self.until_k_th, reverse = True)[:-1]
            return self.until_k_th[-1] 
```
- 所要時間 15分
- どこでつまづいたか/感想
    - reverse=Trueを入れ忘れた & 入れ方も忘れていたので調べた。
    - メモリがもったいなかったかも
        - sortedは新たにリスト作る
            - (追記)関数内では引数の中身を原則変えない方が良いらしい
                - https://github.com/rinost081/LeetCode/pull/9/files#r1874734978
        - スライスも新たに作る
        - self.until_k_th += [val] も無駄にリスト作って合わせているが、普通にappend()で良かった
- 計算量
    - 時間 : O(nlogn) + O(jklogk)
        - 前提 : numsの要素数をn、addが呼ばれる回数を j とする。
        - initの操作: nlogn
            - PythonのsortはTimsortなので、最悪でもO(nlogn)
            - スライスはコピー作るので、O(k)
            - nlognの方が大きいのでnlogn
        - addの操作: 
            - ソートは、 klogk
            - スライスはコピー作るので、O(k)
            - klogkの方が大きいのでO(j*klogk)


    - 空間 : O(n+k+2jk)
        - sortedは元のリストも残すので、毎回長さkの新しい配列ができる
        - さらにスライスするので、それも長さkの新しい配列ができる
        - init
            - n + k
        - add
            - j * (2k)

# STEP2 読みやすくする＆他の人の解法を見る
https://github.com/hayashi-ay/leetcode/pull/54/files?short_path=6e7e84a#diff-6e7e84a8c0c13aabe8bad58b32c7215762116a974e2143b20ee931bf24b51a2f

heapを使っている。heapなら、ソートと違って一つ追加したら全部並べ替えてるのではなく、木を作って木の下からfitするまで段数の回数だけ入れ替えてるだけだから、lognで済む。一番小さいものや一番大きいものが欲しいだけなら、heapの方が良さそう。
ちなみにheapqで最大のものを取り出したいときは、-1をかけた数字を格納してheapを作るとのこと。

>その他のストリーミング問題
- https://discord.com/channels/1084280443945353267/1235829049511903273/1241298902657794068
    - https://leetcode.com/problems/random-pick-with-weight/solutions/6670614/conquer-weighted-random-pick-with-binary-search-beats-100/
    - 整数からランダムに撮りたい時はrandom.randrange(start,stop,step)
    - random.randint(start, stop)でも同じ
    - random.choice(list)で、listからランダムに取ってくれる
    - prefix_sumを取って、二分探索すると効率的
    - https://leetcode.com/problems/find-median-from-data-stream/solutions/1330646/c-java-python-minheap-maxheap-solution-picture-explain-clean-concise/
    - 中央値を知りたいなら、真ん中の二つだけわかってれば良いから、min_heapとmax_heapで管理してそれらをパッと取れるようにしておく

- https://docs.python.org/3/library/bisect.html#bisect.insort_right
    - ソート順保ったまま配列に挿入できる


- top_k_valuesの方が名前わかりやすい
    - https://github.com/SanakoMeine/leetcode/pull/10#discussion_r1925029510

- pythonは1秒に100万ステップ処理できるので、ステップ数がわかれば時間もだいたいわかる
- https://github.com/ichika0615/arai60/pull/8#discussion_r1898337850


- アルゴリズム的には遅いけど、ネイティブコード(Cで実装されてる)なので速い、ということがある
- https://github.com/fuga-98/arai60/pull/9/files#r1966724064




# sorted(step1)の改善版
- until_k_thよりtop_k_valsの方が中身がわかりやすいと思ったので変更
- スライスではなくdelを使うことで、メモリ削減
    →kが小さいと時間計算量的には不利になる(k vs n-k) & 存在しない要素に対する空振りのdelとなるケースがあり混乱生むかもなので、スライスでよかったかも?
- appendを使用
- addの方はtop_k_valsが引数ではないので破壊的にsort
- スライスではなくpopで最後の要素を外す。
```py
class KthLargest:
class KthLargest:
    def __init__(self, k: int, nums: List[int]):
        self.top_k_vals = sorted(nums, reverse=True)
        del self.top_k_vals[k:]

    def add(self, val: int) -> int:
        self.top_k_vals.append(val)
        self.top_k_vals.sort(reverse=True)
        self.top_k_vals.pop()
        return self.top_k_vals[-1]
```
- 計算量
    - 時間: O(nlogn) + O(jklogk)
        - init: O(nlogn)
            - sortedでnlogn、delは後ろから取り除いているだけだからO(1)かと思ったが、削除対象の各要素に対して参照カウントの削除(Py_DECREF)が実施されるため、削除要素数n-kに比例してO(n-k)になるらしい
            - nlognの方が大きいのでnlogn
        - add: klogk
            - appendは1、sortはklogk、popは1
    - 空間: O(n) + O(k)
        - init: O(n)
            - 長さnの配列を作る(ソートでもメモリがいる)
        - add: O(k)
            - ソートするときにO(k)のメモリがいる
# heapの解法
```py
class KthLargest:
    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.min_heap = []
        for num in nums:
            heapq.heappush(self.min_heap, num)
            if len(self.min_heap) > k:
                heapq.heappop(self.min_heap)

    def add(self, val: int) -> int:
        heapq.heappush(self.min_heap, val)
        if len(self.min_heap) > self.k:
            heapq.heappop(self.min_heap)
        return self.min_heap[0]
```
- add の時、一見不要にも思えるif文の条件があるのは、k = nums.length + 1になるケースがあるから。


- 計算量
    - 時間: nlogk + jlogk
        - init: n * logk
        - add: j * logk
    - 空間:
        - k
        - ヒープはただのリスト

- heapの実装
    - https://github.com/python/cpython/blob/3.13/Lib/heapq.py#L207
    - https://docs.python.org/ja/3.13/library/heapq.html
# insortの利用
```py
class KthLargest:
    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.sort_list = sorted(nums)
        self.sort_list = self.sort_list[-k:]

    def add(self, val: int) -> int:
        insort(self.sort_list, val)
        return self.sort_list[-self.k]
```
- 計算量
    - 時間 
        - init: nlogn + k
        - add: k+j
    - 空間
        - init: n
        - add: k

# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class KthLargest:
    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.min_heap = []
        for num in nums:
            heapq.heappush(self.min_heap, num)
            if len(self.min_heap) > k:
                heapq.heappop(self.min_heap)

    def add(self, val: int) -> int:
        heapq.heappush(self.min_heap, val)
        if len(self.min_heap) > self.k:
            heapq.heappop(self.min_heap)
        return self.min_heap[0]
```
3:32, 2:35, 1:45

# STEP4 レビューFB反映