# 問題タイトル
問題リンク
[929. Unique Email Addresses](https://leetcode.com/problems/unique-email-addresses/)



# STEP 1 自力で解く。

```py
class Solution:
    def numUniqueEmails(self, emails: List[str]) -> int:
        base_emails = set()          
        def convert_to_base_email(email):
            if '@' not in email:
                return None
            local_domain = email.split("@")
            if len(local_domain) != 2:
                return None
            local, domain = local_domain
            if len(local) == 0 or len(domain) == 0:
                return None
            local =  local.replace(".", "")
            if "+" in local:
                local = local.split("+")[0]
            converted_email = local + "@" + domain
            return converted_email

        for email in emails:
            converted_email = convert_to_base_email(email)
            if converted_email is not None:
                base_emails.add(converted_email)

        return len(base_emails)

```

- 所要時間　17min
- コメント
    - [str.replace(old, new, count=-1)](https://docs.python.org/ja/3.13/library/stdtypes.html#str.replace)
        - countを指定すると、先頭から count で指定された数の部分文字列だけを置き換えてくれるとのこと。
    - base_emailsはbase_email_setの方がより明示的で良かったかも
    - "Besides lowercase letters, the email may contain one or more '.' or '+'"のチェックをするのを忘れていた
    - base_emailは名前検討の余地があるかもしれない。convert_to_base_emailという名前なのにNoneが返ってくることがあるのも少し気持ち悪いかもしれない.



# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/25/files
    - 1文字ずつ見ていっている。面白い。けれども人にお願いする時こうは頼まないだろう、とも思う。
    - 正規表現でやってるものも。
    - そもそもvalidなemailであるかどうかの確認はしてないみたい。step1ではどこまで求められてるか確認せずに作り込みすぎたかもしれない。
    - https://discord.com/channels/1084280443945353267/1200089668901937312/1210595780109271060
    - s += c で文字列を作っていくと、毎回sをコピーしてそこにc足してsに代入し直すので、コピーにO(n)が毎回かかる→結局O(n^2)に。リストでappendからのjoinにしましょう。
    - https://github.com/plushn/SWE-Arai60/pull/14#discussion_r2051724883
        - O(n^2)になるけどもメールアドレスは実は長さ上限が254文字と決まっているので、そこまで影響ない。

- https://github.com/seal-azarashi/leetcode/pull/14#discussion_r1676988400
    - ユースケースを意識して伝えてから書く
- https://discord.com/channels/1084280443945353267/1225849404037009609/1253042734252556391
    - メアドのルールはRFCで決まっている

- https://github.com/garunitule/coding_practice/pull/14/files#diff-9e8eb89016b73649f9496a29a7f267680a935051a58c436c52adf26b87cf4997R153
    - str.find("探したい文字列")で文字列の中から探したい文字列のindexが取得できる。なかったら-1を返す。
    - https://docs.python.org/ja/3.13/library/stdtypes.html#str.find
    - ちなみに配列に対して似たようなことしたいときの関数はindex()。なぜ別名なのか。そしてこちらはなかったときはValueError
        - 実はstrの方にもindex()はあった。
        - https://docs.python.org/ja/3/library/stdtypes.html#str.index
            - rindex()という最大インデックス返すものもある。
            

- https://github.com/potrue/leetcode/pull/14/files#diff-6a8efe56493bdbfeb045b7aa123660406ebdd5dd4f0a011935562d4eefb9ab46R36
    - Literalは知らなかった。
    - Enumみたいなことができる。
    - https://peps.python.org/pep-0586/


- https://docs.python.org/ja/3/library/stdtypes.html#str.partition
    - str.partition(sep) というものもある
    - こちらはタプルで、('local', '@', 'domain')みたいに返す。最初に出てきた区切り文字で区切る

```py
class Solution:
    def numUniqueEmails(self, emails: List[str]) -> int:
        canonicalized_emails = set()
        valid_letters = set(string.ascii_lowercase + "@+.")        
        def get_canonicalized_version_if_valid(email):
            for c in email:
                if c not in valid_letters:
                    return None
            if '@' not in email:
                return None
            local_domain = email.split("@")
            if len(local_domain) != 2:
                return None
            local, domain = local_domain
            if len(local) == 0 or len(domain) == 0:
                return None
            local =  local.replace(".", "")
            if "+" in local:
                local = local.split("+")[0]
            return local + "@" + domain

        for email in emails:
            canonicalized_email = get_canonicalized_version_if_valid(email)
            if canonicalized_email is not None:
                canonicalized_emails.add(canonicalized_email)

        return len(canonicalized_emails)
```
- ユースケースの想定
    - 同じ人が(.や+を使って)複数登録しているパターンがあることを検知しているので、そららの影響を除いたユニークなメールアドレス数がどれくらいあるのか知りたい。
    - 無効なメールアドレスも混ぜっているので、有効なアドレスのみ数えたい。
    - "有効"の定義は問題文にある通りとする。




# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def numUniqueEmails(self, emails: List[str]) -> int:
        unique_emails = set()
        def canonicalize(email: str) -> str:
            local, domain = email.split("@")
            local = local.replace(".", "")
            local = local.split("+")[0]
            return local + "@" + domain

        for email in emails:
            unique_emails.add(canonicalize(email))

        return len(unique_emails)

```
- ユースケースの想定
    - 同じ人が(.や+を使って)複数登録しているパターンがあることを検知しているので、そららの影響を除いたユニークなメールアドレス数がどれくらいあるのか知りたい。
    - バリデーションチェックは、入力フォームの時点ですでにしているので、ここでは考えなくて良い。
5:07, 3:46, 2:40

計算量
- メールアドレスの個数をn,メールアドレスの長さをkとおく(ただしkは高々255)
- 時間:O(nk)
    - n回ループ
        - 1回あたり:O(k)
- 空間: O(nk)
    - localとdomainがO(k)
    - unique_emailsはO(nk)


# STEP4 レビューFB反映