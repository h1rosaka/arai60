# 問題タイトル
[78. Subsets](https://leetcode.com/problems/subsets/description/)



# STEP 1 自力で解く。
```py
"""
◆コード書く前の思考メモ
樹形図を作りながら、
stack([]:現状,[]残り)を管理。
樹形図の末端でresultにappend 

1分程で終わりそうか
    樹形図の枝の数は、末端の数2^nの2倍で、2^n+1
    毎回の処理は、コピーや要素一個出し入れなので、O(N)くらいのはず。(書いてみて具体が見えたらもう一度確認)
    O(N)なら、nums.length(N)<=10なので、2^10 * 10=1024 * 10 =10^4。1秒10^6ステップだとして十分終わる。
"""
class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        stack = [([], nums)]
        result = []
        while stack:
            partial_subset, rest = stack.pop()
            if not rest:
                result.append(partial_subset[:]) 
                continue
            # restの一番左の要素を採用したパターンと採用しないパターンをstackに入れる
            # いずれにせよrestからはその要素を除く 
            stack.append((partial_subset, rest[1:]))
            stack.append((partial_subset + [rest[0]], rest[1:]))

        return result
```
- 最初一つ前の問題と同じようにループでrestを舐めるようなコードを書いてしまってWAになった。
    - →頭の中でふんわりイメージしていることを、一度言葉に落として、計算量もクリアに見えるくらいになってからコード書く。

再帰関数使うバージョン
```py
class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        result = []
        def make_subsets(partial_subsets, rest) -> None:
            if not rest:
                result.append(partial_subsets[:])# copyは実は不要
                return
            make_subsets(partial_subsets, rest[1:])# 左部分木
            make_subsets(partial_subsets + [rest[0]], rest[1:])# 右部分木

        make_subsets([], nums)
        return result
```
- 最初`if not rest:`のブロックでreturn書くの忘れてTLEになった
    - ベースケースでは(何か返すような関数でなくても)return書く。

- そういえば`make_subsets(partial_subsets, rest[1:])`のところ、copyしないで参照保存しちゃってるな。なんでうまく動いているのだろう(前の問題はcopyを作っていた)
    - 樹形図で見ると、ひたすら左下に進み、行き止まりになると一つ戻って右へ、その右部分木が終わったら一段上へ戻る形となり、その時のpartial_subsetはちょうど都合よく欲しい状態だから、おかしくならない
    - 二つのmake_subsetsがそれぞれ部分木だと考えると、さらにわかりやすい。
- 実は`partial_subsets[:]`のcopyも不要。
    - 前の問題はresultに追加した後、追加したものを指しているラベル(partial_subsets)が同じものを指している状態のまま、そのラベルが指しているものに対してpopやappendをしていたので、resultに追加されたものも巻き添えを受ける形になっていた。
    - この問題は、resultに追加した後partial_subsetsが指すものに対して、popやappendなどそのもの自身を変えるような操作はしない


バックトラックでメモリを節約するバージョン
```py
class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        result = []
        def make_subsets(partial_subsets, rest) -> None:
            if not rest:
                result.append(partial_subsets.copy())
                return
            # xを採用しないパターン
            x = rest.pop(0)
            make_subsets(partial_subsets, rest)
            # xを採用するパターン
            partial_subsets.append(x)
            make_subsets(partial_subsets, rest)
            # 元に戻す
            rest.append(x)
            partial_subsets.pop()

        make_subsets([], nums)
        return result
```


# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/63/files#diff-ddd8c09ee41837c8d5bde978403f850a0b08217fb8ec8eac6d0f2ae10e369d04R7
    - partial_subsets, restと二つ管理せずとも、結局先頭から一つずつみてるだけだからindexの一変数で良いよね、という書き方

- https://github.com/hayashi-ay/leetcode/pull/63/files#diff-ddd8c09ee41837c8d5bde978403f850a0b08217fb8ec8eac6d0f2ae10e369d04R47
    - なるほど。左から一要素ずつ見ていって、今まで作ったpartial_subsetに今見てる要素を末尾につけてできる新しいものたちを追加。
    - extend()
        - https://github.com/hayashi-ay/leetcode/pull/63/files#diff-ddd8c09ee41837c8d5bde978403f850a0b08217fb8ec8eac6d0f2ae10e369d04R47
        - 配列と配列を合体。(appendだと配列のまま1要素として追加されてしまうので)

-  https://github.com/hayashi-ay/leetcode/pull/63/files#diff-ddd8c09ee41837c8d5bde978403f850a0b08217fb8ec8eac6d0f2ae10e369d04R59
    - 採用だったら1、不採用だったら0のbit列
    - 全部で2^nのパターンがあるから、そこを一つずつ見ていく。
    - このように使う/使わないを2進数で表すことをbitmaskというとのこと。


- https://github.com/fhiyo/leetcode/pull/51#discussion_r1690145563
    - tee(it, n)
        - >t1, it2 , ... itn 一つのイテレータを n 個に分ける
        - >tee('ABC', 2) → A B C, A B C
        - https://docs.python.org/ja/3/library/itertools.html#:~:text=it1%2C%20it2%20%2C%20...%20itn%20%E4%B8%80%E3%81%A4%E3%81%AE%E3%82%A4%E3%83%86%E3%83%AC%E3%83%BC%E3%82%BF%E3%82%92%20n%20%E5%80%8B%E3%81%AB%E5%88%86%E3%81%91%E3%82%8B

    - https://github.com/fhiyo/leetcode/pull/51#discussion_r1690145563
        - 難しい。戻ってきた時にまたリベンジ。


1変数バージョン
```py
class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        stack = [([],0)]
        result = []
        while stack:
            subset, index = stack.pop()
            if index == len(nums):
                result.append(subset)
                continue
            stack.append((subset, index + 1))
            stack.append((subset + [nums[index]], index + 1))
        return result
```
- 実はこの書き方も"バックトラッキング"に該当するらしい
    - 再帰から戻ってきた時にpopしたりして、帰りがけに元の状態に戻したりする方法のことをバックトラッキングと呼ぶのかと思っていたが、厳密には違うっぽい。


- 再帰とバックトラック再帰
    - https://web.stanford.edu/class/cs106b-8/lectures/backtracking-optimization/Lecture13.pdf を元にまとめたもの↓
    - 再帰: call stackをどんどん積んでって、最後のところで初期seedを把握、そこからcall stackを戻りながら、最後に一つの結果を返す
    - バックトラック再帰: "'[]'(空)"を初期seedとして再帰タスクを開始し、どんどんそこに追加しながら樹形図を広げていって、それぞれの末端で解候補が得られる


| 項目 | Basic recursion | Backtracking recursion |
|---|---|---|
| 目的 | 1つの解を作る | 複数の解を列挙する |
| タスクの性質 | 同じ処理を繰り返し、1つの答えを作る | 各ステップで分岐し、複数の可能性を探索する |
| 解の構築タイミング | コールスタックを戻る途中で構築される | 再帰の途中で部分解を拡張していく |
| 初期状態 | 最後のベースケースが解の初期値(seed)を決定する | 「空の解」を初期値(seed)にして、最初の再帰を開始する |
| base case の役割 | 解の初期値（seed）を定義する | 1つの完成した解候補を表す |
| 各再帰呼び出しの役割 | 解に少しずつ情報を追加する | 解候補を分岐させて増やす |
| 最終結果 | 最初の呼び出しが1つの最終解を返す | 各 base case で得られた解の集合 |
| 代表例 | 階乗、フィボナッチ | subsets、permutations、N-Queens |



- >The backtracking algorithm traverses this search tree recursively, from the root down, in depth-first order. At each node c, the algorithm checks whether c can be completed to a valid solution. If it cannot, the whole sub-tree rooted at c is skipped (pruned). Otherwise, the algorithm (1) checks whether c itself is a valid solution, and if so reports it to the user; and (2) recursively enumerates all sub-trees of c. The two tests and the children of each node are defined by user-given procedures.
    - https://en.wikipedia.org/wiki/Backtracking

- 再帰の時は「部下に何を返してもらうと自分が答えが出せるか」と考えていたが、この問題は再帰のはずなのに部下が何も返してくれなくて少し違和感を感じていた。再帰ではなくバックトラック再帰の問題だったからなのかも。



# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        stack = [([], 0)]
        result = []
        while stack:
            subset, index = stack.pop()
            if index == len(nums):
                result.append(subset)
                continue
            stack.append((subset, index + 1))
            stack.append((subset + [nums[index]], index + 1))

        return result
```





# STEP4 レビューFB反映