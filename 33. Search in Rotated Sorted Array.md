# 問題タイトル
[33. Search in Rotated Sorted Array](https://leetcode.com/problems/search-in-rotated-sorted-array/description/)



# STEP 1 自力で解く。
```py
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        target_index = None
        def bisect_find(nums, x):
            left_index = bisect_left(nums, x)
            if left_index == len(nums):
                return -1
            if nums[left_index] != x:
                return - 1
            return left_index
        # 整列済みの場合
        if nums[0] < nums[-1]:
            return bisect_find(nums, target)
        # 整列済みではない場合、二つの整列ずみに分ける。
        min_num_index = bisect_left(nums, True, key=lambda x: x <= nums[-1])
        ## 右側の場合
        if nums[min_num_index] <= target <= nums[-1]:
            result = bisect_find(nums[min_num_index:], target)
            if result != -1:
                result += min_num_index
            return result
        ## 左側の場合
        return bisect_find(nums[:min_num_index], target)
```
- 最初`if nums[left_index] != x:`の条件を忘れていてWAになった。
- 調べたが、bisect_findみたいな関数はなさそうで、自前で実装が必要そう



# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/49/files#diff-0691fd54173ad5183bf1c632f87cc4a7353908565057fa6b9facae8e8ec5ec4bR30-R61
    - bisect_findの引数を配列じゃなくてleft, rightにしているから、帰ってくる答えが元の配列のindexになって、後から変換しなくて済んでいる。無駄に部分配列をコピーして作らなくて済んでいるのも良い。


- https://github.com/hayashi-ay/leetcode/pull/49/files#diff-0691fd54173ad5183bf1c632f87cc4a7353908565057fa6b9facae8e8ec5ec4bR4-R26
    - 自分から見て右か左のどちらかは必ず昇順に並んでいるので、そこにtargetが入っているかは確認できる。ということは結局左右どちらにtargetがいるかわかるということなので、いた方を見るようにする。
    - 探しているものLとRの意味
        - 探しているもの: target
            - 前の問題はXX以上のうち一番左、などという形でTTTFFFと捉えていたが、この問題は少し違いそう
        - L: ここより左にはない(ここはあるかも)
        - R: ここより右にはない(ここはあるかも)
            - L <= 探しているものありうる範囲 <= R
    - 手順
        - 探しているものありうる範囲が残り0マスになるまでやる。
            - 残り1マスまでやって最終チェック、ではなく、ループの中でtargetと同一かを確認しているので、残り0になるまで。
            - 前の問題は、「今確認しているものが探しているものかどうか」を残り1つになるまでまで確定できなかったが、この問題はそれが途中でできる
                - TTTTTFFFFの境目を探してはいるが、最後残り一個になるのを待たずとも、今見ているのが探しているものであるかそうではないかを判断できる　＆　この方が途中で見つかった時そこで打ち切れるので効率的


Rの意味を少し変え、L　<= 探しているもののありうる範囲　< Rとなるように絞っていくバージョンで書いてみたが、TLE
```py
# ※注意: TLEになります
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        left = 0 # leftより左にはない。
        right = len(nums) #rightとそこより右にはない。
        while left < right: #残り要素が0になるまで
            mid = (left + right) // 2
            if nums[mid] == target:
                return mid
            # midが右端を含むグループにいる場合
            if nums[mid] <= nums[-1]:
                if nums[mid] < target <= nums[right - 1]:
                    left = mid + 1
                else:
                    right = mid + 1 # ここが本来right = midであるべきだった。
            # midが左端を含むグループにいる場合
            else:
                if nums[left] <= target < nums[mid]:
                    right = mid
                else:
                    left = mid + 1
        return -1
```
- なぜTLEになったか 
    - `nums[mid] < target <= nums[right - 1]`が満たされないケースなので、midにはあるかも知れない。
    - そこまでで考えると`right = mid + 1`としたくなるが、上で`nums[mid] == target`ではないこと確認済みなので、さらにもう一つ絞れて、`right = mid`。
- 無限ループの注意
    - midはLと同じか、増える　→ L = mid, L = mid - 1 で更新するとき無限ループ注意 
    - midはRと同じか、減る　　→ R = mid, L = mid + 1 で更新するとき無限ループ注意 
- right -1でindexを参照する形になってしまっていて、少し不便。(rightが指す場所にはないとして扱うものの、後で「mid~rightらへんにあるか」を確認するので)



- https://discord.com/channels/1084280443945353267/1233295449985650688/1239594872697262121
- https://github.com/Yoshiki-Iwasa/Arai60/pull/36/files#r1970972279
```py
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        def priority(x):
            return (x <= nums[-1], target <= x)
        i = bisect_left(nums, priority(target), key=priority)
        if nums[i] != target:
            return -1
        return i
```
- (True, Flase)のようなboolのタプル同士を比較してbisect_left
- 一つ目のboolが、右側のグループか否か。2つ目のboolがtarget以上か否か。
- 先にどちらのグループに入るのか判定されて、その後グループ内でtarget以上か否かで良いところに入る。
- bisect_left(nums, priority(target), key=priority)
    - numsにpriority(target)を入れたら何番目になるか。ただし順番決めはpriorityを使って変換したものを大小比較して。
    - priority(target): targetをpriorityのルールに沿って(True, False)に変換したもの。

- https://github.com/shintaro1993/arai60/pull/47#discussion_r2422480393
    - >target,left,rightの意味さえちゃんと決まっていればそれ以降の決め方の順序は重要ではないかもしれません。


# STEP3　　3回連続でエラーなしで解けるまで解く
```py
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        """
        探しもの： target
        L : ここより右には探し物はない
        R : ここより右には探し物はない
            L <= ありうる範囲　<= R
        """
        left = 0
        right = len(nums) - 1
        while left <= right:
            mid = (left + right) // 2
            if nums[mid] == target:
                return mid
            # 右側が整列している場合
            if nums[mid] <= nums[-1]:　
                if nums[mid] < target <= nums[right]:
                    left = mid + 1
                else:
                    right = mid - 1
            else:
                if nums[left] <= target < nums[mid]:
                    right = mid - 1
                else:
                    left = mid + 1
        return -1
```
`if nums[mid] <= nums[-1]:　`は`if nums[mid] <= nums[right]:　`でも良かった。

計算量
- 時間: O(logN)
    - ループ回数がO(logN)。1回あたりはO(1)
- 空間: O(1)

# STEP4 レビューFB反映


## *args, **kwargsを使ってラッパー関数を書く
>https://github.com/h1rosaka/arai60/pull/45/files#r2529497639

bisect_left(a, x, lo=0, hi=len(a), *, key=None)
これを「位置引数/名前付き引数」で分類すると：
位置引数（positional）
```
a
x
lo（名前付きにしてもよいが positional でも渡せる）
hi（同上）
```
キーワード専用引数（keyword-only）
```
key
```
### 下記のように書くと、lo, hi, keyも渡そうと思えば渡せる、という状態にできる。
```py
def bisect_find(nums, x, *args, **kwargs):
    left_index = bisect_left(nums, x, *args, **kwargs)
    ...
```
### 仕組み

argument
- https://docs.python.org/3/glossary.html#term-argument
- 実引数
    - キーワード引数(keyword argument)
        - 識別子がついた引数。
        - 渡し方
            - 関数呼び出しの際に引数の前に識別子がついたもの (例: name=) 
            - "**" に続けた辞書の中の値として渡された引数
            ```py
            complex(real=3, imag=5)
            complex(**{'real': 3, 'imag': 5})
            ```

    - 位置引数(positional argument)
        - キーワード引数以外の引数
        - 渡し方
            - 引数リストの先頭に書く
            -  "*" に続けた iterable の要素として渡す
        ```py
        complex(3, 5)
        complex(*(3, 5))
        ```

- そもそも"実"引数とは、
    - "実"引数: argument
    - "仮"引数: parameter
        - https://docs.python.org/ja/3.14/glossary.html#term-parameter
    - 違い
        - https://docs.python.org/ja/3.14/faq/programming.html#what-is-the-difference-between-arguments-and-parameters
        - ```py
            #仮引数 (parameter) は関数定義に表れる名前で定義されるのに対し、 実引数 (argument) は関数を呼び出すときに実際に渡す値のことです。仮引数は関数が受け取ることの出来る 実引数の型 を定義します。例えば、以下のような関数定義があったとして:
            def func(foo, bar=None, **kwargs):
                pass
            #foo、bar、kwargs は func の仮引数です。一方、func を呼び出すときには、例えば:

            func(42, bar=314, extra=somevar)
            #42、314、somevar という値は実引数です。

            ```
- *args / **kwargs はparameter(仮引数)の一種
    - 可変長位置: (他の仮引数で既に受けられた任意の位置引数に加えて) 任意の個数の位置引数が与えられることを指定します。このような仮引数は、以下の args のように仮引数名の前に * をつけることで定義できます:
    
    - 可変長キーワード: (他の仮引数で既に受けられた任意のキーワード引数に加えて) 任意の個数のキーワード引数が与えられることを指定します。このような仮引数は、上の例の kwargs のように仮引数名の前に ** をつけることで定義できます。

    - ```py 
      def func(*args, **kwargs): ...
      ```
    - https://docs.python.org/ja/3.14/glossary.html#term-parameter



- >実引数は関数の実体において名前付きのローカル変数に割り当てられます。割り当てを行う規則については 呼び出し (call) を参照してください。


call
- https://docs.python.org/ja/3.14/reference/expressions.html#calls
    - argumentがどのように関数の中に渡されていくのか
        - 1. parameterの一つ一つに対し、値の入っていないスロットを生成
        - 2. まず、位置引数について、N 個の位置引数がある場合、それらを先頭の N スロットに配置
        - 3. 続いて、キーワード引数について、その名前(識別子)を見て、対応するスロットへの配置を試みる
            - すでにそのスロットが埋まっていたらTypeError
    - 例
        ```py
        def f(a, b, c):
            pass
        f(1, c=3, b=2)
        ```
        - 1. パラメータa,b,cに対し、スロット0,1,2が生成される
        - 2. まず位置引数から順に入れる
            - 1 → a のスロット0に入る
        - 3. キーワード引数を入れる
            - c=3 → スロット2に入れる
            - b=2 → スロット1に入れる
                - cやbのスロットは埋まっていなかったので、TypeErrorにはならない

bisect_findの定義の中での"*args"。
- >仮引数 *identifier がある場合、この仮引数は余分な位置引数が入ったタプル (もしくは、余分な位置引数がない場合には空のタプル) を受け取ります。
- >関数呼び出しに *expression という構文が現れる場合は、 expression の評価結果は イテラブル でなければなりません。 そのイテラブルの要素は、追加の位置引数であるかのように扱われます。 f(x1, x2, *y, x3, x4) という呼び出しにおいて、 y の評価結果がシーケンス y1, ..., yM だった場合は、この呼び出しは M+4 個の位置引数 x1, x2, y1, ..., yM, x3, x4 での呼び出しと同じになります。

bisect_findの定義の中での"*kwargs"。
- >仮引数 **identifier がある場合、この仮引数は余分なキーワード引数が入った (キーワードをキーとし、引数値をキーに対応する値とした) 辞書を受け取ります。
- 関数呼び出しで **expression 構文が使われた場合、 expression の評価結果は マッピング でなければなりません。その内容は追加のキーワード引数として扱われます。

```py
def bisect_find(nums, x, *args, **kwargs):
    left_index = bisect_left(nums, x, *args, **kwargs)
    ...

bisect_find([1, 3, 5, 7], 5, 1)
# nums=[1, 3, 5, 7], x = 5, 1は余分な位置引数となる。
# *identifierである*argsがあるので、argsは余分な位置引数が入ったタプル(1)を受け取る
# `bisect_left(nums, x, *args, **kwargs)`による呼び出し時に、*argsとしてargsの中身が展開される。
```

※args/ kwargsという特別な引数がある、というわけではなく、慣習としてその名前を使っているに過ぎない


## docto-rinさんに共有いただいた解法を理解するためのメモ
```py
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        """
        不変条件：
        - leftより左はrotationエリアが違うか、targetより小さい
        - rightより右はrotationエリアが違うか、target以上
        区間 left <= x <= right のサイズが0になるまで探索する。 

        →私がいつも使っている解釈(LとRを使って探しているものが"存在している範囲"を管理する)に翻訳しようとすると、どうしてもうまくいかない。


        """
        left = 0
        right = len(nums) - 1
        while left <= right:
            mid = (left + right) // 2

            # まずは、midとtargetが同じグループにいるのか、右端と比較してチェックする

            ## 違うグループにいる場合の処理
            ### midは左グループ & targetは右グループにいる
            if nums[mid] > nums[-1] and target <= nums[-1]:
                left = mid + 1
                continue
            
            ### midは右グループ　& targetは左グループにいる
            if nums[mid] <= nums[-1] and target > nums[-1]:
                right = mid - 1
                continue
            
            ## 同じ側のグループにいる場合の処理
            if nums[mid] < target:
                left = mid + 1
            else:
                # target <= nums[mid] 
                right = mid - 1

        # right + 1 = left の状況。
        # 不変条件より、targetと等しくなりうるのはnums[left]のみ。
        if nums[left] == target:
            return left
        return -1

```







---------------------------------------------------------
# nums[mid]== targetの判定をなくしたバージョンを書いてみる

```py
#これだと無限ループになるので、何かを変えないといけない
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        """
        探しもの： target
        L : ここより右には探し物はない
        R : ここより右には探し物はない
            L <= ありうる範囲　<= R
        """
        left = 0
        right = len(nums) - 1
        while left < right:
            mid = (left + right) // 2
            # midとそこより右側が整列している場合
            if nums[mid] <= nums[-1]:
                if nums[mid] <= target <= nums[right]:
                    left = mid　# ここが無限ループ
                else:
                    right = mid - 1
            # midとそこより左側が整列している場合
            else:
                if nums[left] <= target <= nums[mid]:
                    right = mid
                else:
                    left = mid + 1
    
        if nums[left] == target:
            return left

        return -1
```



```py
# L, Rはそのまま、条件式を変える
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        """
        探しもの： target
        L : ここより右には探し物はない
        R : ここより右には探し物はない
            L <= ありうる範囲　<= R
        """
        left = 0
        right = len(nums) - 1
        while left < right:
            mid = (left + right) // 2
            # midとそこより右側が整列している場合
            if nums[mid] <= nums[-1]:
                if nums[mid] < target <= nums[right]:　# 条件を変えた
                    left = mid + 1 # ここが無限ループだった。
                else:
                    right = mid
            # midとそこより左側が整列している場合
            else:
                if nums[left] <= target <= nums[mid]:
                    right = mid
                else:
                    left = mid + 1
    
        if nums[left] == target:
            return left

        return -1
```

```py
# L, Rの意味を変えた
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        """
        探しもの： target
        L : ここより右には探し物はない
        R : ここと、ここより右には探し物はない
            L <= ありうる範囲　< R
        """
        left = 0
        right = len(nums)
        while right - left > 1:
            mid = (left + right) // 2
            # midとそこより右側が整列している場合
            if nums[mid] <= nums[-1]:
                if nums[mid] <= target <= nums[right - 1]:
                    left = mid
                else:
                    right = mid
            # midとそこより左側が整列している場合
            else:
                if nums[left] <= target < nums[mid]: # ここの条件を変えた
                    right = mid
                else:
                    left = mid
    
        if nums[left] == target:
            return left

        return -1
```