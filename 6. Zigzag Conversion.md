# 問題タイトル
https://leetcode.com/problems/zigzag-conversion/description/



# STEP 1 自力で解く。
```py
class Solution:
    def convert(self, s: str, numRows: int) -> str:
        if numRows == 1: # 1だった時のみ、else句に入って0個目のrow(使わない予定のもの)を見てしまう
            return s

        rows = [[] for _ in range(numRows + 1)]
        row_num = 1
        direction = 1
        for c in s:
            rows[row_num].append(c)
            if 1 <= row_num + direction <= numRows:
                row_num += direction
            else:
                direction *= -1
                row_num += direction
        
        result = ""
        for row in rows:
            result += "".join(row)
        
        return result
```
- 最初numRows == 1のケースが考慮もれていて、WAになった。

- `[[] * numRows]`はダメだった記憶がうっすらあるが、ちゃんと説明できないので調べる
    - まず、こう書くと何が作られるか
        - `[[]]`ができる
    - 仕組み
        - シーケンス型(list, tuple, range, str)に対する操作の説明
            - https://docs.python.org/3/library/stdtypes.html#common-sequence-operations
        - `s * n`つまり、sequence * n の定義は下記
            - >equivalent to adding s to itself n times
        - そして adding の方は
            - >the concatenation of s and t
                - concatenation: 連結
        - まとめ: 「掛け算は足し算をn回やるのと同じで、足し算は連結操作になるよ」
    - つまり、`[[] * numRows]`は`[]`という空の配列をnumRows回連結するので、結局空配列になり、上記の結果
        
- うっすら覚えていたもの(grid系の問題を解いていた時に出てきた、やってはいけないこと)はこれ↓
    - OK : [[0] * n for _ in range(m)]
    - NG : [[0] * n] * m
    - [[0] * 3] * 2の例
        - [0] * n は、3回の足し算、つまり連結になって[0, 0, 0]になる。これをAとおくと結果、[A] * 2になる。
        - [A] * 2なので、[A, A]になって、Aを戻すと [[0, 0, 0], [0, 0, 0]]
        - うまく初期化できたように見えるが、実態としては[A, A]なので、同じ配列が2個になってしまっている
            - そのため、左側の配列の先頭だけ1に入れ替えたつもりが、右の次配列の先頭も変わってしまう。
        - では最初の [0] * 3 はなぜやって良いのか
            - ０ を B とおけば [B] * 3で、[B, B, B]と同じものを指すことになるが、Bは(Aと違って)イミュータブルなので、Bの先頭1つだけをちょっと変えよう、とかが発生しない。（Bは数字なので、先頭一つ、とかそもそもない）
                - よって、一番左のBだけ変えようとすると、それはすなわち一番左はBではなく何か別のものに置き換えるということになるので、中央や右のが指しているBに影響しない

    - 実は公式ドキュメントにもちゃんと解説があった
        - 注意事項2
            >Note that items in the sequence s are not copied; they are referenced multiple times.
            - https://docs.python.org/3/library/stdtypes.html#typesseq-list:~:text=Note%20that%20items%20in%20the%20sequence%20s%20are%20not%20copied%3B%20they%20are%20referenced%20multiple%20times.

        - FAQページなんてのもあって、そこにも載っている。
            - >The reason is that replicating a list with * doesn’t create copies, it only creates references to the existing objects. The *3 creates a list containing 3 references to the same list of length two. Changes to one row will show in all rows, which is almost certainly not what you want.
            - https://docs.python.org/3/faq/programming.html#faq-multidimensional-list
    
    - 今回のケースだと、numRows分だけ配列を用意していると見せかけて、最初に作った一つの配列への参照をnumRows個用意しているだけになって、実質一つしか用意できてない。

    



# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/71/changes#diff-ccfa5b3e70552f7a1aea1f6a719cd00d74a1035c89cfab6426fddc4696ed3e21R80
    - 下に行けるだけいって、一番上の行になるまで戻る、という書き方

- https://github.com/hayashi-ay/leetcode/pull/71#discussion_r1553816218
    - >文字列の追記は再構築されます
    - FAQに、文字列の連結(concat)は毎度新しいものを作るので非効率(O(N^2))です。"".joinで繋げましょう。と書いてある。
        - https://docs.python.org/3/faq/programming.html#what-is-the-most-efficient-way-to-concatenate-many-strings-together
    - でもなぜ毎度新しいものを作るとO(N^2)になるのか、また、joinだとどうして効率的になるのかわからない。


    - なぜ毎度新しいものを作るとO(N^2)になるのか
        - そもそもstrとは何なのか
            - [この記事](https://yoichi22.hatenablog.com/entry/2021/07/02/065336)に、関連しそうなcpythonのコード部分のリンクがあった。
                - どうも、文字列は配列で管理されていて、配列の1マスのサイズは、どんな文字がその文字列に入っているかによって変わるらしい。
            - [上の記事にあったCpythonのリンク](https://github.com/python/cpython/blob/0d7f61ddb074659d8c18c8f5ac86a6a18e41f9e5/Objects/unicodeobject.c#L1447)を選択した上でgithub上のcopilotに質問してみた結果を下に書く。
                - ここは Unicode オブジェクト実装の一部で、メモリ割り当てを行っている

                - 「メタデータの入った構造体」と、「実際の文字列データ」をまとめて連続したメモリ領域に置くために、その分の領域を確保している
                    ```
                    [構造体][文字列データ]
                    metadata   'h' 'e' 'l' 'l' 'o'
                    ```
                    イメージ↓
                    ```
                    メモリアドレス：  0x1000  0x1001  0x1002  0x1003  0x1004  0x1005  ...
                    メモリ内容：     [構造体] [構造体] [文字 ]  [文字 ]  [文字 ]  [文字 ]  ...
                    　　　　　　　　　←─────────────────────────────────────────────→
                            　　　　　　　　1つのメモリブロック
                    ```
                    -  メモリのアドレスが振られている1区画は1バイト(そのようにアドレス振っているものが1964年以降主流)
                        - バイトマシンと呼ばれる: https://ja.wikipedia.org/wiki/%E3%83%90%E3%82%A4%E3%83%88%E3%83%9E%E3%82%B7%E3%83%B3
                        - 1964年に出たIBMのSystem/360が採用してデファクトスタンダードに
                    

                    - ちなみに、メタデータ構造体にはこんなものが入っている　TODO: 確認
                        ```
                        PyUnicodeObject {
                            length: 5          // 文字数
                            hash: 12345        // ハッシュ値
                            utf8: "hello"      // UTF-8表現
                            state: {...}       // その他の情報
                        }

                        ```
                - つまり、strは、連続したメモリに1文字ずつ格納されていて、かつ先頭にメタデータの構造体が追加で格納されている
                    
                - コードの意味
                    - `obj = (PyObject *) PyObject_Malloc(struct_size + (size + 1) * char_size);`
                        - struct_size: 構造体用のメモリサイズ
                        - size: 文字数（例：5）
                        - + 1: ヌル終端用
                        - char_size: 1文字のバイト数
                    - malloc
                        - memory allocationの略
                            - https://en.cppreference.com/w/c/memory/malloc
        

        - strの構造(上記)を理解した上で、追記するとどうなるのか考える
            - A→B→...となっていく
                ```
                    - A: [構造体][文字]
                    - B: [構造体][文字][文字]
                ```
            - A→B
                - 構造体+2文字分の空きスペースをどこかに確保して、あたらしい文字列オブジェクトに。
                - metadataを設定(length:2文字)
                - Aにあった1文字の情報を、新しく確保したスペースの先頭1文字目のマスにコピー
                - 末尾に新しい文字を足す

            - メモリの1区画に文字が入っているって具体的にどういうことなのか
                - `s = "A"`
                - 文字 A は Unicode では`U+0041`
                - `U+0041`はUnicodeの ID = 0041(ただし16進数)番目の文字ですよ、ということ。
                    - 16進数の数字を書くときは先頭に0xをつける慣習があるとのこと。今回だと　0x41
                -  0x41は10進数だと、65。8bitの2進数だと、01000001。これが該当区画に保存されている。
                - ちなみに日本語とかは2byteないと表せない大きな番号がUTF8で振られているので、pythonは文字列に日本語が混ざるときは、1文字あたり2区画を割り振って配列を作る。(絵文字とかが入ると4バイトになる。)
                    - https://peps.python.org/pep-0393/


    - joinだとどうして効率的になるのか
        - `"a" + "b" + "c"`が遅い理由
            - a + bのために2マス分メモリ確保。2文字コピー
            - a + b + cのために3マス分メモリ確保。3文字コピー
            - 合計5文字コピーした。が、欲しかったのは3文字だけ。
            - n文字欲しいとき、n + n-1 + n-2 + ... + 2だけコピーが必要。→O(n^2)
        - joinの動き(速い理由)
            - まず全部の長さを計算(O(N))
            - 一回だけメモリ確保(O(N))
            - 順番にコピー(O(N))


    - https://wiki.python.org/moin/PythonSpeed/PerformanceTips#String_Concatenation:~:text=In%20the%20minus%20column%2C%20you%20can%27t%20say%20something%20like%2C%20%22change%20all%20the%20%27a%27s%20to%20%27b%27s%22%20in%20any%20given%20string.%20Instead%2C%20you%20have%20to%20create%20a%20new%20string%20with%20the%20desired%20properties.%20This%20continual%20copying%20can%20lead%20to%20significant%20inefficiencies%20in%20Python%20programs.
    - https://stackoverflow.com/questions/19224059/how-strings-are-stored-in-python-memory-model
    - https://rushter.com/blog/python-strings-and-memory/
    - https://qiita.com/wangqijiangjun/items/5f6d78fecd453a3ec02f




- 余談:バイトとは, 8bitな理由

    - サマリ
        - バイトとは、文字をエンコードするのに使うbitの数(グループ)。さまざまな場所で、この単位で情報を扱う。
        - 最初は大文字と数字記号を表すのに十分な6bitごとに(メモリアドレスふるなどして)情報を扱っていた。(1byte=6bit。ただしこの時まだbyteという単語はない)
        - byteという単語を作ったプロジェクトで、8bitでやると便利だなと気づいたので採用した。
            - 2^nだと好都合
                - メモリアドレスが2進数で振られているので、「xバイト目のアドレス」をbitシフトの裏技で高速計算できる。
            - 4bitじゃ足りないし、16bitは余る。(8bitあれば256種類の文字表せるので大体のケースで十分)
                - byteごとにメモリから取ったり入れたりするので、当時貴重だったメモリに、余りは出したくない。
                - 小文字も追加しても、8bitなら表せる。
            - 0~9は4bitで表せるので、数字を扱うときは8bit単位でやればちょうど2文字扱えて効率的(6bitで2bit余らすよりも。)
            - etc ...
        

    - そもそもバイトとは
        - >バイト (英: byte) は、「複数ビット」を意味する、データ量あるいは情報量の単位である。
            - >1980年頃から1バイトは8ビット (bit) であることが一般的であり、 正式に定義されたのは2008年
            - >本来バイトとは、8ビット固定ではなく、扱う文字種や、あるいはワードサイズをいくつかに分割することによって決められる、1文字を表現する単位で、古くは直接的に「字」とか「キャラクタ」とも呼ばれていた単位に由来するもの
            - >歴史的には5ビットから12ビットまで存在したと言われる
            - https://ja.wikipedia.org/wiki/%E3%83%90%E3%82%A4%E3%83%88_(%E6%83%85%E5%A0%B1)
            - 要は、1文字を表すのに必要なbitの数、あるいはそのまとまり自体の事で、具体的に何個bit使って文字を表すかはいろんなやり方があったので、1byteが何bitか明確に決まってなかった。

        - ITmediaの記事の「なぜ1byte=8bit」なのか
            - Strechというスパコンプロジェクトで、byteという言葉を作り、さらに1byte=8bitだと便利なことに気づいて初めて採用した(が、プロジェクトとしては失敗)(1956)
            - System/360もその方式を採用して、成功したので後続のほとんどのコンピュータが継承した。(1964)
            - https://www.itmedia.co.jp/news/articles/2202/03/news151.html
            - https://archive.org/details/byte-magazine-1977-02/page/n145/mode/2up
    
    
    
    - 最初にbyteを名付けた時
        - >Byte denotes a group of bits used to encode a character, or the number of bits transmitted in parallel to and from input-output units. A term other than character is used here because a given character may be represented in different applications by more than one code, and different codes may use different numbers of bits (ie, different byte sizes). In input-output transmission the grouping of bits may be completely arbitrary and have no relation to actual characters. (The term is coined from bite, but respelled to avoid accidental mutation to bit.,)
            - https://archive.org/details/byte-magazine-1977-02/page/n145/mode/2up
            - Strech プロジェクト(1956年)　IBMのスパコン
        - 下記2つのいずれか、あるいはどちらもである、と言っている
            - 文字をエンコードするのに使われるbitのグループ
            - 並列にI/Oに通信されるbitの数
        - Stretchの設計思想における「マシン・データ階層（Machine data hierarchy）」では、ビット、バイト、ワードは以下のような順序で定義されています
            ```
            ビット（Bit）: 最小単位。
            バイト（Byte）: 文字やI/O転送の単位。
            ワード（Word）: メモリとの並列転送単位（64ビット）。
            ブロック（Block）: I/O命令1回で転送されるワードの集まり。
            ```

    - Strechで8bitで1文字をエンコードすることにした背景
        - 当時の多くのコンピュータ（IBM 702や1401など）は、「キャラクター（文字）」を基本単位として設計されていた。
        - メモリからデータを取り出す際、常に6ビット（1文字分）を一つの塊として読み書きする。アドレスも「1文字（6ビット）ごと」に振られていた。
            - 6bit(=64種類)あれば、当時使っていた大文字、数字、記号に十分
        - しかし、0~9を表すには4bitで十分なので、数字を読み書きするとき毎回6bit中2bitを無駄遣いで、勿体無い。
        - 当時メモリは貴重品なので、情報を扱う時のまとめてグループbitの数(後のbyte)を柔軟に変更できるコンピューター(Stretch)を作ろうとした
        - Stretchではbyteとして1~6bitを柔軟に設定できるようにした。
            - 「メモリ上に並んだビットの列を、ハードウェアがどのような『区切り（バイト）』で解釈して演算するか」を設定できたということ。
            - これにより、上の例での無駄な空の2bitが発生せず、すべてのメモリを有効活用できる。
            - そしてこれは、bit単位でメモリにアドレスするという事でもある(業界初。今まではキャラクターやワード単位でアドレスしていた)
        - 設計中に8bitも使えるようにすると便利だと気づき、新たに取り入れた。
            - 便利な理由:
                - 1.256文字表せるので、大体のアプリで十分な数であると思われる。(今まで使ってなかった小文字やその他諸々を足しても十分な数)
                - 2.1文字は1バイトで表されるので、（内部に記憶する）レコード長がその文字数として扱える（文字種によってレコード長が変えるような方法だとこうはならない)
                    - 文字数がわかればメモリ占有する範囲の大きさも確定して計算に便利、ということ
                - 3.経済的(8は無駄に大きすぎない。必要十分な文字を表せる中で最小(に近い))
                - 4.10進数の1桁は4bitで表現でき、その4bit byteを2個、8bit byteにまとめて扱える(高速化と記憶効率の向上)
                - 5.8-bitのbyteサイズは2の累乗なので、「バイナリアドレッシング」と「ビットレベルへのインデックス付け」のと組み合わさることで、パワフルな特徴を使えて嬉しい。
                    - 「バイナリアドレッシング」
                        - メモリにバイナリ(2新数)でアドレスの数字が振られている
                    - 「ビットレベルへのインデックス付け」
                        - Strechが初めて採用した、キャラクタ単位でなくビットレベルでアドレス振ってアクセスする方式
                    - 上記2つ&8-bit byteを採用する事で使えるパワフルな特徴
                        - 1.アドレスの計算時に、掛け算しなくて済む
                            - Stretchのアドレス（通し番号）は24桁の二進数
                                - 左側の18桁：どの「ワード」かを指定
                                - 右側の6桁: 1つのワード（64ビット）の中の「何番目のビットか」を指定（2^6=64通り）
                            - 掛け算じゃなくてシフトでいい例
                                - 5バイト目のアドレスにアクセスしたい。
                                    - 普通に考える場合: 
                                        - 1バイトは8bitなので、5*8=40bit目、つまり、0101000を見ればOK
                                    - シフトによる裏技:
                                        - 5は0101。これを3bit左にずらすと0101000であり、これは40なのでここ見ればOK。
                                        - x8することは3bit左にずらすのと同じなので、これが使える。(掛け算しなくて良い)
                                - bitごとにアクセスするようにしよう、という方針だったのに、5バイト目にアクセスしたいことなんてあるのか
                                    - 特殊なケースに、でもbitごとにアクセスできる機能があることで対応できるようにハードを作っておく、という話で、一般のユースケースである文字の扱いはバイトごとなので、普通によく発生する。
                        - 2.ワードの境界を一切意識することなく、メモリ全体を一つの巨大で連続したビットの列として扱うことができる
                            - ワードが64bitなので、1byte(=8bit)ずつ足していくと、綺麗に1ワードが満杯になり、アドレス計算が楽。
                                - 逆に1byteが7bitとか64の約数でない数だと、中途半端にワードに跨るケースが発生し、アドレスするための計算/回路が複雑になる
                                    - 先頭2bitはこちらのワードにあるけれど、残りは次のワードに、と複雑なifを挟む必要が出てくる
                            - 補足: なぜワードは64bitに決まっていたのか
                                - ビット単位でアドレスを計算するので、1ワードのビット数は「2の累乗（2^n）」でないと、アドレス計算回路が複雑に。
                                - 高精度な科学計算に必要な「浮動小数点数」を表現するには、32ビットでは精度が不足。
                                - 128ビットでは高価すぎる(回路が大きくなり、コストに見合わない)
        - メモリを読み書きする時の区切りを柔軟に1~6,8bitから選べはするものの、「このコンピュータで文字を何bitでエンコードするか」は別途決める必要があり、上記の理由から8bitが便利なので、それを採用した。
    

    - System/360
        - こちらもIBMのメインフレーム。大成功した。作者はフレデリック・ブルックスさん。(「人月の神話」の著者)
        - Strechは可変長で1byteを定義したせいで、回路が複雑になって大変だったので、今度はそれはやめたかった。
            - 設計コンペして6bitか8bitが良さそうであった。
            - 商業的にはこっちが良さそうだったので、8bitを採用。 
                - 商業的に良い、とは
                    - 1. 小文字も活用できる
                        - 当時文字列は大文字のみだったので、26+記号+数字で50数文字くらいであった。ここに小文字26を足すと、6bitの64種類をoverする。
                    - 2. 丸め誤差対応版の「4bitで1桁を扱うBCD（Binary-Coded Decimal）」と相性が良い(for金融系)
                        - 2進数では0.1は正確に表せないが、金融系では正確に計算が必要
                        - 0.1 + 0.2 とかを計算するとピッタリ0.3にならない。
                            - 0.1 + 0.2 = 0.30000000000000004
                        - BCDでは、10進数のまま扱う。
                            - 10進の1桁を4bitで表す。(0~9の各数字に、01のbit列を割り当てる)
                            - 各桁ごとに10進数のまま足し算する
                                - 普通の加算回路ではなくそれ専用の回路を用意しておく
        - bit単位のAddressingをやめ、8bitずつアドレスを振った(Strechがだけがやっていた変なことをやめて、キャラクターでアドレス振るのに戻した)
        - System/360の1byte=8bit採用がとてもうまくいっていたので、後のコンピュータはほぼすべてこれを踏襲している。


- 浮動小数点数の表現(32ビットでは精度が不足、とは):
    - 固定小数点と、浮動小数点
        - どちらも、コンピュータ世界における小数の扱い方のひとつ
        - 固定小数点法
            - 小数点の位置は左からX番目で固定なので、数字の並びだけ覚えて、という形で記憶させる。
            - 12345だけ覚えさせておいて、扱うときにこちらが勝手に左から4番目に点を打ち、123.45とする
        
        - 科学には大きい数字や、小さい数字がある。　アボガドロ定数 6.02*10^23,  プランク定数: 6.626 × 10−34
        - 仮に1マスに1つの数字を記憶できるマスのようなものを使って、これらを固定小数点数で表現しようとすれば、「ひとつの数を記憶するためのスペース」として、
        正の部分マス24マス、負の部分38マスの合計62マスを確保する必要がある。
        - 固定小数で管理すると
            - 小数点の位置: 左から23番目
            - アボガドロ定数:  6020...0
            - プランク定数:    0...06626
        - しかし、有効数字は4桁ほどなので、不経済。そこで，もう少しコンパクトな表現が考案された。これが浮動小数点法。
               
        - 浮動小数点法
            - 6.02　* 10^23 を、`[6.02, 23]`という２つの数のセットとして捉え、記憶する
            - それぞれ、`[仮数部, 指数部]`と呼ぶ
            - イメージ: 「602|23」という形で、左の3マスを使って仮数部を、右のマスを使って指数部を記憶 → 5マスで済む。(実際はもう少し複雑)
        - https://wa3.i-3-i.info/word14958.html
        - https://www.cis.twcu.ac.jp/~asakawa/comp2d-2008/handout4.pdf
        
    - 実際には10進数ではなく、2進数で記憶している
        - 0.625という小数は1/10が6個、1/100が2個、1/1000が6個、という意味
        - 2進数でも同様に表せる
            -  0.625 = 1/2 + 0/4 + 1/8 
            -  1/2が1個、1/4が0個、1/8が1個
            -  → 0.101
        - 0.625は偶然1/2^nの合計で綺麗に表せたが、表せないものは近いもので近似して記憶される
        - https://docs.python.org/ja/3/tutorial/floatingpoint.html




    - 浮動小数点の詳細
        -  32bit floatの場合
            ```
                | sign | exponent | fraction |
                | 1bit |  8bit    |  23bit   |

                sign → 符号
                exponent → 何倍するか(何桁ずらすかの元になる)
                fraction → 数値本体
            ```
            ```
            　例) 10進数の5の場合
                    2進数だと、101
                    これを小数で書くなら「1.01 を2桁ずらす」という書き方になる
                    1.01 × 2^2
                    0.01は2^-2 = 1/4 = 0.25 が1つ、という意味
                    (1+0.25)*4=5となり、うまく辻褄が合う。

                ・sign = 0
                ・exponent = 10000001　　
                    → これは10進数で129(ただし、ここからbiasと呼ばれる127を引いて、実質2として使う。つまり、2桁ずらす。)
                ・fraction = 01000000000000000000000
                (1 + fraction) × 2^(exponent-bias)
                1.01 × 2^2
            ```


            - biasを使う理由
                - マイナスの指数も使いたかったが、符号付き整数を管理するのは面倒なためbiasというオフセットを利用する。
                - 指数は負の数も必要
                    ```
                    1.01 × 2^2  = 5
                    1.01 × 2^-2 = 0.25 + 0.0625 = 0.3125
                    ```
                - しかし符号付きの整数を扱うのは、符号の判定など特殊なことをする必要があり面倒
                - bias = 127にした理由
                    - 今、指数部分(exponent)は8bitなので、0~255の256通り表せる。
                    - 正負バランスよく半分にするには、-127~128にすると良さそう
                    - 最小値は-127なので、biasを127にすれば、0~255の数字からbiasを引くことで、-127~128が表せる
                    - 補足:　128つまりexponent=255は、∞ や NaNなどの特殊なものとして扱われるとのこと。



        - 64bitでちょうど良い(32bitでは精度が足りない)理由
            - サマリ
                - 仮数が大きいほど、1~2の間を細かく分割してより多くの種類の数字の元を作れて、精度が上がる。
                - 指数が大きいほど、小数点の位置を自在に操れて、より小さい数やより大きい数を作れる。(表せる数の範囲が広がる)
                - 科学に必要な範囲を確保した上で、さらに必要な精度分だけbitを確保すると、64bitくらいがちょうど良い。

            - 全体 = 符号 + 指数（exponent, 範囲）+ 仮数（fraction, 精度）
                - 符号
                    - プラスかマイナスかを表すために1bitだけ使う
                - 仮数(fraction)
                    - コアというかカーネルというか実態というか、そういったもの。これを元に作っていく。
                - 指数(exponent)
                    - fractionとして持っているコアな情報を、指数で指定した分だけ桁をずらし、膨らましたり縮めたり。
                    - 表せる数字の範囲に影響する。
                        - 仮にここ(exponent)に使える情報の量が0bitだと、1.xxxのような、1~2までの数しか表せない。
                    
                    - 2進数で保存されていて、かつそこからbiasを引いた後の数xを「2^xのxだけ右or左に何個ズラす」という形で使用。
                        - 例) 10000001 → 129 → bias127引いて、2 → (2進数の状態で)2桁ずらす(=2^2倍する) 。
                            - fractionとして持っていた1.01(正確には0.01)を、2桁ずらして101に。
            

            - ここでいう精度とは何なのか
                - 前提: 小数は近似された状態で記憶される
                    - ほとんどの小数は 2 進法の分数として正確に表わすことができない
                    - 入力した10進数の小数は、2進法の浮動小数点数で近似された後、実際にマシンに記憶される
                    - つまり、実際の値に対してプラス方向かマイナス方向に誤差が乗った状態で記憶されている。
                    - https://docs.python.org/ja/3/tutorial/floatingpoint.html
                - 精度: 「相対誤差がXX%以下」が保証されている
                    - 相対誤差 = 誤差/値
                    - 例: 
                        - 7bitを仮数に使える
                        - →256通りの数を表せる。
                        - →1~2の間を、256分割して、数字を割り振る
                        - どんな数字Xも、先ほど割り振った数字のどれかが元になり、それを桁数ずらしして作ることになる
                        - 真のXとの誤差は、最大でも目盛り同士の間の距離、つまり1/256= 0.004であり、0.4%(実際にはさらにこれの半分以下)
                        - これを後から指数で何倍かするわけだが、誤差も同様に何倍かするので、相対誤差は0.4%以下のまま。
                    - 「相対誤差がXX%以下」は「有効桁数がZZ(ZZ桁目くらいまでは合ってるよ)」とも表現できる&そちらの方が直感的でわかりやすい
                        - ```
                            誤差 0.0000001　(誤差0.00001%)
                                vs
                            精度 7桁
                            → 後者の方が直感的
                          ```               


            - 使えるbitをどう割り振るか
                - 符号は1bitで十分なので、使える残りのbitをどのようにexponentとfractionに割り振るか、という問題になる。
                
                - 方針：扱う数字の範囲に最低限必ず必要な分をexponentに割いて、残りは全てfractionにする。

                - 必要な桁数: 科学・物理で出てくる数は大体 10^-30 〜 10^20
                    - 電子の質量：約 10^-31 kg, プランク定数：約 10^-34
                    - 太陽までの距離：約 10^11 m, 銀河サイズ：約 10^21 m
                
                - exponent は何bitあれば十分か
                    - 1.XXX * 2^exponent
                        - 1.XXXは2進数であり、10進数だと1~2の間。つまり一桁。
                        - 2^exponentの方を、10^yの形に直せば、桁数がわかる。

                    - exponentが7bitだったら、10^-19 〜 10^19　→ 少し足りない
                        -  2^7種類の数をexponentとして使える → -64 〜 +63
                        - max: 1.11... x 2^63  ≒ 2^64 ≒ 10^(64*0.3) ≒ 10^19
                        - min: 1.11... x 2^-64 ≒ 2^-63 ≒ 10^(-63*0.3) ≒ 10^-19
                            - 補足: log2 ≒ 0.3を使っている。これは、2^10 ≒　10^3から導ける。 

                    - exponent 8bitだったら、 10^-38 〜 10^38 → 十分
                        - 2^8種類の数をexponentとして使える → -128 〜 +127
                        - max: 2^128 ≒ 10^38
                        - min: 2^-127 ≒ 10^38


            - 結局何bitが良さそうか
                - 符号と仮数だけで、9bit必要
                - 9より上で、2^nだと、16,32,64あたり。(2^nだと、メモリアクセス時に都合が良い)
                - 16の場合
                    - 仮数に使える残り7bit(=16-9)
                    - 2^7 = 128 ≒ 10^2
                        - 1~2の間を約100通り(128通り)=10^2に分割。相対誤差は1/128なので1%弱
                        - つまり、下記のような感じで、有効数字2桁(小数点以下の部分が、2桁まで信じられる)
                            ```
                            1.00
                            1.01
                            1.02
                            ...
                            1.99
                            2.00
                            ```
                - 32の場合
                    - 仮数に使える残り24bit(=32-9)
                    - 2^24 ≒ 10^7
                        - 1~2の間を約10^7通りに分割。相対誤差は1/1０^7
                        - 桁数で考えると、7桁
                - 64の場合
                    - 仮数に使えるのは、55bit(64-9)
                    - 2^55 = (2^10)^5 * 2^5 ≒ 10^15 * 2^5 ≒ 10^16
                    - 1~2の間を約10^16通りに分割。相対誤差は1/10^16
                    - 桁数で考えると、16桁

                - 7桁以上必要になるニーズのイメージ
                    - とても似ている大きい数字同士の小さい差分を見たい。
                    - 東京駅-奄美大島の距離を測っている(約1200km)
                    - 従来の旧方式Aを、コストの安い新方式Bで代替したいが、どれくらい測定値に差分があるのか確認したい
                        - A：1234567.0 メートル
                        - B: 1234566.9 メートル
                            - km単位で見ると、両方1234kmになって同じになってしまう。
                            - m単位だと、0.1m、つまり10cm違うことがわかる。
                    - 人間が目で見てやれば、右端の差分だけ見て0.1で即答なのだが、コンピューターだとそうもいかない
                    - ここで float（約7桁）を使うと
                        - 両方1234567に丸められてしまい、差分が0になってしまう。
                        - 人間が直感的にやってる変換をやってあげるなどの工夫が必要
                            ```
                                base = 1234566.0
                                A' = A - base
                                B' = B - base
                            ```

   
        - TODO:
            - 大きい方から足す?理由


        - 誤差(計算誤り)が出る理由、
        - 扱える範囲を超えるとどうなるのか

 
- ちなみにasciiが7bitなのは
    - TODO


- 余談の中の余談
    - 1byteは最初1biteと書いてたが、うっかり1bitと書き間違ったりするかもしれないので、1byteにスペル変更したとのこと。
    - Strechの納品先はロスアラモス(映画「オッペンハイマー」で主人公が田舎に建ててた研究所/街。)
    - 昔のポケモンの努力値の上限は255で、中度半端な数字だなと思っていたが、きっと1byteのメモリ区画に納めたかったからなのだろうと納得。



- 余談:UTF-8
    - Unicode Transformation Format 8-bit
    - Unicode : 世界中のすべての文字に番号（コードポイント）を割り振ろう、という規格
    - Unicode Transformation Format : 文字（Unicode）をバイト列に変換するルール
    - 割り振ったコードは16進数で表される
        - A = U+0041
        - Aは0041番(ただし16進数)
    - なぜ16進数を使うのか
        - A.読みやすいから。
        - 詳細↓
        - まず、16進数は16種類の文字を使う。
            ```
            | 2進数 |10進数| 16進数 |
            | ---- | ---- | ---- |
            | 0000 | 0    | 0    |
            | 1001 | 9    | 9    |
            | 1010 | 10   | A    |
            | 1111 | 15   | F    |

            ```
        - 長くて読みにくい/覚えにくい2進数を、16進数の文字を使って、コンパクトに覚えやすくできる。(しかも難しい計算不要)
          ```
            110110100110 (=3094)がある。
            これを4bitごとに区切って、それぞれに該当する16進数の文字を当てる
            1101   1010   0110
             D      A      6

            →0xDA6(=3094)
          ```
        - なぜこんなマジックができるのか
            - 直感的な説明
                - 16進数は、4bitで表せる数字全てに1対1で記号を振っている。
                - 今`1101 1010 0110`という数字/文字列は覚えにくいから1対1の記号で置き換える
                - `DA6`になり、これを2進数に戻したいなら、また1対1の対応で戻せば良いだけ。(情報は増えても減ってもない)

            - 数学的な説明
                - DA6 = 13×16^2 + 10×16^1 + 6×16^0　　← 16進数の定義通りに分解
                -     = 13×2^8  + 10×2^4  + 6×2^0　 ← 13を8bitシフトしたもの　＋ 10を4bitシフトしたもの + 6
                - 13×2^8は、13を8bitシフトしたものなので、1101 の後ろに 8個ゼロをつける。
                    ```
                        13×2^8  → 1101 0000 0000
                        10×2^4  →      1010 0000
                        6×2^0   →           0110
                        足すと　　　1101 1010 0110
                    ```
        - なぜ16進数なのか。8進数や32進数でも良さそうだが。
            - 8進数
                - 3ビットずつ区切る`110 110 100 110`ことになる。
                    - 1バイト(8ビット)と相性が悪い
                        - 10101010 → 10 101 010　となって綺麗に分割できない。
            - 32進数
                - 5ビットずつ区切ることになる。
                    - 1バイト(8ビット)と相性が悪い(綺麗に分割できない)
                    - 文字が増えすぎて、覚えにくい（0-9 + A-V）






- https://github.com/hayashi-ay/leetcode/pull/71#discussion_r1582061879
    - >"".join(char for row in rows for char in row)　、ともかけるとのこと
    - これと同じ感じ。こう見ると、下の書き方を1行にまとめただけだとわかる。
        ```py
            result = []
            for row in rows:
                for char in row:
                    result.append(char)

            "".join(result)
        ```
    - 

# STEP3　　3回連続でエラーなしで解けるまで解く
<details><summary>コード</summary>
</details>
# STEP4 レビューFB反映