# 問題タイトル
[283. Move Zeroes](https://leetcode.com/problems/move-zeroes/description/)



# STEP 1 自力で解く。
```py
"""
左から数字を見ていく
0以外のものを見つけたら、max_non_zero_indexの次の0と交換する

[0011]
[1001]
[1100]
"""

class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        max_non_zero_index = -1
        for i in range(len(nums)):
            if nums[i] == 0:
                continue
            nums[max_non_zero_index + 1], nums[i] = nums[i], nums[max_non_zero_index + 1]
            max_non_zero_index += 1
        return nums 

```

# STEP2 読みやすくする＆他の人の解法を見る
- https://github.com/hayashi-ay/leetcode/pull/58/changes#diff-c279761355f0309f5c503a5494a18ea73bb072d26d7f39a4b951d76ba74a1a43R2
    - quickselectに言及している
        - 復習
            - k番めに小さい要素を高速に見つけるアルゴリズム
            - 手順
                - 1.適当にpivotを選ぶ
                - 2.pivotより小さいものと、大きいものの二つの配列に分ける
                - 3.小さいもの配列のサイズを見れば、そこにk番目があるのか、あるいは大きい方にk番目があるのかわかる。
                - 4.探しているものがある方だけ、再帰
            - 全部ソートしない点と、必要な方しか見に行かない点が、効率的
            - pivotが端っこになってしまうと、配列が二つに分けられず、効率が悪い

- https://github.com/fhiyo/leetcode/pull/54/changes#r1729230640
    - > まとめて 0 fill は、loop unrolling できたりするのでちょっと嬉しいこともあるでしょう。
    - nonzerosを別のリストにとっておいて、後でnumsを先頭から舐めて
    - loop unrolling
        - コンパイラが、単調なループを見つけたときに、効率よく処理できるように自動でちょっと組み替える
            ```c++
            for (int i = 0; i < n; i++) {
                a[i] = 0;
            }
            ```
            ```c++
            for (int i = 0; i < n; i += 4) {
                a[i]   = 0;
                a[i+1] = 0;
                a[i+2] = 0;
                a[i+3] = 0;
            }
            ```
        - i++や、比較、ジャンプといったオーバーヘッドの回数が減る

- https://github.com/fhiyo/leetcode/pull/54#discussion_r1729952172
    - exceptの後、何も書かない、という書き方もある。
    - > 何も書いていない except は ^C で送られてくるシグナルさえ捕まえます。
    タイミングよく ^C を送ることで、括弧の対応関係を変えられるプログラム、セキュリティー的にもまずそうではないですか。
    - 知らなかった。

- https://discord.com/channels/1084280443945353267/1322513618217996338/1356874787800223807
    - >next の第2引数の default を使えば例外がなくせますね。
    - >https://docs.python.org/ja/3/library/functions.html#next
        - >イテレータが使い尽くされた場合、 default が与えられていればその値が返されます。

- erase-remove idiom
    - ```py
        nonzeros = [x for x in nums if x != 0]
        nums[:] = nonzeros + [0] * (len(nums) - len(nonzeros))

        ```
    - 必要なものだけ前にまとめて(erase)、後ろのいらないものを0に。（まとめて0にするだけだから、CPU的に簡単で嬉しい）

```py
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        non_zeros = [num for num in nums if num != 0]
        nums[:] = non_zeros + [0 for _ in range(len(nums) - len(non_zeros))]
```
- `[0] * (len(nums) - len(nonzeros))`の方がシンプルでよかったかもしれない



# STEP3　　3回連続でエラーなしで解けるまで解く
- maxよりlastの方が、0でない数字の最後尾、という感じでイメージしやすいかと思ったので変更
```py
class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        last_non_zero_index = -1
        for i in range(len(nums)):
            if nums[i] == 0:
                continue
            nums[i], nums[last_non_zero_index + 1] = nums[last_non_zero_index + 1], nums[i]
            last_non_zero_index += 1
```


# STEP4 レビューFB反映